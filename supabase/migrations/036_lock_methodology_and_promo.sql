-- ===== MIGRATION 036: закрыть методологию и промокоды от прямого чтения юзерами =====
-- Решение владельца 16 июля. Две дыры из предзапускового аудита (M1/M4), отложенные
-- тогда именно потому, что обе завязаны на «горячие» пути чтения.
--
-- ⚠️ ПОРЯДОК: СНАЧАЛА задеплоить парный код, ПОТОМ применить эту миграцию.
--    Код уже переводит все легитимные чтения на service-role клиент:
--      • lib/ai/rag.ts            — match_knowledge_chunks + knowledge_chunks + knowledge_vault
--      • app/api/ai/warmup-plan   — knowledge_chunks + knowledge_vault
--      • app/api/referral         — promo_codes (чтение + счётчик uses_count)
--    Если применить ДО деплоя — генерация останется без методологии (контент станет
--    заметно хуже), а промокоды перестанут активироваться.
--
-- ────────────────────────────────────────────────────────────────────────────
-- 1. knowledge_chunks — методология (главный актив продукта)
-- ────────────────────────────────────────────────────────────────────────────
-- Было: SELECT разрешён ЛЮБОМУ authenticated (001:261, ослаблено в 002:146).
-- Значит любой оплативший (в т.ч. конкурент за $49) мог одним запросом к PostgREST
-- выкачать всю методологию мимо приложения — это и есть moat продукта.
-- Стало: читает только service-role (серверный RAG). Админ-политика на запись
-- («Admin manages knowledge chunks», 021) НЕ трогается — админка продолжает работать.
drop policy if exists "Authenticated can read knowledge chunks" on knowledge_chunks;

-- ────────────────────────────────────────────────────────────────────────────
-- 2. promo_codes — обход системы через чужой код
-- ────────────────────────────────────────────────────────────────────────────
-- Было: FOR SELECT USING (auth.role()='authenticated' AND is_active=true) (004:27) —
-- любой залогиненный получал СПИСОК всех активных промокодов с номиналами и мог
-- активировать «код для Пети». Адресные коды переставали быть адресными.
-- Стало: таблица не читается клиентом вовсе; код проверяется на сервере
-- (/api/referral service-role клиентом). Админ-политика «Admin can manage promo codes»
-- (FOR ALL, 004:21) остаётся — админка кодов работает.
drop policy if exists "Authenticated can read active promo codes" on promo_codes;

-- Примечание: promo_code_uses не трогаем — там своя политика («Users can insert own use»),
-- юзер должен уметь отметить своё использование.
