# AMA — контекст проекта (читать в начале новой сессии)

> «Память» проекта между сессиями. Весь КОД в git (источник правды).
> Здесь — то, чего в коде не видно: решения, статус, дорожная карта, ограничения.
> ОБНОВЛЯЙ этот файл по мере работы (особенно «Не доделано» и «Решения»).

## 🧭 МАСТЕР-ПЕРЕДАЧА В НОВУЮ СЕССИЮ (8 июля 2026) — ЧИТАТЬ САМЫМ ПЕРВЫМ

> Сессия 7-8 июля была большой: сделаны дружелюбные ошибки по всему приложению + ЦЕЛАЯ НОВАЯ ФИЧА
> «Экспресс-диагностика блога» с кучей итераций по фидбэку тестеров + инфра-правки. ВСЁ задеплоено в `main`,
> tsc/тесты/build зелёные на каждом шаге. Детали — в блоках «глубокая ночь-1…6» ниже, НЕ переделывать.

### ✅ ЧТО СДЕЛАНО ЭТОЙ СЕССИЕЙ (кратко, детали в блоках ниже)
1. **Дружелюбные ошибки** — `lib/friendlyError.ts` (прячет сырой тех-текст, показывает русские сообщения) +
   `authErrorMessage` (Supabase Auth → RU). 60 вызовов в 27 файлах + 4 auth-страницы. admin/* оставлены сырыми.
2. **Экспресс-диагностика блога (ГЛАВНАЯ ФИЧА)** — оценка Instagram по чек-листу «к продажам» (10 блоков, балл/10,
   диагноз, топ-пробелы, CTA на консультацию t.me/avavasilik). **ТРИ входа:** (1) кнопка+бейдж «X/10» в «Материалах»
   (диалог, по подключённому my_instagram), (2) карточка на странице проекта → `/projects/[id]/blog-audit`,
   (3) баннер на главной → `/blog-audit` (по ЛЮБОМУ @хендлу, скрейп на лету, БЕЗ проекта). Vision оценивает блок
   «Визуал» по картинкам. Верификатор рекомендаций (Haiku) убирает советы, противоречащие профилю. Без домыслов о
   продажах. `lib/blogAudit/*`, `app/api/blog-audit[/standalone]`, `lib/jobs/runBlogAuditJob.ts` +
   `runStandaloneBlogAuditJob.ts`, `components/projects/BlogAuditDialog.tsx` (+ `BlogAuditPanel`/`BlogAuditScorecard`),
   `components/blogAudit/StandaloneBlogAudit.tsx`. **Миграция НЕ нужна** (результат в jobs.result). Стоимость ~$0.13/аудит.
3. **OCR сканов/картинок в материалах** — `lib/ai/ocr.ts`: image-only PDF и картинки распознаются через Claude
   vision (`app/api/upload/route.ts`, maxDuration 120). Чинит «текст не распознался» на скриншотах отзывов.
4. **Клиентские ошибки → `/admin/errors`** — `logClientError` + `POST /api/client-error` + `instrumentation-client.ts`
   шлёт туда. Теперь ошибки ТЕСТЕРОВ видны ассистенту напрямую (source='client', с UA/URL). Проверено живьём.
5. **Баннер in-app браузера** — `components/shared/InAppBrowserNotice.tsx`: для Instagram/Telegram-webview (где ломается
   `new URL` → «URL is not a constructor»). Обычные браузеры не триггерят.
6. **Загрузка аудио** — полный текст ошибки + пред-проверка размера (`NEXT_PUBLIC_MAX_AUDIO_MB`, дефолт 50); миграция
   **030** (лимит бакета audio-temp 200 МБ) — ВЛАДЕЛЕЦ ПРИМЕНИЛ ✅.

### ⚠️ НЕЗАКРЫТЫЕ ДЕЙСТВИЯ ВЛАДЕЛЬЦА (не потерять — напомнить в новой сессии):
1. **Supabase Pro** (сейчас Free): оформить на карту Августы → поднять **Storage → Upload file size limit → 300 МБ**
   → в Vercel добавить env **`NEXT_PUBLIC_MAX_AUDIO_MB=300`** и передеплоить. БЕЗ этого аудио >50 МБ не грузятся.
2. **Перенос платных сервисов на Августу** — полный чек-лист в **`SERVICES_BILLING.md`** (Vercel, Supabase, Anthropic,
   OpenAI, Apify, домен; Prodamus — под РФ-данные Августы). Владелец делает сам (карты/оплата).
3. Миграции: все по **030** применены ✅. Новые — с 031.

### 🔜 ПРИОРИТЕТЫ НОВОЙ СЕССИИ
- Обрабатывать новый фидбэк тестеров по диагностике (читать `/admin/errors` — клиентские ошибки теперь там).
- Возможные фичи диагностики (по желанию владельца): tripwire для НЕзалогиненных (публичный лендинг), скрейп названий
  актуальных (доп. Apify-стоимость), задействовать лайки/комменты в разборе.
- Юр-обвязка/152-ФЗ — ОТЛОЖЕНА владельцем до этапа с оплатами.
- Push-напоминания — упаковка в тариф зафиксирована, сама фича НЕ построена (крупная работа с нуля).

### 📋 КОМАНДА ДЛЯ НОВОЙ СЕССИИ (скопировать и вставить):
```
Продолжаем AMA (amaproduct.com), /Users/macbook/project/ama. Прочитай PROJECT_CONTEXT.md
(блок «МАСТЕР-ПЕРЕДАЧА» вверху) и SERVICES_BILLING.md. НЕ поднимай решённое.
За прошлую сессию (7-8 июля): дружелюбные ошибки по всему приложению; целая новая фича
«Экспресс-диагностика блога» (3 входа: Материалы/страница проекта/баннер главной по @хендлу,
vision для визуала, верификатор рекомендаций, без домыслов о продажах); OCR сканов/картинок
в материалах; клиентские ошибки тестеров теперь в /admin/errors; баннер in-app браузера;
фикс загрузки больших аудио. Всё в main, задеплоено, tsc/тесты/build зелёные.
НЕЗАКРЫТО ВЛАДЕЛЬЦЕМ: Supabase Pro + лимит загрузки 300МБ + env NEXT_PUBLIC_MAX_AUDIO_MB=300;
перенос платных сервисов на Августу (SERVICES_BILLING.md). Приоритет — новый фидбэк тестеров
(клиентские ошибки читать в /admin/errors). Работай по своему усмотрению, спрашивай если нужно решение.
ВАЖНО: после любой RLS-правки тестируй СОЗДАНИЕ проекта живьём.
```

## 🚦 ПЕРЕДАЧА СЕССИИ (8 июля 2026, день) — ЧИТАТЬ ПЕРВЫМ

### ✅ Фидбэк тестера по диагностике блога (5 правок)
Тестер прислала свежий разбор @bridespanteleeva (score 4.4/10) + скрины с пометками. Сделано:
1. **Блок «Кейсы и доверие» → весь на консультации.** Кейсы почти всегда в ВИДЕО/ФОТО (Reels, карусели,
   скрины-отзывы), а не в тексте подписей — скрейп их не видит, оценка по тексту врала («точки А не видно»).
   Все 5 пунктов `cases` → `fromText:false` в `lib/blogAudit/checklist.ts`. Теперь блок показывает «на консультации».
2. **«Есть вовлечение» (Прогрев) → оценивается по КОММЕНТАРИЯМ, не по тексту подписей.** В скрейпе есть только
   ЧИСЛА комментов (💬 N у каждого поста), не их содержимое. Добавил в промпт `runBlogAudit.ts` инструкцию: судить
   вовлечение по активности 💬 относительно охвата/подписчиков, а не искать «вопрос/опрос» в подписях.
3. **Два заголовка укорочены** (тестер подчеркнула хвосты): «Есть понимание конкретной целевой аудитории~~, а не
   «для всех»~~» и «Позиционирование не банальное ~~(не только «помогаю похудеть…»)~~».
4. **ГЛАВНОЕ — скоркард переоформлен: одна оценка «X из 10» → разложение 100 баллов на 3 зоны.** Тестер: серая
   (неоцениваемая) зона занижала восприятие, читалась как «плохо» → манипулятивно. Теперь хедлайн — стек-бар из
   3 сегментов (сумма = 100): **зелёный** = набрано по видимой части (`scored`), **серый** = «на консультации»
   (`100 − assessableMax`, т.е. пункты, что честно нельзя оценить машинно — кейсы/актуальные/визуал/воронка),
   **жёлтый** = оценимая зона роста (`assessableMax − scored`). Диагноз-строка сохранена, большое «4.4 из 10» из
   скоркарда убрано. `BlogAuditScorecard` в `components/projects/BlogAuditDialog.tsx` (+ хелперы `ballWord`,
   `ZoneLegend`). Данные не меняли — зоны считаются из уже имеющихся `scored/assessableMax`; **миграция НЕ нужна**,
   кэш старых разборов рендерится корректно. Проверено на 5 реальных кэшах: green+grey+yellow = 100 везде.
   ⚠️ **Нюанс для тестера:** зелёный = СЫРЫЕ набранные баллы (у @bridespanteleeva = 32), а «44/100» в старом виде было
   нормализованное КАЧЕСТВО видимой части (32/72). В её макете было «44 зелёных» — это она прочла нормализ. число как
   баллы; аддитивно-честное зелёное = 32 (32+28+40=100). Бейдж «X/10» на карточке IG (из `GET /api/blog-audit`) НЕ
   трогали — там компактный тизер, `score10` остался в данных.
- tsc 0, 39 тестов, build ✓. Живьём тестером ещё не прокликано (нужен свежий прогон — старый кэш уже с новым баром).

### ✅ Добавление продукта прямо в мастере «План прогрева» (фидбэк владельца)
Мастер прогрева, Шаг 1 «Продукт» показывал тупик «Нет продуктов. Добавьте в настройках проекта», а
отдельной страницы добавления продукта после онбординга НЕТ (продукты создаются только в `ProjectWizard`).
Владелец: у проекта может быть несколько продуктов, прогрев нужен под один — дай добавить продукт прямо тут.
- **Новый action `create_product`** в `POST /api/projects` — вставляет в `products` И зеркалит в материал
  `product_description` (как `ProjectWizard`, т.к. модель читает `project_materials`, а не таблицу `products`;
  без материала продукт не попал бы в генерацию). RLS не трогал — существующие `products_write`/
  `project_materials_write` (`has_project_access editor`) уже разрешают вставку владельцу; юзер-скоуп клиент.
  **Создание проекта не затронуто.**
- **`WarmupWizard` Шаг 1:** инлайн-форма «Добавить продукт» (название/тип/цена ₽) → создаёт продукт → сразу
  выбирает его. Список продуктов теперь `allProducts = props.products + добавленные`. Кнопка-пунктир
  «+ Добавить продукт» и когда продукты уже есть (можно добавить ещё). `components/strategy/WarmupWizard.tsx`.
- tsc 0, 39 тестов, build ✓. ⚠️ Живьём не прокликано (нужен вход тестера) — но путь вставки идентичен онбордингу.

### ✅ «Не найти кнопку создания нового плана прогрева» (фидбэк тестера)
Тестер: планов прогрева может быть >1 (не пересекаясь по датам), но кнопки создать новый не найти. Причина —
**тупик на экране «🎉 Поздравляю»**: после сохранения плана `WarmupWizard` остаётся с `planSaved=true` (клиентское
состояние переживает `router.refresh()`), показывая только «Перейти в контент-план» — выглядит как «готово», не как
форма. Начать новый план без перезагрузки было нельзя.
- **Кнопка «Создать ещё один план прогрева»** на экране конгратов → `startOver()` (в него добавлен `setPlanSaved(false)`)
  → мастер сбрасывается на Шаг 1. + подпись, что планов может быть несколько, следить за непересечением по датам.
- Заодно фикс задвоения: после сохранения `router.refresh()` перечитывает `products` (уже с инлайн-добавленным),
  а `addedProducts` его тоже держал → `allProducts` теперь дедупит по id. `components/strategy/WarmupWizard.tsx`.
- tsc 0, 39 тестов, build ✓.

## 🚦 ПЕРЕДАЧА СЕССИИ (7 июля 2026, глубокая ночь-6) — ЧИТАТЬ ПЕРВЫМ

### ✅ Фидбэк тестера-2 по диагностике
1. **Модель не домысливает факты.** SYSTEM-промпт `runBlogAudit`: запрет утверждать продажи/доход/заявки/охваты
   (их данных нет) — формулировать как «блог не выстроен на продажу», а не «продажи проседают». Проверено живьём:
   summary стал «локация упакована отлично… блог не выстроен на продажу».
2. **Заголовки везде:** «Экспресс-диагностика блога» / «Оцени, насколько хорошо продаёт твой Instagram» — на
   `/blog-audit`, `/projects/[id]/blog-audit`, баннере дашборда и в диалоге «Материалов».
2b. **Город в жирной строке.** Модель писала «города нет», хотя PUNTA CANA есть. Причина — моя неточность в промпте
   («жирная строка = первая строка Bio»); на деле жирная строка = **fullName**, у нас в скобках строки
   «Аккаунт: @ник (…)». Промпт исправлен + локация распознаётся на любом языке/в любом виде. Проверено ×2: score=2.
3. **Метрики постов (ответ):** лайки/комменты (и просмотры рилз) УЖЕ есть в скрейпе (buildAccountText показывает
   ❤/💬 в каждом посте, модель их видит) — бесплатно, тот же вызов Apify. Репосты/сохранения/охваты Instagram
   публично НЕ отдаёт (это приватная статистика владельца) — получить нельзя ни за какие деньги через скрейп.

## 🚦 ПЕРЕДАЧА СЕССИИ (7 июля 2026, глубокая ночь-5) — ЧИТАТЬ ПЕРВЫМ

### ✅ Фидбэк тестера по диагностике: формулировка + верификатор рекомендаций
1. **Единая формулировка** «Обсуждается на консультации» для всех неоцениваемых пунктов (вместо разнородного
   «не видно из текста…») — в `runBlogAudit` (нота пункта) и в подписи `BlogAuditScorecard`.
2. **Верификатор рекомендаций** (2-й проход на **Haiku**, дёшево): выкидывает советы, противоречащие профилю —
   «добавь X», когда X уже есть. Тестер поймала: город PUNTA CANA в жирной строке, а совет «добавь город».
   `verifyGaps()` в `runBlogAudit.ts` + в основной промпт добавлена инструкция читать жирную строку и не выдумывать
   пробелы. **Проверено живьём** на @bridespanteleeva — «добавь город» ушёл.
3. **Актуальные — оставили вариант А** (на консультации). Apify-актор отдаёт только ЧИСЛО актуальных
   (`highlightReelCount`), а не названия/содержимое — подтверждено докой. Скрейп названий (отдельный актор, доп.
   стоимость) НЕ делаем пока (решение владельца/тестера).
4. **Стоимость 1 диагностики (оценка):** Apify-скрейп ~$0.004 (только автономная) + Claude Opus (аудит, вход ~10-12k
   с картинками, выход ~2.5k) ~$0.12 + Haiku (верификатор) ~$0.005 = **~$0.13 автономная / ~$0.10-0.12 проектная**.
   Доминирует Opus. Если понадобится дешевле под tripwire-объём — перевести аудит на Sonnet 5 (~вдвое дешевле), но
   потеряем в качестве; пока оставили Opus.

## 🚦 ПЕРЕДАЧА СЕССИИ (7 июля 2026, глубокая ночь-4) — ЧИТАТЬ ПЕРВЫМ

### ✅ Автономная диагностика блога с главной — по @хендлу, БЕЗ проекта (просил тестер)
Тестер: нужен вход в диагностику на главной (над проектами), чтобы проверять блог тем, у кого ещё нет проекта.
Сделано (по сути это и tripwire-основа Августы, но для залогиненных):
- **Баннер «Диагностика блога» на дашборде** (`dashboard/page.tsx`, над статами/проектами) → **страница `/blog-audit`**.
- **`components/blogAudit/StandaloneBlogAudit.tsx`**: ввод @хендла → `POST /api/blog-audit/standalone {handle}` →
  jobs+after() → **`lib/jobs/runStandaloneBlogAuditJob.ts`**: `scrapeInstagram` (Apify, на лету) → текст+картинки
  (`IMAGE_URLS_HEADER`) → `runBlogAudit`. Клиент поллит. **Проект НЕ нужен, материал НЕ создаётся** (только job).
- rate-limit **`blog-audit-standalone` 10/ч** (жжёт Apify, строже проектного 20/ч).
- **`BlogAuditScorecard`** вынесен из панели — единый вид результата у проектного, автономного флоу и диалога.
- **✅ Проверено ЖИВЬЁМ на проде:** баннер виден → страница → ввёл @natgeo → свежий скрейп+аудит → скоркард 4.4/10,
  **блок «Визуал» оценён по картинкам 10/10** (vision: «фирменный NatGeo стиль…»), неоцениваемых 9 вместо 14, CTA на месте.
- Итог: **ТРИ входа в диагностику** — (1) кнопка/бейдж в «Материалах» (диалог, по материалу проекта), (2) карточка на
  странице проекта → `/projects/[id]/blog-audit` (по материалу), (3) баннер на главной → `/blog-audit` (по @хендлу, скрейп на лету).

## 🚦 ПЕРЕДАЧА СЕССИИ (7 июля 2026, глубокая ночь-3) — ЧИТАТЬ ПЕРВЫМ

### ✅ Фидбэк тестера: «где вход в диагностику блога?» — вынесено на дашборд
Единственный вход был спрятан в «Материалы» → Instagram → и только при подключённом аккаунте → тестер не нашёл.
Теперь: **карточка «Диагностика блога» на главной странице проекта** (`projects/[id]/page.tsx`, рядом с Материалы/
Тренды/Визуал) → **отдельная страница `/projects/[id]/blog-audit`**. `BlogAuditDialog` отрефакторен — ядро вынесено
в переиспользуемый **`BlogAuditPanel`** (диалог в «Материалах» И страница используют его). На экране запуска — подсказка
«нужен подключённый Instagram». **✅ Проверено живьём на проде:** карточка видна, страница рендерит скоркард из кэша.

## 🚦 ПЕРЕДАЧА СЕССИИ (7 июля 2026, глубокая ночь-2) — ЧИТАТЬ ПЕРВЫМ

### ✅ Фидбэк тестера: загрузка аудио на «Исследование» — обрезанная ошибка + лимит размера
Тестер: 3 файла из 9 не загрузились, текст ошибки обрезан («The object ex…»). Причина: файлы **~77 МБ** (интервью
53 мин) превышают лимит загрузки Supabase → «The object exceeded the maximum allowed size». Плюс текст резался до
30 симв в правом span (`research/page.tsx:474`).
- **UI:** ошибка теперь показывается **ЦЕЛИКОМ** отдельной строкой под именем файла (не обрезается), через
  `friendlyUploadError()` — размер/формат/сеть маппятся в понятный русский текст.
- **Миграция 030** — поднимает `file_size_limit` бакета `audio-temp` до 200 МБ (в 011 был `on conflict do nothing`
  → 200 МБ могли НЕ примениться, если бакет уже существовал с дефолтом).
- ⚠️ **ДВА ДЕЙСТВИЯ ВЛАДЕЛЬЦА (без них большие файлы НЕ загрузятся):** (1) применить миграцию 030; (2) **поднять
  ГЛОБАЛЬНЫЙ лимит загрузки** проекта: Supabase Dashboard → Project Settings → Storage → «Upload file size limit» →
  ≥200 МБ. Глобальный лимит КЭПИТ лимит бакета; по умолчанию 50 МБ — именно он режет 77 МБ.
- Загрузка идёт напрямую в Supabase Storage по signed URL (мимо Vercel), так что 4.5МБ-лимит Vercel тут ни при чём.
- **Пред-проверка размера на клиенте** (research/page.tsx): ловит превышение ДО заливки, понятная ошибка с цифрами.
  Лимит — env **`NEXT_PUBLIC_MAX_AUDIO_MB`** (дефолт 50 = Free). **ПОСЛЕ апгрейда на Pro + поднятия лимита Storage в
  дашборде — выставить это же число в Vercel env** (напр. 300) и передеплоить, иначе клиент будет резать на 50 МБ.
- **Владелец берёт Supabase Pro** ($25/мес) — подходит. На Free проект ещё и засыпал после недели простоя; Pro убирает
  это + разблокирует настраиваемый лимит загрузки. PITR (для 152-ФЗ) — отдельный add-on, не сейчас; дневные бэкапы 7 дней в Pro есть.

## 🚦 ПЕРЕДАЧА СЕССИИ (7 июля 2026, глубокая ночь) — ЧИТАТЬ ПЕРВЫМ

### ✅ OCR материалов (скриншоты/сканы) + фидбэк проджекта + бейдж диагностики
**Проблема проджекта (реальный баг):** в «Кейсы и отзывы» грузили PDF-примеры отзывов — «текст не распознался».
Причина: это **image-only PDF (сканы/скриншоты без текстового слоя)** — `pdf-parse` возвращает пусто → 400
«PDF не содержит текста». А картинки (jpeg/png) вообще не обрабатывались. Раздел прямо приглашает грузить
**скриншоты отзывов**, так что правильный фикс — распознавать их через vision, а не убирать PDF.
- **`lib/ai/ocr.ts`** — `ocrPdf(buf)` / `ocrImage(buf, mediaType)` через Claude vision (MODEL=opus; PDF как document-блок
  base64 ≤32МБ, картинки как image-блок). `imageMediaType()` мапит имя/MIME в поддерживаемый тип.
- **`app/api/upload/route.ts`** (maxDuration 60→120): PDF — если текстовый слой пуст → **OCR-фолбэк** (скан читается
  как картинки); новая ветка для **изображений** (jpeg/png/webp/gif) → OCR. Оригинал файла всё равно сохраняется в
  Storage. **✅ Проверено живьём** на файлах проджекта: PDF-кейс Льва распознан дословно («Фитнес тренер… +150 000₽»),
  картинка тоже даёт текст.
- **Ярлык «Добавить текст (PDF, Google Docs)» → «Добавить текст»** (проджект: PDF/Google Docs там ни при чём, это поле
  диктовки/ручного ввода). Правлено в `KnowledgePageClient.tsx` и `KnowledgeUploader.tsx`.
- **Бейдж «X/10»** на карточке подключённого IG (из кэша `GET /api/blog-audit`) + кнопка становится «Перепроверить блог».
- tsc 0, 39 тестов, build ✓.

### ✅ КЛИЕНТСКИЕ ОШИБКИ ТЕПЕРЬ В `/admin/errors` (владелец: «чтобы ошибки тестеров ты видел сам»)
Раньше клиентские ошибки шли ТОЛЬКО в Sentry (панель владельца, у ассистента нет доступа). Теперь они пишутся и в
наш БД-лог, который читаю Я. `instrumentation-client.ts` (window.onerror/unhandledrejection) уже слал в Sentry с
`url`+`ua`; добавлено: он ЖЕ шлёт `POST /api/client-error` → `logClientError()` (lib/sentry.ts) → `error_events`
(**source='client'**, миграция НЕ нужна — у source нет CHECK). В контексте строки — страница (route) и браузер (ua).
`/admin/errors` и `GET /api/admin/errors` уже показывают source='client' + context. Владелец Sentry-доступ мне НЕ даёт
(нет интеграции) — этот лог и есть канал.

### 🔎 Диагностика бага `URL is not a constructor` (2 клиентские ошибки от тестеров)
ВСЕ `new URL(...)` в нашем коде — серверные (route-хендлеры); клиентского `new URL` у нас нет. Значит ошибку кидает
**рантайм Next-роутера / зависимость** в браузере без рабочего `URL`-конструктора — классика **устаревшего in-app
WebView** (Августины тестеры открывают ссылки внутри Instagram/Telegram). Добавил в `instrumentation-client.ts`
**проб**: `try{ new URL(...) }catch{ report('UnsupportedBrowser', ..., ua) }` → при следующем заходе такого браузера
в `/admin/errors` появится точный UA. Полифиллить вслепую в кастомном Next-форке рискованно, поэтому по просьбе
владельца сделан **баннер** `components/shared/InAppBrowserNotice.tsx` (в `app/layout.tsx`): детект (сломанный
`new URL` ИЛИ UA Instagram/FBAN/Telegram/TikTok/WeChat/`; wv)`) → дисмиссимый баннер «открой в Safari/Chrome» +
кнопка «Скопировать ссылку». **✅ Проверено живьём:** обычные Chrome/Safari НЕ триггерят (нет ложных срабатываний),
in-app UA (Instagram/Telegram/FBAN/Android-WebView) — матчатся. Скрытие в sessionStorage.

### ✅ vision для блока «Визуал» в диагностике блога — СДЕЛАНО
Раньше блок «Визуальная упаковка» (5 пунктов) всегда «на консультации» — скрейп давал только текст. Теперь:
- `scrapeAccount.ts::extractImageUrls()` собирает URL картинок (аватар `profilePicUrlHD` + обложки постов `displayUrl`);
  `runInstagramScrapeJob` дописывает их в `raw_content` под маркером `IMAGE_URLS_HEADER` (только для my_instagram).
- `runBlogAudit` парсит URL, тянет до 4 картинок (base64, UA-заголовок, ≤4.5МБ) и шлёт их как image-блоки в Claude —
  блок `visual` оценивается ПО КАРТИНКАМ; `notAssessableCount` авто-падает 14→9, пометка «N пунктов» и бейдж авто-обновляются.
- **✅ Проверено живьём:** с приложенными картинками visual реально скорится (assessableMax 10, заметки по реальным
  изображениям: «жёлто-золотая палитра…»), без картинок — остаётся «на консультации».
- ⚠️ **Уже подключённые аккаунты** (Августа и т.п.) скрейпились БЕЗ картинок → у них визуал остаётся «на консультации»
  до **переподключения** IG (кнопка «Перепроверить» не рескрейпит — нужно удалить и заново подключить аккаунт, чтобы
  сохранились URL картинок). Новые подключения получают оценку визуала сразу.

## 🚦 ПЕРЕДАЧА СЕССИИ (7 июля 2026, поздняя ночь)

### ✅ НОВАЯ ФИЧА: Диагностика блога к продажам (идея Августы + тестера-проджекта)
Чек-лист «блог к продажам» (PDF от Августы: 10 блоков × 5 пунктов × 0–2 = 100 баллов, 5 диагнозов по диапазонам).
Юзер жмёт «Диагностика блога» на подключённом Instagram → Claude оценивает блог → балл/10, диагноз, разбор по
блокам, топ-пробелы, **CTA на бесплатную консультацию с маркетологом** (t.me/avavasilik). Задумано как самопроверка
внутри продукта + будущий tripwire (лид-магнит, продающий созвон). **Tripwire в этой сессии НЕ делали** (владелец:
позже, когда продукт полностью готов) — сделан только внутрипродуктовый вариант.
- **Единый источник правды — `lib/blogAudit/checklist.ts`** (10 блоков, пункты, диапазоны диагноза). Дословно из PDF.
- **`lib/blogAudit/runBlogAudit.ts`** — Claude-рубрика (MODEL=opus), парсит JSON, СЧИТАЕТ суммы САМ (не доверяем
  арифметике модели, урок #39). **Ключевое честное решение:** скрейп даёт только `{bio, posts[]}` — актуальные
  (highlights), визуал/картинки и КУДА ведёт ссылка НЕ видны. Такие пункты (`fromText:false`, 14 из 50) детерминированно
  НЕ оцениваются (модель их не скорит), помечаются «проверим на консультации» → это и есть upsell-крючок. Балл
  нормализуется к 100 по оценённым пунктам (обычно 36 пунктов = 72 макс).
- **`app/api/blog-audit`** (POST enqueue job / GET кэш) + **`lib/jobs/runBlogAuditJob.ts`** — паттерн jobs+after()
  как у instagram-скрейпа. **МИГРАЦИЯ НЕ НУЖНА** — результат хранится в `jobs.result` (type='blog_audit'), кэш
  читается из jobs по (project_id, type, status). editor+ и rate-limit `blog-audit` 20/ч. Работает по УЖЕ
  подключённому my_instagram (без нового Apify-скрейпа, без затрат).
- **UI: `components/projects/BlogAuditDialog.tsx`** — скоркард (headline-балл, диагноз, прогресс, саммари,
  топ-пробелы, разбор по блокам с заметками по каждому пункту, блок «на консультации», CTA). Кнопка в
  `KnowledgePageClient` в состоянии «Аккаунт подключён» у my_instagram. CTA настраивается env
  `NEXT_PUBLIC_CONSULT_TELEGRAM` (дефолт `avavasilik` — то, что просил владелец, env ставить НЕ обязательно).
- **✅ Проверка:** tsc 0, 39 тестов (6 новых на чек-лист + 9 на friendlyError), build ✓. **Живой Claude-прогон**
  `runBlogAudit` на слабом тест-профиле → 1.1/10, корректный диагноз, острые заметки на «ты», неоцениваемые пункты
  честно помечены — весь конвейер (промпт→парсинг→скоринг→диагноз) верифицирован.
- **✅ ЖИВАЯ ПРОВЕРКА UI НА ПРОДЕ ПРОЙДЕНА (claude-in-chrome, admin-аккаунт владельца, тест-проект «sd»):** подключил
  @natgeo → кнопка «Диагностика блога» появилась → скоркард отрисовался (4.6/10, диагноз «Есть база, но смыслы и
  упаковка слабые», 46/100, острое саммари, 5 топ-пробелов, разбор по блокам с цветными точками, замки «Не видно из
  текста» на неоцениваемых пунктах, блоки «Актуальные/Визуал → на консультации», пометка «14 пунктов…», CTA →
  t.me/avavasilik подтверждён). Кнопка→диалог→job→поллинг→рендер работают end-to-end. Тест-аккаунт @natgeo удалён,
  проект «sd» в исходном виде. (Осталась 1 done-job blog_audit в jobs — безвредный кэш, chain-watch чистит >30 дней.)
- **Дальше (по желанию):** публичный tripwire-лендинг (аноним вводит @handle → скор → запись); бейдж «5/10» на
  карточке аккаунта из кэша; расширить скрейп (externalUrl, картинки → vision для блока «Визуал»), чтобы честно
  закрывать больше пунктов автоматически.



### ✅ СДЕЛАНО: дружелюбные ошибки по ВСЕМУ приложению (приоритет №2 прошлой передачи)
Владелец решил: **юр-обвязку/152-ФЗ отложить до этапа с оплатами** («пока нет оплат — не так важно»). Взялся за хвост
дружелюбных ошибок. Новый общий helper **`lib/friendlyError.ts`** (чистый, client-safe, покрыт 9 юнит-тестами
`tests/smoke/friendly-error.test.ts`):
- `friendlyError(error, fallback?)` — эвристика: **русский текст = наша копия для юзера → показываем как есть**
  (валидация, лимиты/квоты «Лимит исчерпан…», READ_ONLY, «сессия истекла»); **латиница/тех-маркеры = сырьё**
  (RLS `violates row-level security`, `Failed to fetch`, Postgres, стектрейсы) → прячем за `SERVICE_ERROR_MESSAGE`
  («Упс, ошибка сервиса — это на нашей стороне…»). Технический блок-лист ловит тех-текст даже с русским вокруг.
- `authErrorMessage(error)` — маппинг английских ошибок Supabase Auth в RU («Invalid login credentials» → «Неверный
  email или пароль» и т.д.), т.к. auth-ошибки юзер-actionable, их нельзя генерализовать.
- **Применено: 60 вызовов `toast.error(... instanceof Error ? .message ...)` в 27 user-facing файлах** →
  `friendlyError(...)` (сохранены fallback-строки и `{ duration }`); **4 auth-страницы** → `authErrorMessage`.
- **ОСОЗНАННО НЕ трогал `app/(dashboard)/admin/*`** (13 вызовов остались сырыми) — их вижу только я (админ), сырой
  тех-текст там полезен для отладки; фидбэк тестера про конечных юзеров.
- Проверка: **tsc 0, 33/33 теста, build ✓**. Правка чисто клиентская (копия тостов), RLS/миграции НЕ трогались →
  живой прогон создания проекта не требовался (нет RLS-правки). Логика выбора сообщения покрыта юнит-тестами.

### ✅/🔜 ПРИОРИТЕТ №3 (push в тариф) — УПАКОВКА ЗАФИКСИРОВАНА, ФИЧА ЕЩЁ НЕ ПОСТРОЕНА
Push-инфраструктуры **нет в коде вообще**: ни web-push/VAPID, ни service worker, ни PWA-manifest, ни напоминаний из
контент-плана. Владелец решил: **зафиксировать упаковку сейчас, фичу строить потом.** Сделано — добавлены строки в
`lib/generations-config.ts` PLAN_CONFIG: Pro → «Push-напоминания из контент-плана (при запуске)»; Producer →
«Автопостинг + видео + push-напоминания (при запуске)». Это Про+ (в Соло/триал не входит), помечено «при запуске» как
автопостинг/видео. **САМА ФИЧА (web-push/PWA ИЛИ Telegram-бот, напоминания из контент-плана) — крупная работа с нуля,
НЕ начата.** Когда будем строить — сначала выбрать канал (PWA-push vs Telegram); напоминания вешать на даты из
контент-плана (content_items). Это следующий кандидат в разработку после юр-обвязки.

## 🚦 ПЕРЕДАЧА СЕССИИ (7 июля 2026, ночь)

### 🔜 СЛЕДУЮЩАЯ СЕССИЯ — ПРИОРИТЕТЫ (согласовано с владельцем)
Продукт ГОТОВ. Остаток — запусковые решения владельца + юр-обвязка, не разработка. По приоритету:
1. **ПРАВО/ДАННЫЕ (следующий продуктовый приоритет):** оферта, политика 152-ФЗ, чеки (уточнить у Продамуса), PITR-бэкапы
   Supabase, экспорт данных юзера. БЕЗ этого нельзя легально брать деньги в РФ.
2. **Хвост UX-фидбэка тестера:** пройтись по всем `toast.error(error.message)` в приложении → дружелюбное «ошибка сервиса,
   это на нашей стороне» вместо сырого тех-текста (в мастере создания уже сделано, см. ниже).
3. **Push-напоминания** — ВКЛЮЧИТЬ В ТАРИФ (Про+), НЕ отдельной докупкой (решение владельца).
4. Активация биллинга (живой РФ-платёж Продамус → BILLING_ENFORCED) — решение владельца, код спит.
5. PostHog — отложено до объёма юзеров. Лендинг/партнёрка/оптимизация маржи — позже.
Полный разбор с обоснованием — блок «🎯 СТРАТЕГИЯ И ПРИОРИТЕТЫ» ниже. Чек-лист владельца (то, что делает он сам) —
владельцу оформить отдельно (запрашивал `LAUNCH_CHECKLIST.md`, НЕ создан — сделать в новой сессии, если попросит).

### Готовая команда для новой сессии (скопировать и вставить):
```
Продолжаем AMA (amaproduct.com), /Users/macbook/project/ama. Прочитай PROJECT_CONTEXT.md
(блок «ПЕРЕДАЧА СЕССИИ» вверху) и AUDIT_REGISTRY.md. НЕ поднимай решённое.
За прошлую сессию (7 июля): фоновые задачи для IG-скрейпа/рилз, мультипользовательские
проекты (роли+приглашения, миграции 025-027), read-only режим для гостя, встроенный лог
ошибок /admin/errors (миграция 028), починка создания проектов (миграция 029). Все миграции
по 029 применены. Мой админ = producer+admin (максимум). Приоритеты новой сессии — блок
«СЛЕДУЮЩАЯ СЕССИЯ» вверху PROJECT_CONTEXT: (1) юр-обвязка/152-ФЗ, (2) хвост дружелюбных
сообщений об ошибках по всему приложению, (3) push в тариф. Работай по своему усмотрению,
спрашивай если нужно решение. ВАЖНО: после любой RLS-правки тестируй СОЗДАНИЕ проекта живьём.
```


### ✅ ВСЕ МИГРАЦИИ СЕССИИ ПРИМЕНЕНЫ (025, 026, 027, 028, 029) — проверять новые с 030+.
🔴→✅ **029 (создание проектов) — ПОЧИНЕНО И ПРОВЕРЕНО ЖИВЬЁМ.** Регрессия от 026: создание ЛЮБОГО проекта падало
(«new row violates RLS for table projects»). Мастер делает `.insert().select()` (INSERT RETURNING), а `projects_select`
(026) проверял владельца через функцию, которая ЗАНОВО запрашивает `projects` по id → только что вставленная строка
не видна под-запросу → 42501. Фикс 029: `owner_id = auth.uid()` проверяется НАПРЯМУЮ (колонка новой строки видна в
RETURNING), членство — через `is_active_project_member` (DEFINER). Проверено REST-тестом под реальной сессией:
return=minimal→201, до фикса representation→403, после 029 representation→201. Системно проверил тот же класс: только
`projects` ссылалась на себя в SELECT-политике; `project_members` безопасна (владелец проходит через is_project_owner,
читает projects а не вставляемую строку — приглашение работает живьём); контент-таблицы читают projects (чужую строку).

### ⚠️ УРОК ДЛЯ БУДУЩИХ RLS-ПРАВОК (не повторять):
1. **SELECT-политика таблицы НЕ должна запрашивать СВОЮ ЖЕ таблицу через функцию/под-запрос**, если в неё делают
   `.insert().select()` (INSERT RETURNING) — вставленная строка не видна вторичному запросу → 42501. Проверять владельца
   по колонке напрямую.
2. **После любой RLS-правки тестировать СОЗДАНИЕ нового проекта с нуля**, а не только доступ к существующим (эту дыру
   пропустил именно потому, что тестил только доступ).
3. RLS-баги ловятся ТОЛЬКО на живой БД (tsc/тесты/сборка их не видят). После RLS-миграции — обязательный живой прогон.

### 📝 ФИДБЭК ТЕСТЕРА (частично сделано, есть хвост):
Тех-ошибки (не по вине юзера) надо показывать явно «это на нашей стороне, подождите», а не сырой текст — иначе юзер
думает, что сам что-то не так заполнил. **СДЕЛАНО в мастере создания проекта** (commit 33ccf85: «Упс, ошибка сервиса
— это на нашей стороне…»). **ХВОСТ (на будущее):** пройтись тем же принципом по остальным `toast.error(...)` с сырым
`error.message` по всему приложению (генерация, загрузки, сохранения) — сейчас многие показывают технический текст.

### ✅ SENTRY-ЭТАП: ВСТРОЕННЫЙ ЛОГ ОШИБОК (7 июля)
У ассистента нет доступа к панели Sentry владельца → раньше «ошибка → письмо Sentry → ручная пересылка мне».
Закрыто встроенным логом, который Я ЧИТАЮ НАПРЯМУЮ. Таблица `error_events` (admin-read RLS, миграция **028**);
`lib/sentry.ts::captureException/captureMessage` теперь ПОМИМО Sentry пишут серверные/фоновые/cron-ошибки в БД
(best-effort через service-role, НИКОГДА не бросает, не зовёт captureException → нет петли; если таблицы нет —
молча пропускает). Клиентские ошибки в БД НЕ пишутся (остаются в Sentry). Просмотр: **`/admin/errors`** (ссылка
в сайдбаре под Admin) + **`GET /api/admin/errors?limit=200&level=`** — этот эндпоинт Я читаю сам когда владелец
скажет «сломалось», без пересылки. `DELETE /api/admin/errors[?id=]` — очистка; chain-watch чистит >30 дней.
Sentry (письма-алерты владельцу) — второй слой, без изменений. **✅ ПРОВЕРЕНО ЖИВЬЁМ (7 июля):** миграция 028
применена, засеял тестовую запись через service-role → `/admin/errors` под админом отобразил её со всеми полями,
эндпоинт вернул 200 только админу → тестовую запись удалил, лог чист. Связка запись→admin-чтение→отображение работает.

### 🎯 СТРАТЕГИЯ И ПРИОРИТЕТЫ (7 июля, СОГЛАСОВАНО С ВЛАДЕЛЬЦЕМ) — читать перед планированием
Продукт ГОТОВ (фичи есть, launch-blocker'ы закрыты, наблюдаемость настроена). Осталось — в основном ЗАПУСКОВЫЕ
РЕШЕНИЯ ВЛАДЕЛЬЦА и юр-обвязка, не разработка. Решения этой сессии:
- **PostHog (продуктовая аналитика) — ОТЛОЖИТЬ** до пост-запуска с объёмом. Пока ~30-50 тестеров и прямой контакт
  Августы с ними — качественный фидбэк ценнее графиков. Не строить сейчас.
- **Push/напоминания — НЕ платная докупка.** Владелец предлагал докупку; РЕШЕНО (владелец согласился): это фича
  УДЕРЖАНИЯ, прятать её за отдельную покупку вредно (она снижает отток → работает на нас). Включать в тариф (Про+), не микро-докупкой.
- **Цены — НЕ менять сейчас.** Маржа на текущей модели (Opus 4.8) здоровая: Соло ~65%, Про ~68%, Продюсер ~46% —
  на $49 НЕ в минус. «$50 мало» — про захват ценности, не выживание. Пересмотреть ПОСЛЕ запуска на данных
  конверсии. Следить за тарифом «Продюсер» (тонкая маржа 46% + тяжёлая нагрузка) при добавлении дорогих фич.
- **Приоритет остатка (по важности):** (1) активация биллинга — живой РФ-платёж Продамус → `BILLING_ENFORCED=true`
  (решение владельца, код спит); (2) пересмотр цен вместе с (1) на данных; (3) **ПРАВО/ДАННЫЕ: оферта, политика
  152-ФЗ, чеки, PITR-бэкапы Supabase, экспорт данных юзера** — БЕЗ ЭТОГО НЕЛЬЗЯ ЛЕГАЛЬНО БРАТЬ ДЕНЬГИ В РФ, часто
  забывают; из чисто-продуктового это следующий приоритет; (4) push в тариф; (5) PostHog при объёме; (6) лендинг/
  партнёрка; (7) оптимизация маржи (кэш промпта #38, картинка как единица).

**✅ ПРИОРИТЕТ №1 ПРОШЛОЙ СЕССИИ СДЕЛАН: фоновые задачи для Instagram-скрейпа и виральных рилз (commit 089c0d5, main, задеплоено).**
Тот же паттерн `jobs` + `after()`, что для транскрипции (roadmap №8), теперь и на `/api/instagram/scrape` и `/api/viral-reels` (POST). Оба роута теперь только валидируют вход и создают строку в `jobs`, обработка (Apify-скрейп → [для рилз] Whisper-транскрипция → Claude-анализ → запись в `project_materials`/`viral_reels`) идёт через `after()` — клиент только поллит `GET /api/jobs/[id]`, больше не держит SSE-соединение открытым (тот же выигрыш в надёжности: заблокированный/свёрнутый телефон не роняет анализ). В отличие от транскрипции, самопродолжение (`/api/jobs/continue`) НЕ нужно — один скрейп+анализ укладывается в `maxDuration=300` за один заход. Общая скрейп-логика вынесена в `lib/instagram/scrapeAccount.ts` и `lib/reels/scrapeReel.ts`, раннеры — `lib/jobs/runInstagramScrapeJob.ts` / `lib/jobs/runViralReelJob.ts`, клиентский поллер — `lib/jobs/pollJob.ts` (используется в `InstagramAccountDialog.tsx` и `ViralReelsManager.tsx`; `research/page.tsx` не трогал — там свой самостоятельный поллер, работает, регрессировать незачем).
**Миграция НЕ нужна** — `jobs.type` (024_jobs.sql) не имеет CHECK-констрейнта, только `jobs.status` его имеет.
**✅ Проверено живьём на проде (аккаунт Августы, claude-in-chrome):** добавлен тестовый конкурент `@natgeo` через новый `/api/instagram/scrape` → job создался → after() отработал → поллинг `GET /api/jobs/[id]` (10 запросов, все 200) → материал сохранён → диалог закрылся с успехом. Тестовый рилз (`instagram.com/reel/DYrJtH1NWD4/`) через новый `/api/viral-reels` → job `done` → Whisper-транскрипт пришёл («Как удержать мужчину?...») → сохранено в `viral_reels`. Побочно замечено (НЕ регресс, поведение идентично старому SSE-коду): на этом конкретном рилз Claude вернул не-JSON текст → `analysis`/`reel_type` не распарсились → упало на дефолт «Виральный рилз» без анализа — это существовавший ограничитель парсинга промпта, не связан с переходом на jobs. Оба тестовых артефакта (`@natgeo`, тестовый рилз) удалены после проверки.

### ✅ ЗАДАЧА №2 ЭТОЙ СЕССИИ СДЕЛАНА: мультипользовательские проекты (роли + приглашения по email)
Раньше этого не было вообще — каждый проект жёстко привязан к одному `owner_id`, RLS **везде** проверяла только его.
Проверено владельцем 7 июля: тариф «Продюсер» уже ОБЕЩАЕТ «команда 3–5 + клиентский доступ», «Про» — «+1 место» (см.
`lib/generations-config.ts` PLAN_CONFIG) — а функциональности не было. Полный план — `/Users/macbook/.claude/plans/zazzy-purring-ladybug.md`.

**⚠️ МИГРАЦИЯ 025_project_members.sql — ВЛАДЕЛЬЦУ ПРИМЕНИТЬ через SQL Editor (как 018-024), иначе фича не работает.**
Добавляет: таблица `project_members` (project_id, user_id?, invited_email, role editor|viewer, status pending|active),
функция `has_project_access(project_id, min_role)`, RLS на `projects`/`project_members` (SELECT — owner+member;
settings/удаление проекта — owner-only), ретрофит RLS на 11 project-scoped таблиц (`project_materials`,
`project_chunks`, `products`, `funnels`, `warmup_plans`, `content_plans`, `content_items`, `content_versions`,
`ai_conversations`, `style_examples`) + `viral_reels`/`content_trends` (project-scope ветка) + `saved_content`
(добавлена read-видимость команде, запись осталась личной). Триггер на `profiles` (after insert) авто-привязывает
pending-приглашение по email при регистрации — реального письма НЕ нужно (RESEND_API_KEY всё ещё не стоит), хотя
письмо-уведомление тоже пробуется через `lib/email.ts` (тихо no-op без ключа, как письма конца триала).

**Роли:** owner (неявно, `projects.owner_id`, полный доступ + управление командой + danger zone) · editor (весь
контент, не может пригласить/удалить проект) · viewer («клиентский доступ», только чтение).

**Ключевой архитектурный вывод (важно для будущих правок):** RLS — реальная граница безопасности. НО ~10 роутов
пишут через **admin/service-role клиент** (обходит RLS): `brand-kit` (route/upload/analyze), `video/upload-url`,
`video/overlay`, `materials` DELETE (storage), `materials/[id]/file` (signed URL), `stories/sets`,
`instagram/scrape`, `viral-reels` POST, `jobs/transcribe` — там app-код через `requireProjectAccess(supabase,
projectId, userId, 'editor'|'viewer')` (`lib/projects/access.ts`) — ЭТО и есть граница, не RLS. Плюс ai/* роуты
(chat/generate/edit/suggest-*/analyze-competitors/research-analyze/extract-tone-of-voice/generate-image/
generate-week-brief/plan-stories/edit-stories) явно требуют editor+ — они жгут реальные деньги (Claude/OpenAI) и
не все из них вообще пишут в RLS-таблицу (viewer иначе мог бы палить чужую квоту). Все ОСТАЛЬНЫЕ ~15 роутов —
просто убран избыточный `.eq('owner_id', user.id)`, RLS фильтрует сама (тот же результат для owner, + доступ
участникам). Осознанно НЕ трогал: `app/api/projects/route.ts` (PATCH настройки/DELETE — owner-only, danger zone),
`materials/route.ts` GET + `materials/import/route.ts` (это «переиспользовать материалы из МОИХ ДРУГИХ проектов»,
намеренно owner_id-scoped — иначе продюсер-редактор одного клиента мог бы утащить материалы в проект другого).

**UI:** `/projects/[id]/team` (owner-only, скрыт если `teamSeats=0` на тарифе) — `components/projects/TeamMembers.tsx`
(инвайт по email + роль, список команды, смена роли, удаление). Кнопка «Команда» на `/projects/[id]` видна только
владельцу при `teamSeats>0`. `getProjectRole()`/`requireProjectAccess()` в `lib/projects/access.ts` — UI-гейтинг,
не сама граница безопасности (это RLS).

**Не доделано (осознанно, безопасно отложено — RLS уже расширяет доступ, эти места просто продолжат писать «not
found» участнику до правки, НЕ дыра в безопасности):** длинный хвост нишевых роутов — `admin/context-inspector`,
`admin/context-backfill` (остаются owner+admin, диагностика), `carousel/render`, `carousel/structure`,
`projects/scrape-product`, `projects/autofill`, `content/route.ts`. Плюс UI других страниц (materials upload,
content-plan, visual) пока не скрывает кнопки редактирования от viewer — RLS всё равно заблокирует запись, но
сообщение об ошибке будет не самое дружелюбное («new row violates row-level security policy»).

**✅ ЖИВАЯ ПРОВЕРКА ПРОЙДЕНА ПОЛНОСТЬЮ (7 июля, аккаунт Августы + QA-аккаунт):** миграции 025→**026**→**027**
применены владельцем. По ходу проверки НАШЛИ И ПОЧИНИЛИ 2 бага одного класса (оба — рекурсия RLS, tsc/тесты их не
ловят, т.к. это поведение живой БД):
  - **026_fix_project_rls_recursion.sql** — `projects` и `project_members` ссылались друг на друга в своих RLS-политиках
    напрямую (живой подзапрос) → Postgres «infinite recursion detected» → у Августы пропал её проект (dashboard 0
    проектов). Фикс: обёртка проверок в SECURITY DEFINER-функции (`is_project_owner`, `has_project_access` стала DEFINER),
    их внутренний запрос обходит RLS вместо повторного триггера.
  - **027_fix_project_members_self_recursion.sql** — `project_members_read` проверяла «есть ли у меня активная запись»
    подзапросом к самой `project_members` изнутри её же политики → та же рекурсия → приглашение падало 500 (хотя простой
    GET ростера случайно работал). Фикс: `is_active_project_member()` SECURITY DEFINER.
  - **После обоих фиксов — E2E вживую:** пригласил `ama-qa-bot@gmail.com` редактором → запись создалась (роль/статус
    active) → **залогинился под QA-аккаунтом** → проект Августы виден в его списке (своих проектов у него 0) → открыл
    проект, материалы, ассистента → **реально сгенерировал контент голосом Августы**. Раздел «Команда» корректно скрыт
    от не-владельца. Всё работает как задумано.
  - ⚠️ **Тариф Августы переключён на `producer`** (по прямой просьбе владельца — она основной партнёр, тестирует ВСЕ
    функции; статус `trialing` не тронут). `ama-qa-bot` остался редактором в её проекте (тест-артефакт, безвредный —
    можно удалить на странице «Команда», если мешает).

### 🔑 QA-АККАУНТ (внутренний, НЕ чужой — создан прошлой сессией Claude Code для смоук-тестов)
`ama-qa-bot@gmail.com` / `QaTest!23456` — доверенный тестовый аккаунт, заведён ~7 июня для проверок логина/интерфейса
под авторизацией (см. историю ниже: «QA-юзер оставлен, переиспользуемый»). Реального человека за ним нет. Использовать
для живых проверок вместо того, чтобы трогать реальных тестеров Августы. Тариф сейчас `trial`. Это НЕ подозрительный
аккаунт — если в будущем всплывёт вопрос «что это», ответ здесь.

### ✅ ХВОСТЫ ФИЧИ КОМАНДЫ + РЕГРЕСС-АУДИТ (7 июля, вечер)
**Пункт 1 (доступ редакторам к хвостовым маршрутам):** проверено — редактор нигде реально не заблокирован, правок
НЕ требовалось (отложенные маршруты либо не-проектные: autofill/scrape-product онбординга, carousel render/structure
stateless auth-only; либо уже RLS-editor+; либо намеренно admin/owner-only диагностика).
**Пункт 2 (viewer не видит технических ошибок) — БЕЗОПАСНЫЙ вариант сделан** (commit e0d2399): read-only баннер для
роли viewer через новый `app/(dashboard)/projects/[id]/layout.tsx` (`components/projects/ReadOnlyBanner.tsx`) +
`isRlsError()`/`READ_ONLY_MESSAGE` в `lib/projects/access.ts` → дружелюбное «доступно только редакторам» вместо
сырого RLS-текста на прямых write-путях (content-items, content delete, content/results, voice-rules, style-bank,
trends). **ПОЛНОЕ скрытие всех ~45 кнопок редактирования от viewer — ОСОЗНАННО ОТЛОЖЕНО** отдельной аккуратной
задачей (нет общего контекста роли, протяжка через 11 страниц = риск регрессии; баннер+дружелюбные ошибки закрывают
суть безопасно).
**РЕГРЕСС-АУДИТ (независимый агент, состязательный) — нашёл и ПОЧИНИЛ 2 High (commit после e0d2399):**
  - **H1** `ai/warmup-plan` — я убрал owner-проверку, но ЕДИНСТВЕННЫЙ из ai-маршрутов забыл добавить
    `requireProjectAccess(editor)` → viewer мог запускать дорогую Claude-генерацию плана на деньги владельца. Добавлено.
  - **H2** `projects/scrape-social` — дорогой скрейп (Apify) запускался ДО RLS-gated записи → viewer жёг бы деньги,
    падая только на сохранении. Добавлен `requireProjectAccess(editor)` до скрейпа.
  - Побочно: M1 — `isRlsError` теперь по коду Postgres `42501` (точнее текста); L3 — style-bank тоже дружелюбная ошибка.
  - **Аудит подтвердил ЧИСТО:** нет остаточной RLS-рекурсии; НЕТ пути, где не-участник читает/пишет чужой проект;
    цепь генерации контента для владельца цела (rag.ts всегда был project_id-scoped, не owner_id); `.single()` нигде
    не падает (Supabase возвращает null, не throw); старые pre-025 политики (`viral_reels_system_write` и т.п.) — УЖЕ
    OR'd и строго уже (admin/self), не шире.
  - Осталось НЕ критично (осознанно): M2 seat-limit TOCTOU-гонка (owner-only, безобидный перебор счёта); L2
    materials/import owner-only (намеренно — редактор не должен тащить материалы между чужими проектами).

## 🚦 (архив) ПЕРЕДАЧА СЕССИИ (7 июля 2026, вечер)

**Что произошло за 1–7 июля:** полный причинно-следственный предзапусковый аудит продукта (см. блок «ЭТАП 1» ниже + `AUDIT_REGISTRY.md`) → все launch-blocker'ы починены и живьём проверены на проде (аккаунт Августы) → нашли и починили КРИТИЧЕСКИЙ баг (методология сервиса никогда не векторизовалась — RLS) → построили и реализовали roadmap к масштабу: CI, Sentry, rate-limiting, сторож цепи (`chain-watch` cron), замкнут цикл обучения на результатах, приватный бакет materials, **фоновая транскрипция** (roadmap №8, `after()`-паттерн, проверено живым циклом), защита контента от занижения охватов Instagram/Meta. Всё в `main`, задеплоено, `tsc`+24 теста (`npm test`)+`build` зелёные на каждом шаге.

**Миграции применены владельцем по 024 включительно** (018 voice_rules, 019 content_trends, 020 materials bucket, 021 knowledge_chunks RLS, 022 rate_limits, 023 saved_content_results, 024 jobs). Если появится 025+ — применить так же через SQL Editor.

### ‼️ РЕШЕНИЕ ВЛАДЕЛЬЦА: биллинг-гейтинг НЕ включать по своей инициативе
Триал сейчас НЕ ограничен намеренно (тестеры Августы работают свободно, она сама платит за инфраструктуру). `BILLING_ENFORCED` и письма о конце триала — код готов и спит, включать ТОЛЬКО по прямой команде владельца (Mathew). НЕ поднимать это как «незакрытый гэп».

### 🔜 ПРИОРИТЕТЫ СЛЕДУЮЩЕЙ СЕССИИ (по важности)
1. **Распространить фоновые задачи (`jobs` + `after()`) на Instagram-скрейп и виральные рилзы** — тот же паттерн, что для транскрипции (см. `lib/jobs/`), сейчас эти два пути всё ещё client-orchestrated SSE. Осознанно отложено с прошлой сессии (объём работы ≈ то, что уже сделано для транскрипции — начинать со свежим контекстом).
2. **Мультипользовательские проекты для тарифа «Продюсер»** — ПРОВЕРЕНО: этого НЕТ и никогда не было (владелец сам поднял вопрос 7 июля). Тариф продаёт «10 проектов», но это просто лимит количества проектов у ОДНОГО аккаунта — RLS everywhere завязан только на `owner_id`, нет ни ролей, ни приглашений, ни общего доступа. Тариф обещает функциональность агентства, которой физически не существует. Нужно: таблица `project_members` (роли владелец/редактор/просмотр), приглашение по email, RLS-политики на членство. Реальный незакрытый гэп продукта, не косметика.
3. **Перед запуском на 30-50 тестеров — владельцу проверить самому (не код):** тариф Supabase (лимиты подключений на бесплатном плане) и лимиты запросов/мин аккаунтов Anthropic/OpenAI (мой rate-limiting защищает от одного абьюзера, но не от суммарной нагрузки всех тестеров разом). Vercel — подтверждён план Pro (виден в скриншоте владельца), maxDuration=300 работает штатно.
4. Дальше по roadmap: продуктовая аналитика (PostHog — сейчас ноль данных о воронке), PWA-push/TG-напоминания из контент-плана, оптимизация кэша промпта (#38, отложена как некритичная).

### Готовая команда для новой сессии (скопируй и вставь):
```
Продолжаем AMA (amaproduct.com), /Users/macbook/project/ama. Прочитай PROJECT_CONTEXT.md
(блок «ПЕРЕДАЧА СЕССИИ» вверху) и AUDIT_REGISTRY.md. НЕ поднимай решённое.
Задача 1: распространи паттерн фоновых задач (lib/jobs/, after()-self-continuation)
на Instagram-скрейп (app/api/instagram/scrape) и виральные рилзы
(app/api/viral-reels) — по образцу транскрипции. Проверь живьём на проде
(аккаунт Августы через claude-in-chrome), как делали для транскрипции.
Задача 2: мультипользовательские проекты для тарифа Продюсер (роли
владелец/редактор/просмотр, приглашение по email) — сейчас этого нет вообще,
RLS завязан только на owner_id.
Работай по своему усмотрению, спрашивай, если нужно решение.
```

## 🧭 ЭТАП 1 (КОД) — ПРЕДЗАПУСКОВЫЙ АУДИТ ВЫПОЛНЕН (1 июля 2026, вечер). Реестр → `AUDIT_REGISTRY.md`
Мульти-агентный причинно-следственный аудит 15 подсистем + состязательная верификация (3 линзы). **Полный реестр с доказательствами (file:line + git) и фиксами — в `AUDIT_REGISTRY.md`** (durable, не терять).
- **41 выжившая находка:** 🔴 8 launch-blocker · 🟠 8 high · 🟡 11 medium · ⚪ 14 low. Плюс 14 СЫРЫХ (billing/infra/visual-engine — верификаторы упали на 5-час лимите, нужен дешёвый повторный verify перед фиксом).
- **ГЛАВНОЕ (разрывы цепи ядра — доказано):** несколько ключевых звеньев НЕ доходят до генерации:
  - `autofill` — вообще не пишет в `project_materials` (только поля формы) → профиль не доходит. [LB#1]
  - `scrape-social` — пишет `material_type:'other'` для ВСЕХ платформ, а `'other'` НЕТ в `ALWAYS_INCLUDE` → соцсети (вкл. свой IG) не доходят. [LB#2]
  - `scrape-product` — пишет в таблицу `products`, а не в `project_materials` → продукт не доходит. [LB#3]
  - `interview_transcript` обрезается до 3000 симв в rag.ts → ~95% интервью теряется. [LB#4]
  - `voice_rules` НЕТ в CHECK-констрейнте `project_materials` → INSERT падает, фича мертва для новых проектов. [LB#5/#6]
  - `content_trends` — миграция 013 не создаёт колонки `scope`/`project_id`, которые запрашивает код + RLS запрещает юзеру писать свои тренды. [LB#7/#8]
  - ✅ ОПРОВЕРГНУТО (цепь ЦЕЛА): `competitors` и `my_instagram` доходят — их пишет `instagram/scrape` под верными типами (оба в ALWAYS_INCLUDE). `analyze-account` — мёртвый endpoint.
- **РЕШЕНИЯ по процессу (одобрено владельцем):** (1) инспектор контекста — в проде под админ-гейтом (доказательство + QA Этапа 2); (2) режим — мульти-агентный аудит, фиксы делаю вручную (не фаню правки по проду); (3) деплой — фиксы копятся на ОТДЕЛЬНОЙ ветке, `tsc`+`build` зелёные, дифф на ревью ПЕРЕД push (прод живой платный). Приоритет — качество, без заплаток.
- **✅ СДЕЛАНО (ветка `prelaunch-audit-fixes`, 6 коммитов, tsc+build зелёные):** все 8 launch-blocker'ов + высокие #10/#11/#12/#13/#14/#15/#19 + инфра-бакет + инспектор контекста (`/admin/context-inspector`) + биллинг-гейт после триала (код-гэп №4) + анти-эскалация Продамус. Детали и оставшийся бэклог (средние/лоу, не блокеры) — в `AUDIT_REGISTRY.md`.
- **✅ ЗАПУШЕНО В MAIN И ЗАДЕПЛОЕНО** (owner дал добро). Миграции 018/019/020 применены.
- **✅ ЭТАП 2 (ЖИВОЙ ПРОГОН) — ПРОЙДЕН на проде под аккаунтом Августы (проект `d159326c-…`):**
  - Инспектор контекста на РЕАЛЬНЫХ данных: 14/17 звеньев ALWAYS_INCLUDE доходят (my_instagram, competitors×4, tone_of_voice, meanings_map, interview_transcript, audience_research, cases_reviews, blog_lines, funnel, strategy, tactics). Цепь ядра работает вживую.
  - Живая генерация поста: контент глубоко построен на её материалах (боли аудитории, КОНКРЕТНЫЙ кейс «200 микроблогов х2» из cases_reviews, её голос/CTA-воронка) — moat подтверждён end-to-end, не generic AI. Метеринг+post-hook OK.
  - Находка Этапа 2 (ПОЛНОСТЬЮ закрыта): материалы резались до 3000 → (а) лимиты medium подняты ВЫШЕ реальных размеров (15k) → входят целиком; (б) длинные (интервью 129k/исследование 46k) НЕ эмбедились нигде → теперь `research-analyze` эмбедит полный текст в project_chunks (`lib/ai/embed.ts`) → ничего не теряется; (в) бэкфилл `POST /api/admin/context-backfill?projectId=` для старых проектов (products→product_description + до-эмбеддинг). Проверено инспектором на проде Августы: my_instagram 3000→11903, competitors→33746, эмбеддинг-чанки 23→86, product_description дошёл, reaching 14→19.
  - 🔴 **КРИТИЧЕСКАЯ НАХОДКА — методология сервиса НИКОГДА не доходила до генерации (закрыта, доказана).** Root cause: `knowledge_chunks` имел только SELECT-политику RLS (миграция 001), без INSERT → векторизация админ-загрузки молча отклонялась, а `upload/route.ts` не проверял ошибку → 0 чанков методологии с первого дня. Фундамент продукта не влиял на контент → отсюда ручные правки в прошлом. Фикс (все части в проде): (1) миграция **021** — write-политика admin для knowledge_chunks (ПРИМЕНЕНА); (2) upload проверяет ошибку вставки; (3) `POST /api/admin/knowledge-reembed` (admin) переэмбедил volt → **255 чанков**; (4) пороги match_knowledge_chunks 0.78→0.35 / match_project_chunks 0.72→0.4 (для text-embedding-3-small 0.78 был слишком высок → извлекалось ~0). **Проверено диагностикой инспектора `?q=`: methodology matched 14 чанков, top-sim 0.57; project embeddings matched 14.** Методология теперь реально в промпте каждой генерации.
  - Диагностика инспектора: `GET /api/admin/context-inspector?projectId=…&q=<запрос>` → сколько чанков методологии/проекта поднимается + top-similarity (постоянный QA извлечения).
  - ✅ Живой прогон (Августа, после всех фиксов): генерация стала заметно сильнее (голос резче/подлиннее, психология аудитории из карты смыслов, мышление «по системе» из методологии) — регрессов нет. Визуал-рендер картинки поста ПРОВЕРЕН ГЛАЗАМИ на проде (4:5, фактурный фон, розовый градиент-акцент «САРАФАН», кириллица-шрифты) → фикс #14 (шрифты в tracing) работает вживую, рендер не падает.
- **✅ БЭКЛОГ (10 средних/лоу закрыто, commit 16bf128):** #25 голос эталонов в JSON-поля; #22 guard пустого IG-профиля; #23 понятное сообщение на аудио-аплоад; #26 OOM-guard+видимость транскрипта рилз; #27 по-слотовая замена трендов; #39 валидатор stop_reason; #36 voice_rules order+limit; #41 ffmpeg null-guard; #35 текст ошибки; #21 удалён мёртвый analyze-account. Остаток лоу — косметика/визибилити + оптимизация кэша промпта #38 (отложена, рефактор ядра AI-вызова).
- **✅ ТРЕНДЫ — живой прогон (аккаунт Августы):** страница трендов грузится без ошибки миграции (колонки scope/project_id ✓ = LB#7), добавление своего project-тренда сохраняется («Добавлено в Мои тренды» ✓ = LB#8 RLS-запись), удаление работает. Тестовый тренд удалён.
- **✅ БЭКЛОГ-2 (commit d3e51fc):** scrape-product #30 (убран O(n²), дедуп, 8k) + #29 (нормализация product_type: синонимы→whitelist); autofill #17 (лог при отсутствии APIFY_TOKEN) + #28 (Apify-таймаут 80→60с под maxDuration 90).
- **ОСОЗНАННО ОТЛОЖЕНО (пост-запуск, не блокеры):** #38 сплит кэша промпта — оптимизация СТОИМОСТИ/латентности (не качества), рефактор ядра AI-вызова; риск > выгода при приоритете качества. #32/#33/#34/#37/#40 — «не баг»/косметика-визибилити. RESEARCH-конвейер (аудио→транскрипция→эмбеддинг) вживую требует реальной записи от владельца — механизм эмбеддинга доказан бэкфиллом (чанки 23→86), фикс транскрипции подтверждён 1 июля.
- **✅ ОНБОРДИНГ — живой прогон на НОВОМ проекте (создан+удалён на аккаунте Августы):** фоновый scrape-social сохранил `my_instagram` 4309 симв (Apify), продукт → `product_description` 194 симв — оба ДОХОДЯТ до генерации. LB#1/2/3 подтверждены end-to-end на свежих данных. Тестовый проект удалён (404).
- **ДАЛЬШЕ (по желанию):** прокликать остальные функции вживую (онбординг нового проекта, research-конвейер, визуал-рендер, тренды/рилзы); добить бэклог средних/лоу из `AUDIT_REGISTRY.md`. Продамус — живой колбэк при активации.

## ‼️ РЕШЕНИЕ ВЛАДЕЛЬЦА (7 июля 2026): ТРИАЛ НЕ ОГРАНИЧИВАТЬ до его явной команды
Августа сама несёт затраты и раздала доступ друзьям-тестерам — они должны работать СВОБОДНО. `BILLING_ENFORCED` включать ТОЛЬКО когда владелец (Mathew) прямо скажет; то же с письмами о конце триала (активируются ключом RESEND — ставить только при запуске оплаты). Механика гейтинга/переходов/писем ГОТОВА и спит — НЕ включать по своей инициативе, НЕ поднимать заново как «незакрытый гэп». Rate-limits (022) — исключение: это анти-абуз страховка кошелька (60/ч чат и т.п., в 5-10 раз выше нормальной работы), реальным тестерам не мешает; при единственной жалобе — поднять лимиты.
Продамус (живой РФ-колбэк) — зависит от Августы, в плане, без фокуса.

## ✅ БЕЗОПАСНОСТЬ МАТЕРИАЛОВ — приватный бакет + подписанные ссылки (7 июля, живьём проверено)
Найдено при пост-миграционном аудите: бакет `materials` в проде приватный, а код рассчитан на постоянные публичные ссылки — несостыковка (спящая, пока живых файлов не было). Владелец выбрал вариант «безопаснее» (не публичный бакет). Сделано и проверено ПОЛНЫМ живым циклом на проде (загрузка→подписанная ссылка 5 мин→скачивание содержимого совпало→старый публичный URL заблокирован 400→удаление→бакет и БД чистые, 0 сирот):
- migration 020 исправлена (`public: false`, была true — никогда не применялось к проду, но вводило в заблуждение на чистых окружениях).
- upload/route.ts хранит голый storage-путь вместо getPublicUrl().
- НОВОЕ `GET /api/materials/[id]/file` — подписанная ссылка на 5 мин, владение проверяется на уровне приложения, выдаёт через service-role клиент.
- **Вторая находка в процессе проверки:** DELETE /api/materials удалял файл из Storage через ПОЛЬЗОВАТЕЛЬСКУЮ сессию БЕЗ проверки ошибки — при сбое файл оставался вечным сиротой без единого следа в логах (живой тест это подтвердил: DB-строка удалялась, файл — нет). Починено: удаление через service-role клиент (владение уже проверено выше) + лог ошибки. Проверено — теперь чистит полностью.

## ✅ АУДИТ ВСЕХ ЗАГРУЗОК/СКАЧИВАНИЙ ФАЙЛОВ (7 июля, живьём по всем бакетам)
Владелец спросил «везде ли работает как надо» — проверил ВСЕ 3 бакета живым циклом (не только materials):
- **materials** (приватный) — см. блок выше, полностью починен и проверен.
- **project-brand** (публичный, намеренно — next/og рендерит карусели/сторис по прямому URL): живой тест загрузка→публичный URL отдаёт файл→удаление→объект реально пропадает из бакета (200 после удаления — это ожидаемый CDN-кэш на 1ч, не риск для публичного бакета). Код везде идёт через service-role клиент — тот самый класс бага (сессионный клиент + storage), что был в materials, здесь не встречается.
- **audio-temp** (приватный, подписанные URL для загрузки И скачивания): живой тест — подписанная ссылка на загрузку выдаётся только для СВОЕЙ папки (чужой user_id → 403 Access denied подтверждён), загрузка проходит, транскрипция обрабатывает ошибку контролируемо (500 с понятным текстом, не падает). **Нашлась и починена 3-я находка того же класса:** очистка временного аудио после ошибки транскрипции (не только успеха) — раньше при сбое ffmpeg/Whisper на последнем куске файл оставался сиротой в приватном бакете (не риск безопасности, просто мусор). Живой тест подтвердил: после фикса файл с ошибкой пропадает сам.
- Подчищены исторические сироты (5 старых аудио-огрызков + тестовые файлы) — бакеты у проекта Августы сейчас чистые.
- **Побочное наблюдение (НЕ починено, не блокер):** в project-brand у проекта Августы ~25 старых образцов бренда с 11 июня, часть — точные дубли (накопление от повторных «Распознать заново»). Безопасности не угрожает, просто место на диске. Не трогал без явного решения владельца — если попросит, почищу.
**Вывод: вся инфраструктура загрузки/скачивания файлов теперь проверена и работает корректно по всем 3 бакетам.**

## ✅ ROADMAP №8 — фоновая транскрипция (7 июля, живьём проверено на проде)
Механизм полностью работает: миграция 024_jobs применена, живой тест на реальном проекте — загрузка аудио → POST /api/jobs/transcribe → сервер обработал через `after()` без единого доп. запроса от клиента → статус дошёл до `done` с результатом → временный файл в audio-temp удалился сам. Полный цикл подтверждён. Заодно почищено: 15 неиспользуемых старых образцов бренда (из 25 — 10 реально используются в brand_kit, не тронуты).
Дальше по желанию: тот же паттерн (jobs + after() + self-continue) можно распространить на Instagram-скрейп/виральные рилзы, если появится реальная боль с долгими SSE-сессиями — сейчас не блокер.

## 🗺 ROADMAP К МАСШТАБУ (одобрен владельцем 1 июля 2026; топ-3 реализованы сразу)

### 🔴 До запуска — СДЕЛАНО в этой сессии (1 июля, вечер)
1. **✅ Смоук-тесты + CI** — vitest (`npm test`, tests/smoke/: подпись Продамуса roundtrip+tamper, инварианты цепи ALWAYS_INCLUDE/RAW_LIMIT/isUsableMaterial, цены тарифов, splitIntoChunks) + `.github/workflows/ci.yml` (tsc+test+build на каждый push). ⚠️ CI — «пожарная сигнализация», НЕ блокирует деплой (деплой=push): красный ран = коммит уже на проде, чинить вперёд/revert. Сделать блокирующим позже: protect main + required check.
2. **✅ Сторож цепи** — `GET /api/cron/chain-watch` (Vercel Cron ежедневно 7:00 UTC, vercel.json): по активным проектам (14д) ищет материалы в error/зависший processing >1ч/пустые звенья ALWAYS_INCLUDE + knowledge_chunks=0; чистит старые rate_limits. Алерт: console.error (Vercel logs) + POST на `ALERT_WEBHOOK_URL` (env, опционально — навесить TG-мост/Slack). Auth: CRON_SECRET / x-vercel-cron (как refresh-trends).
3. **✅ Rate-limiting** — миграция **022_rate_limits.sql** (⚠️ ПРИМЕНИТЬ; до применения — fail-open, ничего не ломается) + `lib/rateLimit.ts` (fixed-window per-user, атомарный RPC, fail-open, админ-байпас) на 10 дорогих роутах: chat 60/ч, generate 40/ч, plan-stories 20/ч, image 20/ч, transcribe 120/ч (per-chunk), instagram/scrape 10/ч, viral-reels 10/ч, video 10/ч, autofill 10/ч, scrape-product 20/ч. 429 + code:'rate_limited'.

### 🔴 До запуска — осталось (владелец/конфиг)
4. **✅ Sentry — ИНТЕГРИРОВАН (6 июля, Sentry-lite без SDK):** владелец завёл бесплатный аккаунт, DSN в коде (fallback; публичен by design) + env SENTRY_DSN для ротации. instrumentation.ts (onRequestError → серверные ошибки), instrumentation-client.ts (window.onerror, кап 5/страницу), chain-watch → captureMessage. Осознанно БЕЗ @sentry/nextjs (кастомный форк Next, wrap next.config = риск). Тестовое событие доставлено (HTTP 200). CRON_SECRET уже стоял в Vercel (не трогать). TG-алерты — опционально, отложены (Sentry покрывает). Тест-юзеры добавлены (6 июля): Unshikova_kris@mail.ru, juliadagis@gmail.com (пароли переданы владельцу в чате).
5. **Биллинг добить — КОД ГОТОВ (6-7 июля):** ✅ стейт-переход trialing→view_only по концу триала (в chain-watch cron; активен ТОЛЬКО при BILLING_ENFORCED — до запуска инертен, чтобы баннер не врал «на паузе»); ✅ письма Resend (`lib/email.ts`, спящие без ключа): «триал кончается через N дн» (≤7д, один раз) + «триал закончился» — идемпотентно через billing_events (паттерн вебхуков). Осталось у владельца: живой РФ-платёж Продамуса → env `RESEND_API_KEY` (+ домен в Resend) и опц. `EMAIL_FROM` → `BILLING_ENFORCED=true`. Письмо «неудачное списание» — при активации биллинга.

### 🟠 Первые недели после запуска
6. **Продуктовая аналитика** (PostHog): онбординг-воронка, время до первого контента, retention D1/D7, какие материалы коррелируют с удержанием. Сейчас аналитики НОЛЬ.
7. **✅ Цикл обучения ЗАМКНУТ (6 июля):** «Результаты» теперь включают и «Готовое» (saved_content) — метрики вносятся для обоих источников (бейдж «Готовое»); залетевшие тексты (post/storytelling/other, >80 симв, есть метрики) промоутятся в style_examples со score 50+reactions+saves*2+reach/100 (сохранения весят выше лайков); RAG сортирует «Готовое» по engagement (без метрик — по свежести, как раньше). Миграция **023_saved_content_results** (⚠️ ПРИМЕНИТЬ; до неё страница деградирует к плану, RAG работает как раньше — select * не падает). Идея на потом: автоподтяжка охватов по ссылке через Apify.
8. **Фоновая очередь** (Inngest/QStash) для транскрипции/скрейпа/карты смыслов вместо SSE-хартбитов — телефон можно блокировать, iOS-надёжность.
9. **Push/напоминания** из контент-плана (PWA-push или TG-бот) — драйвер привычки/retention.

### 🟡 Масштабирование (при выручке)
10. **Мультипользовательские проекты** для тарифа Продюсер (роли владелец/редактор/клиент-просмотр) — за это агентства платят.
11. **Маржа:** сплит кэша промпта (#38, отложен осознанно), gpt-image-1 как единица контента.
12. **Данные/право:** Supabase PITR-бэкапы, экспорт данных юзера, оферта/политика (152-ФЗ), чеки.
13. **Витрина:** лендинг с кейсами, партнёрка продюсеров (вместо снесённого MLM).

## 🔜 СТАТУС / ПЕРВЫМ ДЕЛОМ В НОВОЙ СЕССИИ (обновлено 1 июля 2026 — аудит надёжности онбординга)

### ⚙️ ТЕКУЩИЙ ПРОД — ФАКТ (НЕ перепроверять как «неизвестное», НЕ поднимать заново)
- **Stripe — LIVE.** Боевой ключ `sk_live_…` стоит в env Vercel (подтверждено владельцем 1 июля). Приём на юр-лицо Августы. ⚠️ Старая пометка ниже «Stripe → live потом» — УСТАРЕЛА.
- **APIFY_TOKEN — СТОИТ в Vercel и давно работает.** От него: анализ конкурентов, виральные рилзы, Instagram-автозаполнение. В локальном `.env.local` его НЕТ — это нормально, НЕ флагать как «отсутствует».
- **Storage-бакеты в проде существуют:** `materials`, `audio-temp`, `project-brand` (проверено живым вызовом). `materials` создан вручную, НЕ в миграциях → тех-долг.
- **OpenAI (Whisper + gpt-image-1) — ключ валиден, квота есть** (живой вызов 1 июля). Транскрипция падала НЕ из-за кредитов.
- Мёртвые env: `REPLICATE_API_TOKEN`, `SHOTSTACK_API_KEY` — в коде не используются (видео через локальный ffmpeg).

### 🐛 Аудит надёжности 1 июля — ПЕРВОПРИЧИНЫ и фиксы (в main)
- **Instagram-автозаполнение** («проверь, что аккаунт публичный»). Причина: онбординг-`autofill` НИКОГДА не использовал Apify (`git log -S apify` пусто) — стоял на хрупком прямом скрапе, который IG блокирует с серверных IP. НЕ регресс — изначально ненадёжный путь. Фикс: `autofill/route.ts` → primary через Apify (`apify~instagram-profile-scraper`), хрупкие методы — фолбэк; честная ошибка; maxDuration 90.
- **Транскрипция интервью на iPhone** («ни один файл»). Причина: time-split (69fbb80, на месте) зависит от чтения длительности В БРАУЗЕРЕ; iOS Safari часто не читает → файл слался ОДНИМ куском → >25 МБ Whisper → провал. Фикс: нарезка больше НЕ зависит от браузера (при неизвестной длительности клиент режет по 10-мин окнам, сервер сигналит `ended` на пустом сегменте за концом файла); реальная причина теперь видна пользователю. (`transcribe/route.ts` + `research/page.tsx`)
- **`sharp`** (brand-kit/analyze) не был прямой зависимостью и не externalized → риск падения на Vercel. Фикс: в `package.json` + `serverExternalPackages`.
- **`scrape-product`** без guard ключа + онбординг ТИХО глотал сбои `scrape-social` (`.catch(()=>{})`). Фикс: guard + мягкое уведомление вместо тишины.
- **Follow-up (НЕ сделано):** таймауты на Apify-вызовы; `scrape-social` Instagram тоже на Apify; идемпотентная миграция бакета `materials`; убрать мёртвые env.

### ‼️ ПРОЦЕСС (прямая просьба владельца, 1 июля): при КАЖДОМ важном решении/фиксе/изменении конфигурации — СРАЗУ обновлять этот файл. НЕ возвращаться к уже решённому. Перед тем как назвать проблему — проверить, не решена ли она (git log / этот блок).

## 🎯 СЛЕДУЮЩАЯ СЕССИЯ — ПОЛНЫЙ ПРЕДЗАПУСКОВЫЙ АУДИТ (первая задача)
Причина: инцидент 1 июля (транскрипция/Instagram работали «косметически», но ломались на реальных данных). Владелец подозревает такие же дыры в других функциях. Продукт платный, скоро запуск — всё должно РЕАЛЬНО делать то, что заявлено.

**МЕТОД (для каждой функции, без косметики):**
1. **Спец** — что функция ДОЛЖНА делать (по дизайну/этому файлу).
2. **Реальность** — что делает на самом деле: прочитать код + проследить причинно-следственную цепочку до внешних сервисов (OpenAI/Anthropic/Apify/Supabase/ffmpeg).
3. **Крайние случаи** — большие файлы (резать+объединять!), iOS/Safari, пустой/битый ввод, блокировки скрейпа, таймауты Vercel (maxDuration/память), отсутствие данных, отсутствие env.
4. **Вердикт:** соответствует / косметика / не работает — и ПОЧЕМУ (доказать, а не гадать; отличать регресс от «никогда не работало» через git).
5. **Чинить сразу и ПОЛНО** (не заплатку). Учитывать все нюансы ДО, а не после.

**Что аудитить (критично к запуску):**
- **Контент-движок + МНОГОУРОВНЕВАЯ КОНТЕКСТНАЯ СИСТЕМА — ЯДРО/MOAT ПРОДУКТА.**
  СУТЬ (слова владельца, 1 июля): каждая функция сервиса — ЗВЕНО ОДНОЙ ЦЕПИ, которое ДОПОЛНЯЕТ контекст, чтобы контент был качественнее/живее/человечнее — заведомо лучше, чем если человек просто скинет материалы в обычный AI. Всё (конкуренты, соцсети, созвоны/интервью, исследования) анализируется и ВЛИЯЕТ на генерацию. Это и есть причина, зачем нужна каждая функция.
  РЕАЛИЗАЦИЯ (подтверждено в коде: `lib/ai/rag.ts` + `app/api/ai/chat/route.ts`) — генерация собирает слои:
    1) **Методология/экспертка** — `systemKnowledge` (knowledge_vault/knowledge_chunks);
    2) **Материалы проекта** — RAG-эмбеддинги (project_chunks/project_materials);
    3) **ВСЕГДА-включаемые звенья** — `ALWAYS_INCLUDE` (сырьём, не по эмбеддингу): my_instagram, competitors, tone_of_voice, meanings_map, unpacking_map, blog_lines, audience_research, interview_transcript, audience_survey, cases_reviews, funnel_description, marketing_strategy, marketing_tactics, product_description, content_reference, chatbot_description, additional;
    4) **Голос** — style_examples → «Готовое» → системные примеры;
    5) **Сохранённое «Готовое»** — `buildSavedBlock`.
  ⚡ КЛЮЧЕВОЙ ВЫВОД ДЛЯ АУДИТА: надёжность КАЖДОЙ функции = качество контента. Скрейп IG/конкурентов упал → нет звеньев my_instagram/competitors → хуже контент. Транскрипция созвонов упала → нет interview_transcript/audience_research → хуже контент. Значит баги 1 июля были НЕ косметикой, а РАЗРЫВАМИ цепи качества.
  АУДИТ по КАЖДОМУ звену: (а) функция РЕАЛЬНО производит свой материал (анализ конкурентов → сохраняет material_type=competitors; интервью → interview_transcript/audience_research; IG → my_instagram; и т.д.); (б) материал ДОХОДИТ до генерации и влияет на результат. Проверить сквозняком end-to-end на реальных данных.

**ДВА ЭТАПА АУДИТА (план владельца, ОДОБРЕНО):**
- **Этап 1 — КОД:** полный причинно-следственный аудит всех функций по методу выше. Анализ + сразу фиксы.
- **Этап 2 — ЖИВОЙ ПРОГОН:** владелец логинится (аккаунт Августы-продюсера с реальными материалами) → ассистент через браузер (`claude-in-chrome` MCP) проклацывает КАЖДУЮ функцию на проде с реальными данными и проверяет, что контент реально впитывает конкурентов/исследования/голос. Финальная проверка «работает как задумано».
- **Онбординг:** создание проекта, автозаполнение (IG/TG/YT — теперь Apify), scrape-social (Instagram тоже перевести на Apify), scrape-product, материалы/upload (бакет `materials` есть, но не в миграциях).
- **Исследование:** аудио→транскрипция(ПОЧИНЕНО)→таблица→карта смыслов — проверить ВЕСЬ конвейер.
- **Визуал-движок:** карусели/посты/сторис/схемы/дизайнер — рендер, бренд-кит, шрифты, AI-картинки.
- **Тренды + виральные рилзы** (Apify — есть токен).
- **Биллинг:** Stripe live e2e (реальный платёж→вебхук→tier); **Продамус — живой РФ-колбэк НЕ проверялся!**; **КОД-ГЭП: гейтинг НЕ блокирует после конца триала** (см. архив-блок ниже, п.4); `BILLING_ENFORCED`.
- **Инфраструктура:** все env в Vercel; бакеты; миграции; sharp/ffmpeg трассировка; таймауты на Apify-вызовах.

**МЕХАНИЗМ ПРОТИВ РЕГРЕССОВ (внедрить в ходе аудита):**
1. Этот файл = живой источник правды (правило выше).
2. По каждой функции — записать СПЕЦ + известные нюансы + статус проверки (реестр «работает как заявлено»).
3. Смоук-тесты критичных путей (транскрипция/скрейп/генерация/рендер/вебхук) — прогон перед деплоем.
4. Никаких `.catch(()=>{})` — все ошибки видимы.
5. Перед добавлением функции — сразу перечислить крайние случаи и покрыть их.

---

## 🔜 (архив) СТАТУС на 18 июня 2026 — биллинг живой + ПОЛНЫЙ ДИЗАЙНЕР: элементы/AI-картинки/градиент/2-фото + перенос в КАРУСЕЛИ

### Сделано за сессию 14–18 июня (всё в main, задеплоено; детали — в датированных блоках ниже + git log)
- **💳 БИЛЛИНГ Фаза 1+2 — РАБОТАЕТ.** Тарифы trial/solo/pro/producer (PRICING), миграции **016+017 ПРИМЕНЕНЫ**. Метеринг в реальных точках (chat genFormat + plan-stories + video). **Stripe проверен END-TO-END** (тест: оплата → вебхук → tier=solo/active, billing_events пишутся, продление). **Продамус**: интеграция + ссылка-подписка работают (демо-страница «Про 14 900₽, автосписание» открылась), но **вебхук-подпись НЕ проверена живым колбэком** (тестер не в РФ, оплата картой не проходит без РФ-IP; владелец не в РФ). Переключатель на /pricing **«🇷🇺 Карта РФ / 🌍 Зарубежная»** (Продамус ₽ / Stripe $). `BILLING_ENFORCED` (env) **ВЫКЛЮЧЕН** — лимиты не действуют, никто не блокируется.
- **🐛 Визуал-фиксы (по тестерам):** транскрипция больших аудио — резка **по времени через ffmpeg** (был баг byte-range на m4a; проверено на 65-мин файле); AI-редактор плана — таймаут+человеческие ошибки+сохранение текста (слабый LTE); сторис — убор CTA-кнопки голосом; точечная подложка `[[...]]` в сторис.
- **🎨 Движок визуала расширен:** **сторис-схемы** (kind `scheme` — этапы зигзагом + рисованные стрелки, тёмный фон); **редактор сторис «как в Инстаграме»** (kind `free`): фон **фото / бумага / тёмный / светлый**, текст-блоки — **перетаскивание + щипок (масштаб+поворот) + ±10°**, размер/цвет(бренд)/плашка/выравнивание/дубликат, экспорт через движок, превью на Montserrat. Всё на `/stories` и `/visual`.
- **🧩 БИБЛИОТЕКА ЭЛЕМЕНТОВ дизайнера (b) — СДЕЛАНО (18 июня):** в редакторе `free` кнопки **Текст / Стрелка / Дуга / Номер / Иконка / Картинка**. `FreeBlock` расширен (`type: text|image|shape` + `src`/`shape`/`aspect`); движок `Free` рендерит SVG-стрелки (прямая + дуга со стрелкой, направляются поворотом), номерные кружки-ярлычки (`Badge`, авто-нумерация), эмодзи-иконки (⚠️✅🔥… = текст-блок), картинки/стикеры (загрузка с сохранением **PNG-прозрачности** — `downscaleImage` получил выбор формата). Новый изоморфный модуль **`lib/carousel/shapes.tsx`** — ОДИН код фигур и для Satori-движка, и для DOM-превью → WYSIWYG. Жесты drag/scale/rotate работают на ЛЮБОЙ блок (щипок масштабирует `size` у текста, `widthPct` у фигур/картинок). **Проверено ГЛАЗАМИ** (dev `?format=free` + `&photo=`): стрелки/номера/⚠️/плашки поверх фото и на дизайн-фоне — ок; схема не сломалась.
- **🖼 AI-КАРТИНКИ дизайнера (a) — СДЕЛАНО (18 июня):** роут `/api/ai/generate-image` (OpenAI **gpt-image-1**) рисует стикер (флэт-иллюстрация на прозрачном фоне) или фон сторис по текстовому/голосовому описанию → грузит в Storage → отдаёт URL; кнопка **«AI-картинка»** в редакторе. Доступ к gpt-image-1 у аккаунта проверен реальной генерацией (прозрачность ✓). ⚠️ Реальный $ OpenAI + новый тип выхода → кандидат в «единицу контента» при включении биллинга (сейчас enforcement OFF, не метрится).
- **🎠 ДИЗАЙН-КАРУСЕЛИ — СДЕЛАНО (18 июня):** редактирующая поверхность вынесена в общий **`components/carousel/FreeCanvas.tsx`** (value/onChange-контролируемый 1 слайд: фон+элементы+жесты+панель). `StoryEditor` теперь тонкая обёртка над ним (1 слайд 9:16, поведение сторис не изменилось). Новый **`CarouselDesigner`** (на `/visual`, секция «Дизайн-карусель с нуля») — N слайдов **4:5**, лента слайдов (добавить/копия/удалить/переключить), на каждом весь дизайнер (элементы/AI/градиент/2-фото), «Собрать карусель» → рендер всех + ZIP. Движок `Free` рендерит и при 4:5 (проверено глазами `?format=free&cf=1`). Так весь дизайнер доступен в каруселях = её главный референс (Пост 2).

### ⚠️ ОПЛАТА — что осталось до боевого запуска (статус на 18 июня)
Владелец: «всё работает, live-ключи Stripe и проверку Продамуса сделаю перед запуском, время есть».
1. **Stripe → live** (владелец, конфиг): `sk_live_…` + боевой вебхук (его `whsec_…`) в env Vercel → редеплой. Продукты/цены создадутся сами (ensurePrice). 
2. **Продамус → выкл. демо** в ЛК + **проверить вебхук на 1-м реальном РФ-платеже** (подпись хэндрол — на mismatch вебхук логирует ключи, поправить за минуты). ENV уже стоят: `PRODAMUS_SECRET_KEY`, `PRODAMUS_LINK_SOLO/PRO/PRODUCER`.
3. `BILLING_ENFORCED=true` в env → включит гейтинг.
4. **🔴 КОД-ГЭП перед запуском (не конфиг!):** гейтинг сейчас считает только МЕСЯЧНЫЙ лимит (300 на Соло), но **НЕ блокирует по окончании 2-мес триала**. Нужно: `gateContentUnit`/новый чек — если триал кончился (trial_ends_at прошёл) и не active/paying → блок/«только просмотр» + переход trialing→view_only→paused (при входе/крон). БЕЗ этого после триала юзер генерит бесплатно. Сделать перед `BILLING_ENFORCED=true`.
5. Фаза 3 (не блокеры): email конца триала; рефералка «месяц в подарок».
- **Тест-юзеры:** `yuliya_joker1@mail.ru` (после тест-оплаты на **solo/active** в БД — можно вернуть на trial), `aya272satori@gmail.com` (trial). Пароли в истории чата.

### 🎨 ПОЛНОЦЕННЫЙ ДИЗАЙНЕР — анализ референсов + план (СЛЕДУЮЩАЯ БОЛЬШАЯ ЗАДАЧА)
Владелец хочет «иллюстрации, схемы, стрелочки, работа с фото» (стиль Canva). Разобрал её 3 IG-референса (через её браузер, claude-in-chrome):
- **Пост 1 (avavasilik, её):** сторителлинг НА ФОТО — **2 фото на слайд** (верх/низ) + белый текст, формула «Да, я [результат] / Но [трудность]». → нужна раскладка «2 фото на слайд».
- **Пост 2 (avavasilik, её) — ГЛАВНЫЙ стиль:** дизайн-карусель — **фактурный фон** (мятая бумага, деньги), **флэт-иллюстрации/стикеры** (люди за столом, эмодзи-аватары, ⚠️-иконки, человечки), **номерные ярлычки**, маркир. списки, заголовки с **градиентными акцентными словами** (розовый→оранжевый).
- **Reel (afferent_ai, чужой):** туториал «GPT генерит картинку → плагин Canva делает её редактируемой по слоям». Это РУЧНОЙ приём, НЕ интеграция.
- **РЕШЕНИЕ:** Canva НЕ интегрировать (чужой редактор, огранич. API, дублирует наше). Строим СВОЁ на базе уже готового редактора `free`. План по приоритету:
  - **(b) Библиотека элементов — ✅ СДЕЛАНО (18 июня):** стрелки (прямая+дуга), эмодзи-иконки (⚠️ и пр.), номерные ярлычки, картинки/стикеры — как перетаскиваемые `shape`/`image`/text-блоки в редакторе `free`. `FreeBlock` расширен (`type` + `src`/`shape`/`aspect`); движок `Free` рендерит все три вида; общий модуль `lib/carousel/shapes.tsx` (Satori+DOM, WYSIWYG); кнопки в `StoryEditor`; жесты drag/scale/rotate на любой блок. Проверено глазами.
  - **(a) AI-генерация картинок/фонов — ✅ СДЕЛАНО (18 июня):** роут `POST /api/ai/generate-image` (OpenAI **gpt-image-1**, nodejs, auth+ownership): по описанию рисует **стикер** (флэт-иллюстрация на ПРОЗРАЧНОМ фоне, png) или **фон сторис** (1024×1536), декодит b64 → грузит в `project-brand/{id}/ai/` → public URL. В `StoryEditor` кнопка **«AI-картинка»** + панель (голосовой ввод `VoiceTextarea`, тоггл стикер/фон): стикер → `image`-блок, фон → подложка. **Проверено ГЛАЗАМИ:** одна реальная генерация (свинка-копилка, RGBA-прозрачность ✓ → доступ к gpt-image-1 у аккаунта ЕСТЬ) + рендер `image`-блока поверх фото через движок (dev `?format=free&sticker=`). ⚠️ Картинка = реальный $ OpenAI и НОВЫЙ тип выхода — кандидат в «единицу контента» при включении биллинга (PRICING §13); сейчас enforcement OFF → не метрится. Промпт просит «без текста на картинке» (текст кладётся сверху нашим движком).
  - **Градиентные акцентные слова — ✅ СДЕЛАНО (18 июня):** `RichText` получил опцию `accentGrad` → `**слово**` заливается градиентом `gradFrom→gradMid→gradTo` (тёплый оранжево-розовый, её референс) через `background-clip:text` + `color:transparent`. **Satori это ПОДДЕРЖИВАЕТ** (проверено глазами: cover «КЛИЕНТОВ», post «ЗАПУСКАХ», scheme «ЦА/смыслы/воронки/продукт» — градиент на свету и на тёмном). Включено на заголовках Cover/Content/Post/Scheme; body/плашки/CTA/Badge — солид-акцент. ⚠️ Градиент сейчас = тёплый дефолт темы (`gradFrom/Mid/To`), не выводится из бренд-акцента — для тёплого бренда avavasilik идеально; для холодного бренда позже хранить градиент в brand_kit.
  - **2 фото на слайд — ✅ СДЕЛАНО (18 июня):** новый фон редактора **«2 фото»** (`bgMode='split'`): загружаешь верх/низ → две половины 9:16, текст/стрелки/элементы кладутся поверх обеих (переиспользует всю свободную раскладку). `SlideSpec.split={top,bottom}`, движок `Free` рендерит две half-height картинки. Проверено глазами (dev `?format=free&top=&bottom=`). Формула её Поста 1 (Да, я … / Но …) набирается текст-блоками.
  - **Фактурные фоны** — ✅ ЧАСТИЧНО СДЕЛАНО (в редакторе фон бумага/тёмный/светлый; экспорт через Backdrop — глазами ещё НЕ проверено, реюз проверенного Backdrop).
  - **ИТОГ дизайнера:** все 4 пункта плана (b → a → градиент → 2 фото) + **перенос в КАРУСЕЛИ** (FreeCanvas → StoryEditor тонкий + CarouselDesigner N×4:5) СДЕЛАНЫ и в main. Дальше — полировка по фидбэку владельца (UX перетаскивания на телефоне) и/или возврат к КОД-ГЭПУ биллинга №4 перед запуском оплаты. Бэклог-идеи: слои/z-order и снап-направляющие в редакторе; реордер слайдов карусели (сейчас add/dup/del, без drag-перестановки); пресет «Да, я…/Но…» для 2-фото; бренд-градиент в brand_kit (сейчас тёплый дефолт); сохранение собранного в галерею.

### ⚠️ ПЕРВЫМ ДЕЛОМ в новой сессии
1. Прочитать PROJECT_CONTEXT + PRICING + AGENTS, затем `git log --oneline -30`.
2. **ПОЛНОЦЕННЫЙ ДИЗАЙНЕР — ВСЁ ✅ СДЕЛАНО** (b элементы / a AI-картинки / градиентные акценты / 2-фото + **перенос в дизайн-КАРУСЕЛИ** через общий `FreeCanvas`; см. блок ниже). **Ждём фидбэк владельца** (тестит редактор/карусели на телефоне; первую реальную AI-картинку пусть прогонит на своём проекте). Дальше — полировка по её замечаниям ИЛИ возврат к КОД-ГЭПУ биллинга №4 (блок «ОПЛАТА») перед запуском оплаты.
3. Когда владелец скажет про запуск оплаты — **доделать код-гэп №4** (блокировка по окончании триала) перед `BILLING_ENFORCED=true`.
4. Правила разработки не нарушать: `npx tsc --noEmit` + `next build` (с копией lightningcss) до коммита; деплой = push в main; рендеры проверять ГЛАЗАМИ через dev demo-GET (`/api/carousel/render?demo=1&format=free|scheme|story&...`); миграции/env применяет владелец; коммиты с Co-Authored-By: Claude Opus 4.8 (1M context).

---

## 🔜 ПРЕДЫДУЩИЙ СТАТУС (обновлено 14 июня 2026 — БИЛЛИНГ Фаза 1: backbone)

### Контекст входа в сессию
Владелец сказала: видео-оверлей в проде ЕЩЁ НЕ пробовала (попробует короткий ролик позже — баги чинить по видео/скринам, паттерн ffmpeg→Whisper+кадры). Видео изолировано, биллинг не блокирует → стартовали **БИЛЛИНГ**.

### ⚠️ Главная находка перед стартом
Биллинг-фундамент был ЗАЛОЖЕН рано (migration 003: тарифы free/starter/pro/agency, RPC consume_generation, 2-ур. MLM-рефералка, промокоды 004), НО: (а) тарифы/цены/лимиты НЕ совпадали с утверждёнными (PRICING); (б) **счётчик списывался ТОЛЬКО в `/api/ai/generate`, у которого 0 вызовов** (страница /generator удалена; реальная генерация идёт через `/api/ai/chat` genFormat) → лимит не гейтил НИЧЕГО. Платёжных провайдеров/webhook/email не было.

### Сделано за сессию 14 июня (Фаза 1 — ЗАДЕПЛОЕНО в main, commit 2c58eba; миграция 016 ПРИМЕНЕНА владельцем; tsc+build чисто; енфорсмент OFF)
- **Тарифы переписаны под утверждённые** (`lib/generations-config.ts` — единый источник правды, client-safe): `trial / solo / pro / producer`; цены ₽ и $ (Соло 4900/$49, Про 14900/$149, Продюсер 29900/$299); лимиты: trial/solo 300 единиц, pro/producer «безлимит (fair use)» с анти-абуз-потолком 2000/8000; projects 3/1/3/10; фичи из PRICING; `PAID_PLANS`, `TRIAL_DAYS=60`, `VIEW_ONLY_GRACE_DAYS=7`. `SubscriptionTier`/`SubscriptionStatus` типы; `SubscriptionPlan` оставлен алиасом. REFERRAL_REWARDS помечен legacy (MLM, под снос в Фазе 3).
- **Миграция `supabase/migrations/016_billing.sql` (⏳ ВЛАДЕЛЕЦ ПРИМЕНЯЕТ):** tier CHECK → trial/solo/pro/producer (+default trial); все строки нормализованы в 'trial' (реальных платящих нет, старые pro/agency были другими тарифами — НЕ дарим); +колонки `subscription_status` (trialing/active/past_due/view_only/paused/canceled), `trial_ends_at`, `current_period_end`, `payment_provider`+`provider_customer_id`+`provider_subscription_id`; всем выдан свежий 2-мес триал (trial_ends_at = now()+60д); `generation_limit()` обновлён под новые тарифы; consume_generation/add_bonus_generations из 003 переиспользуются. Идемпотентна, БЕЗ локаутов.
- **Метеринг ПОЧИНЕН — считаем в реальных точках** (`lib/generations.ts` `gateContentUnit`): 1 единица = `/api/ai/chat` при genFormat (готовый пост/рилз/карусель/сторителлинг) + `/api/ai/plan-stories` (серия сторис) + `/api/video/overlay` (видео). Рефанд при пустом/упавшем результате во всех трёх. **БЕСПЛАТНО** (fair-use/экспорт): правки в чате (chat без genFormat), рендер слайдов/картинки готового текста, бренд-анализ. Pro/Producer не блокируются (fair-use).
- **🚦 ЕНФОРСМЕНТ ВЫКЛЮЧЕН по умолчанию** — `BILLING_ENFORCED` (env, default off). Пока off: usage СЧИТАЕТСЯ (реальные данные копятся), но НИКТО не блокируется (402 не отдаётся) → деплой backbone НЕ запрёт ~50 триальщиков. Флипнуть в `'true'` только когда тарифы+оплата+экран апгрейда готовы (launch).
- UI-консьюмеры старых тарифов починены под новые (PricingClient — 3 карты, Соло-герой, ₽; pricing/settings/referral/admin-users/admin-analytics + admin API). Админ-кнопка «триал» теперь пишет trial_ends_at+status (и не пишет невалидный 'free').
- **Фаза-1 UI (тоже в рабочем дереве, tsc+build чисто):** `components/billing/UpgradeDialog.tsx` — экран апгрейда (Соло-герой, 3 карты ₽), event-driven: `showUpgrade(reason)` диспатчит событие, единый `<UpgradeDialogHost/>` в dashboard-layout его ловит. `components/billing/TrialBanner.tsx` — тонкий баннер под хедером, читает subscription_status+trial_ends_at, показывает «триал кончается через N дней» (≤7д, dismissible на сессию) / «закончился — генерация на паузе» (view_only) / «на паузе» (paused); до миграции (нет колонок) → null (безопасно). 402 `code:'limit_reached'` обрабатывается на клиенте → `showUpgrade('limit')` в: `/create` (genFormat не шлёт — но хэндлер есть на будущее), `/assistant` (главный — шлёт genFormat), сторис-билдер (`plan-stories`), `/visual` видео (`video/overlay`). Карусель-структура/рендер и post-hook НЕ метятся (экспорт). Пока BILLING_ENFORCED off — 402 в проде не возникает (хэндлеры готовы к launch).

### 🎨 Сторис: точечная подложка `[[...]]` (14 июня, фидбэк владельца — ЗАДЕПЛОЕНО, commit 7e81d16)
Фидбэк (видео+скрин): «я сказала выдели подложкой только первое предложение, а он выделил весь текст». Раньше подложка была на весь кадр (per-frame `plate: boolean`). Теперь — **точечно**:
- Маркер `[[ ... ]]` в headline/body → подложка ложится ТОЛЬКО под этот фрагмент (предложение/слово), остальной текст — чистым на фото. Кадр с любым `[[]]` → «selective mode» (немаркированный текст рендерится plain). Без маркеров → как было (подложка по фото-анализу на весь кадр). `**акценты**` работают внутри плашки.
- Движок: `StoryText` переписан на сегменты (plated/plain, widow-fix на каждый сегмент); `tokenize` срезает `[[]]` (чтобы маркеры не светились в RichText/каруселях). `edit-stories` понимает «выдели подложкой только X» → оборачивает X в `[[]]`. Demo-GET рендера получил dev-параметры `&hl=/&bd=` для глазной проверки.
- **Проверено ГЛАЗАМИ** (dev og, реальный текст владельца на контрастном фоне): маркированные фразы — в плашках, остальное plain, акцент розовым внутри плашки. ✓

### 🎬 Видео-сторис — стало находимым (14 июня, фидбэк: «в Оформлении сторис нет загрузки видео, только фото»)
Фича «Видео с текстом» жила ТОЛЬКО на `/visual` (секция 4); владелец искала загрузку видео в «Оформление сторис» (`/stories`, там было только фото). Вынес флоу в компонент `components/carousel/VideoStory.tsx` (upload→/api/video/upload-url→uploadToSignedUrl→/api/video/overlay→скачать) и подключил на ОБЕИХ страницах: `/visual` (вместо инлайна) и `/stories` (карточка под фото-флоу, разделитель «или видео-сторис»). ⚠️ Само видео-наложение на Vercel владелец ещё не прогоняла вживую — это первый реальный прод-тест (если упрётся в лимиты ffmpeg/Vercel — план Б Shotstack).

### 🐛 Батч багфиксов + сторис-схемы (14 июня, фидбэк тестеров) — ЗАДЕПЛОЕНО
- **Транскрипция больших аудио ПОЧИНЕНА** (commit 69fbb80): был баг — большие файлы резались ПО БАЙТАМ, а байт-срез `.m4a`/mp4/ogg = битый файл → Whisper падал на кусках 2+ (файл Арины 65 мин/65МБ не грузился). Теперь `/api/ai/transcribe` режет ПО ВРЕМЕНИ через ffmpeg (mono 16k mp3, всегда валидно); клиент (`research/page.tsx`) считает длительность на устройстве (`getAudioDuration`), окна по 10 мин. **Проверено локально на реальном файле Арины** — кусок из середины расшифровался. next.config: ffmpeg-static трейсится и в transcribe-роут.
- **AI-редактор «Load failed» укреплён** (тот же commit): `AiEditChat` → таймаут (AbortController 150с) + человеческие ошибки («Сеть оборвалась…») + восстановление введённого текста при сбое (не теряется). Класс «слабый LTE», как у диктовки.
- **Сторис: убор призыва/CTA** — `edit-stories` теперь чистит cta (розовую кнопку-фразу) по просьбе «убери призыв/розовую кнопку».
- **«AI долго грузит»** — это латентность Opus (10-30с на сложных), не баг; радикально не ускорить, но теперь не падает в ошибку.
- **🆕 СТОРИС-СХЕМЫ** (commit bf41784): новый шаблон движка `scheme` — вступление + этапы, спускающиеся зигзагом (лево/право), соединённые рисованными кривыми (SVG), на тёмном фоне, акценты `**слово**` (по референсу владелицы — чёрный скрин «ЦА→смыслы→воронки→продукт»). UI `SchemeStory` (вступление + этапы по строкам) на `/visual` и `/stories`. RichText получил align:'right'. **Проверено ГЛАЗАМИ** (dev demo `?format=scheme`) — совпадает с референсом.
- **🆕 РЕДАКТОР СТОРИС «как в Инстаграме» — ПОЛНОЦЕННЫЙ** (commits 6c780aa→b901277; владелец: «доделай полностью, проверю целиком»): движок `free` (текст-блоки по xPct/yPct поверх фото, + **поворот** `rotation` через CSS transform — проверено ГЛАЗАМИ) + `StoryEditor` на `/stories` и `/visual`. Умеет: загрузка фото; добавить текст; **перетаскивание пальцем** (1 палец) + **щипок двумя пальцами = масштаб + поворот** (pointer-capture на canvas, мультитач); на блок — размер ±, поворот ±10°/сброс, цвет (белый/бренд-текст/бренд-акцент), плашка вкл/выкл, слева/по центру, **дубликат**, удалить; экспорт 1080×1920 через рендер-движок. **Фирменные цвета из бренда проекта** (accent/плашка bg+text — fetch `/api/brand-kit`, brand передаётся и в экспорт). Превью грузит **Montserrat** (@font-face из /fonts) → на экране ≈ как в экспорте. Плашки в превью построчно (box-decoration-break). Drag/pinch-UX вживую тестит владелец на телефоне.
- **Бэклог визуала (на потом):** AI-генерация этапов схемы из темы; brand-accent в схему (сейчас дефолтный розовый); в редакторе — слои/z-order, направляющие-снап, кастом-шрифты.

### 🔌 ПРОДАМУС — рекуррент ЕСТЬ (ответ на вопрос владельца)
Владелец дала: ссылка `https://sistema-avavasilik.payform.ru/`, секрет `st1265po89`. Просила узнать про рекуррентные платежи.
- **Продамус ПОДДЕРЖИВАЕТ подписки/рекуррент** (раздел «Рекуррентные платежи / подписки и клубы» в ЛК): продукт-подписка с периодом, ценой, **демо-периодом (=триал)**, скидкой на первый платёж, числом автосписаний — настраивается В ЛК (не в ссылке). REST API: `setActivity` (актив/деактив подписки), HMAC-подпись запросов/коллбэка секретным ключом из ЛК. Важно: деактивацию «от пользователя» нельзя откатить — возобновление = новая подписка.
- **Владелец ДОЛЖНА:** зайти в ЛК Продамуса → проверить, доступен ли раздел «Подписки/Рекуррентные платежи»; если нет — запросить включение у поддержки Продамуса. Затем (Фаза 2) создадим продукты-подписки на 3 тарифа (период=месяц, демо=2 мес).
- Секрет `st1265po89` = «секретное слово» Продамуса → пойдёт в env `PRODAMUS_SECRET_KEY` (НЕ в код/репозиторий). Stripe — позже («подключу позже»).

### 💳 Фаза 2 — STRIPE подготовлен (14 июня, ЗАДЕПЛОЕН commit fe0dfd8, СПЯЩИЙ без ключей)
Владелец: «подготовь всё по страйпу, пока с продамусом узнаю». ⚠️ Она прислала в чат логин/пароль/2FA-резерв-код от Stripe — Я ИМИ НЕ ПОЛЬЗОВАЛАСЬ (вход в аккаунт для интеграции НЕ нужен), посоветовала сменить пароль + перегенерить 2FA-коды. **Stripe подключается ТОЛЬКО через API-ключи в env, НЕ через логин.**
- Хостед **Stripe Checkout** (редирект на страницу Stripe — клиенту publishable-ключ НЕ нужен). `lib/billing/stripe.ts`: ленивый клиент (нет `STRIPE_SECRET_KEY` → `billing_not_configured`); `ensurePrice(plan)` сам создаёт Product+Price по `lookup_key` (USD, ежемесячно: solo $49 / pro $149 / producer $299) — ручная настройка в дашборде НЕ нужна; маппинг статусов + period-end (устойчив к смене версии API — читает и из items, и из корня).
- Роуты (nodejs): `/api/billing/checkout` (Checkout Session по тарифу), `/api/billing/stripe/webhook` (проверка подписи + **идемпотентность** через `billing_events` → пишет subscription_tier/status/current_period_end/provider в profile), `/api/billing/portal` (управление/отмена подписки).
- Миграция **`017_billing_events.sql`** (⏳ ВЛАДЕЛЕЦ ПРИМЕНЯЕТ перед запуском Stripe) — лог обработанных событий (RLS, только service-role).
- UI: `PricingClient` «Подключить» → `/api/billing/checkout` → редирект; без ключей — мягкий тост «оплата скоро подключится» (в проде ничего не ломается).
- **ENV для запуска (владелец, тестовый режим Stripe сначала):** `STRIPE_SECRET_KEY` (Developers → API keys → Secret key), `STRIPE_WEBHOOK_SECRET` (после добавления вебхука `https://amaproduct.com/api/billing/stripe/webhook` в Stripe → Developers → Webhooks; события: checkout.session.completed, customer.subscription.created/updated/deleted, invoice.payment_failed). Цены/продукты создаются сами при первом checkout. Боевой режим — после теста.
- **Продамус (РФ) ТОЖЕ ГОТОВ** (commit 778f5d8, спящий): владелец сказала «по продамусу есть всё». `lib/billing/prodamus.ts` — HMAC-SHA256 sign/verify по спеку Продамуса (привести к строкам → рекурсивный ksort → JSON unescaped-unicode/**escaped-slashes** → hmac; различие list/object в JSON воспроизведено; подпись в заголовке `Sign`), парсер php-form-боди, encode/parse order_id (`userId.plan.ts`). Роуты `/api/billing/prodamus/checkout` (ссылка на payform с `subscription`=id тарифа из env) и `/api/billing/prodamus/webhook` (проверка подписи + идемпотентность order_id+date + апдейт profile; ребиллы матчатся по sub id). Кнопка «Подключить» → Продамус первым, Stripe фолбэком. **ENV:** `PRODAMUS_SECRET_KEY`, `PRODAMUS_SUB_SOLO/_PRO/_PRODUCER` (id подписок из ЛК — создать 3 подписки: месяц, демо 60 дн = триал), опц. `PRODAMUS_FORM_URL`. ⚠️ **СИГНАТУРУ/ПОЛЯ НАДО ПРОВЕРИТЬ ЖИВЫМ коллбэком** при активации (хэндрол HMAC — не тестировался против реального Продамуса; на mismatch вебхук логирует ключи для подгонки).

### ⚠️ ПЕРВЫМ ДЕЛОМ в новой сессии
1. **Статус на 14 июня:** миграция 016 ПРИМЕНЕНА ✓, Фаза 1 ЗАДЕПЛОЕНА (2c58eba) ✓, точечная подложка сторис задеплоена (7e81d16) ✓. Осталось спросить владельца: проверила ли рекуррент в ЛК Продамуса? Теперь, когда миграция применена, можно **вживую глазами проверить баннер триала + экран апгрейда** (тест-аккаунт: выставить trial_ends_at ~3 дня сервис-ключом → увидеть баннер; диалог — диспатч `ama:show-upgrade` через preview_eval).
2. **Фаза-1 UI — СДЕЛАНО** (экран апгрейда + баннер триала + 402-хэндлеры, в рабочем дереве). Осталось: (а) **live-проверка ГЛАЗАМИ** баннера/диалога — лучше ПОСЛЕ миграции (до неё нет колонок trial_ends_at → баннер пустой; диалог можно проверить, диспатчнув `ama:show-upgrade` в консоли через preview_eval); (б) **стейт-машина переходов** trialing→view_only→paused (запись в БД при входе/крон) — нужна только когда включаем BILLING_ENFORCED; пока баннер сам выводит состояние из trial_ends_at.
3. **Фаза 2 — Stripe ГОТОВ** (код задеплоен, спящий). Для активации: владелец применяет миграцию 017 + ставит env `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` (тестовый режим) + добавляет вебхук — детали в блоке «💳 Фаза 2 — STRIPE» выше. Затем живой тест оплаты в test-mode. **Продамус (РФ)** — следующий тем же паттерном, когда владелец подтвердит рекуррент в ЛК.
4. **Фаза 3:** email конца триала (нужен провайдер, напр. Resend — ключ от владельца; пока in-app), кэш в оставшиеся AI-роуты, рефералка «месяц в подарок» + партнёрка продюсеров (снос MLM).
5. Метеринг-решение (что списывает единицу) — см. PRICING §13; правило разработки не нарушать (tsc+build до коммита, lightningcss-копия, деплой=push, миграции/env применяет владелец, env -u ANTHROPIC_* для dev AI-роутов).

---

## 🔜 ПРЕДЫДУЩИЙ СТАТУС (обновлено 13 июня 2026 — марафон фидбэка: визуал-движок v2, правила голоса, видео-оверлей)

### Сделано за сессию 8–13 июня (ВСЁ в main, задеплоено; детали — в датированных блоках ниже + git log ~60 коммитов)
- **Навигация/UX:** меню проекта 13→8 плиток (цвета ротируются в палитре бренда); «AI-ассистент»+«Сделать контент» → одна «Создать контент» (чат); Исследование → в Материалы; Виральные рилз → в Тренды (+понятное «добавлено» с подсветкой); «Мой стиль» слит в «Готовое» (**RAG теперь реально учится на saved_content per-project**, плашка не врёт); «Готовое»: просмотр карточки → «Редактировать в чате» (AI спрашивает, что менять) + «Оформить визуально» + «Добавить свой пост»; страница **«Создать визуал»** (сторис/картинка поста/карусель/видео); сироты УДАЛЕНЫ (/generator, /style-bank, /calendar; /viral-reels→redirect); «Твои бонусы» скрыт до пересборки рефералки (реко: «месяц в подарок» + партнёрка продюсеров — строить с биллингом).
- **Качество текста:** week-brief контекст 800→3500 симв + кэш; план прогрева видит niche-тренды/рилзы + кэш; анти-теллы: 2 новых ЖЁСТКИХ запрета (существительные через точку; «И знаешь, что самое…?») + per-field напоминание в JSON-форматах; **📌 ПРАВИЛА ГОЛОСА per-project** (кнопка под каждым ответом + авто-подсказка на «не пиши/всегда/запомни» → project_materials.voice_rules → ВЕРХ системного промпта chat/generate/edit).
- **Визуал-движок v2 (марафон по её видео):** бренд в Материалах, вкладки «Стиль постов»/«Стиль сториз» (story-стиль в brand_kit.story БЕЗ миграции; авто-распознавание сразу после загрузки примеров; превью сториз на её фото; × удаление примеров; «Распознать заново»+тайминги); сторис: плашки СПЛОШНЫМИ строками ВПРИТЫК (StoryText), widow-fix (retry-вёрстка), позиция+подложка по **АНАЛИЗУ ФОТО** (lib/photoBands: не класть текст на лицо, на ровном фоне БЕЗ плашек), **ДОСЛОВНАЯ раскладка** (сторител/карусель/пост-хук: ни слова не менять; «Сторис N»=границы) + **CTA-фильтр КОДОМ** (blacklist «пиши в директ»/«листай дальше» + verbatim-гард последнего кадра — промпт модель пробивала); **галерея «Мои оформленные сторис»** (автосейв в brand_kit.story_sets + Storage, «Редактировать», ZIP, кап 12) + **правки голосом** для сторис И каруселей (только запрошенное, локи поверх автоматики); свои фото в слайды карусели (📷 per-slide + скрим); форматы поста 4:5/1:1/горизонтальный; авто-крючок (понимает задание «сделай с заголовком X», иначе дословная фраза) + подпись с копированием.
- **🎬 ВИДЕО С ТЕКСТОМ v1:** /visual → «Видео с текстом (сторис)»: видео по signed-url (50МБ) → `/api/video/overlay` — **ffmpeg-static ПРЯМО НА VERCEL** (без внешних сервисов; next.config трейсит бинарь), прозрачный PNG-оверлей НАШИМ движком (плашки/чистый, позиция, story-бренд) → 1080×1920 H.264. Пайплайн проверен локально end-to-end. **⚠️ ПЕРВЫЙ ПРОД-ПРОГОН — за владельцем (короткий ролик!); если Vercel-лимиты подведут — план Б Shotstack.**
- **Надёжность:** диктовка переживает слабый LTE (моно 48kbps ≈ ×6 меньше, таймауты, авто-ретрай, человеческие ошибки; >3.5МБ через Storage); фото с iPhone (даунскейл на устройстве + по одному файлу — Vercel 4.5МБ); из аудита: `/api/carousel/render` закрыт auth (был открыт!), контент-план показывает СВЕЖУЮ версию юнита («В план» терялся за старой), шаг-гайд → ассистент.
- **Модель:** вышла Fable 5 ($10/$50 = 2× Opus 4.8) — **РЕШЕНИЕ: НЕ переходить** (Продюсер уходит в ~0/минус; наш кейс Opus 4.8 закрывает). Код совместим, переход = env. См. PRICING §12.

### ⚠️ ПЕРВЫМ ДЕЛОМ в новой сессии
1. **Спросить владельца про итоги теста визуал-пакета** — особенно 🎬 видео-оверлей В ПРОДЕ (короткий ролик 10–20 сек первым). Всплывшие баги чинить по её видео/скринам (паттерн отработан: `ffmpeg` → Whisper(language=ru) + кадры `fps=1/5`; OPENAI_API_KEY в .env.local).
2. **Дальше по плану — БИЛЛИНГ** (цены утверждены, PRICING.md): тарифы в БД, гейтинг (~300 ген Соло), экран апгрейда (Соло-герой), Продамус (РФ) + Stripe (мир), уведомления конца триала, кэш в остальные AI-роуты (chat/generate/edit/week-brief/warmup-plan УЖЕ с кэшем; остались plan-stories/suggest-angles/analyze-competitors/suggest-trends-синтез), учёт «единиц контента» для дорогих операций (серия сторис, бренд-анализ, видео-оверлей!), **рефералка** по реко (см. блок аудита 11 июня).
3. **Бэклог визуала** (по желанию владельца): серия кадров на одном видео / AI-раскадровка на видео; видео-фон в карусели/посте; «Результаты» → ранжировать «Готовое» по охватам (замкнуть цикл обучения).
4. Правила разработки и живого теста — в разделах ниже (НЕ нарушать; главное: `npx tsc --noEmit` до коммита, build c lightningcss-копией, деплой=push в main, миграции/env применяет владелец, `env -u ANTHROPIC_*` для локального dev AI-роутов, рендеры проверять ГЛАЗАМИ через dev demo-GET).

---

## 🔜 ПРЕДЫДУЩИЙ СТАТУС (обновлено 8 июня 2026 — UX-проход по фидбёку владельца)

### Сделано в этом проходе (8 июня, всё в `main`, `tsc`+`build` чисто; деплой пушем)
Большой UX-проход по 8 скринам + замечаниям Августы (УПРОЩЕНИЕ, не новые фичи):
- **Меню проекта: 13 плиток → 8, единая фирменная палитра** (была «радуга», иконки → `text-primary`). Объединены «AI-ассистент»+«Сделать контент» → одна **«Создать контент»** (→ чат `/assistant`). Убран **Календарь**. Плитки: Создать контент · Контент-план · План прогрева · Материалы · Тренды · Фирменный стиль · Готовое · Результаты. Сайдбар: проектные под-пункты → «Создать контент»/«Контент-план». **`/generator` НЕ удалён** — живёт как просмотрщик «Последнего контента» + шаг гайда новичка; `/calendar`, `/viral-reels`, `/style-bank`, `/stories` тоже живы по URL (просто не плитки).
- **«Виральные рилз» → внутрь «Тренды»** (`ViralReelsManager scope=project` встроен в `/trends`, секция «Залетевшие рилз — референсы»).
- **«Исследование» → внутрь «Материалы»** (карта-вход на `/knowledge` ведёт на `/research`).
- **«Мой стиль» слит в «Готовое»**: плитка «Готовое» → `/library?project=ID` (фильтр по проекту добавлен) + **плашка «🎯 AI учится на этом контенте»** (+ ответ на scope: per-project, данные проекта не текут в другой). **ВАЖНО (логика):** раньше AI учился стилю ТОЛЬКО из `style_examples`; `saved_content` («Готовое») не читался. Теперь `buildRAGContext` (`lib/ai/rag.ts`) подмешивает `saved_content` этого проекта как style-примеры — приоритет **style_examples → saved_content → system**, дедуп по телу, ≤5, КАЖДЫЙ источник в своём `try` (битая таблица не роняет уже собранное). **Миграции НЕ нужно** (обе таблицы есть). «Одобрить стиль» в генераторе по-прежнему пишет в `style_examples` (обе ветки кормят обучение).
- **Тренды: понятное подтверждение** — после «Добавить» тост «Добавлено в Мои тренды… AI вплетёт» + авто-скролл и flash-подсветка раздела «Мои тренды» (раньше был только незаметный тост — «куда добавилось?»).
- **Реферальный слайд онбординга СПРЯТАН** (был «бонусные запросы» — жаргон + без биллинга ничего не даёт). Онбординг 6 → 5 слайдов; в слайде анализа «Style Bank» → «Готовое».

**Догон (2-я итерация фидбэка, тот же день):**
- **«Готовое»: ручное «Добавить свой пост»** (`/library`) → пишет в `saved_content` (project_id из `?project` или выбор в форме) → AI учится (RAG уже читает saved_content). VoiceTextarea (можно надиктовать).
- **«Тренды» вынесены в сайдбар** (проектный под-пункт `✦ Тренды`, рядом с «Создать контент»/«Контент-план»).
- **Картинка поста переосмыслена** (фидбэк «текст не влезает на картинку»): на изображение — короткий **КРЮЧОК** (новый `/api/post-hook` — Sonnet выбирает самую цепляющую фразу; авто-fit шрифта в Photo-шаблоне движка по длине заголовка), весь текст поста — блок **«Подпись»** с копированием + подсказка «весь текст на картинках? → сделай карусель». Правильный IG-паттерн: картинка=хук, текст=подпись.
- **Фирменный стиль** оставлен ОТДЕЛЬНОЙ плиткой (решение владельца: бренд+сторис «похожи и дополняют» — пусть вместе).

### 🔍 Экспертный аудит (11 июня, по запросу владельца, на Fable 5) — фиксы в main, реко ниже
**Исправлено сразу (5 фиксов):** (1) **`/api/carousel/render` закрыт авторизацией** (был ОТКРЫТЫЙ рендер-эндпоинт; demo-GET теперь только dev); (2) **RAG: «Готовое» реально доезжает до генерации** — жёсткий фильтр content_type прятал чат-сейвы ('other'/null) от типизированной генерации; теперь свой формат в приоритете, остальные принимаются как голос; (3) шаг 4 гайда новичка вёл на /generator → теперь /assistant (единый «Создать контент»); (4) онбординг-подсказки ссылались на несуществующие названия («Анализ аккаунта», старое место «Фирменного стиля») → переписаны на реальные разделы; (5) BottomNav: удалён стейл-спецкейс /generator.
**Проверка агентских находок (важно):** анти-AI-тэллы ПОКРЫВАЮТ JSON-форматы (через buildSystemPrompt) — отчёт агента ошибался; «все эндпоинты authed» — ошибался (render был открыт); «$300/мес утечка» — преувеличение (главный потребитель chat = fair-use по утверждённой политике PRICING §9).
**✅ ВНЕДРЕНО (владелец сказала «давай», 11 июня, в main):**
- **Сироты убраны:** УДАЛЕНЫ `/generator`, `/style-bank`, `/calendar` (страницы; APIs `/api/style-bank` для админки и данные `style_examples` ЖИВЫ — RAG читает); `/viral-reels` → redirect на `/trends`; «Твои бонусы» скрыт из сайдбара (роут `/referral` жив). Ссылки перевешены: гайд-шаг 4 + dashboard-CTA + WarmupTimeline → `/assistant`; «Последний контент» → `/content-plan?week=N` (неделя по day_number айтема).
- **Качество (a)+(b):** week-brief контекст 4×800 → 10 кусков/3500 симв (кейсы/продукт доезжают) + **кэш** (системный блок per-project, недели 2+ читают по ~10% цены); **план прогрева теперь видит niche-тренды и залетевшие рилзы** (блок «АКТУАЛЬНОЕ В НИШЕ» — как УГЛЫ для смыслов дней, БЕЗ форматов — план остаётся format-free) + **кэш** (ретраи/«начать заново» дешевеют).
- **Качество (c-лайт):** в JSON-генерации (reels/carousel/stories/live/email) явное напоминание голоса per-field (без тире, без телеграфных обрывков, без пустых обещаний). Полноценный второй валидатор-проход для JSON решили НЕ делать (удвоил бы стоимость генерации) — промпт-вариант бесплатный.
**Осталось из реко (на потом):** (d) «Результаты» → ранжировать «Готовое» по охватам; week-brief/прогрев прогнать вживую после деплоя (качество тем).
- **Биллинг-этап (уже в плане):** кэш в warmup-plan/plan-stories/suggest-angles/analyze-competitors/week-brief; учёт «единиц контента» для plan-stories/brand-analyze/suggest-trends/analyze-competitors (chat остаётся fair-use); крон-чистка audio-temp + каскад project-brand при удалении проекта; неатомарный refundGeneration.

### 🎨 3-я итерация фидбэка (11 июня, скрины+видео владельца) — всё в main
- **Онбординг** больше не радуга: все слайды на фирменном градиенте `BRAND_GRADIENT` (#F5A84A→#E86BA0→#D44E7E, = .gradient-accent).
- **«Фирменный стиль» переехал в Материалы** (карточка-вход на /knowledge) и получил **2 вкладки: «Стиль постов» / «Стиль сториз»** (просьба владельца). Story-стиль хранится в **`projects.brand_kit.story` (jsonb) — миграция НЕ нужна**: `/api/brand-kit/analyze` принимает `target:'story'` (vision-промпт под сторис, пишет в kit.story, основные колонки не трогает), POST `/api/brand-kit` принимает `{story:{accentColor,bg,text,bgStyle}}` (merge). Сторис-билдер и превью берут story-стиль ПОВЕРХ основного (фолбэк — стиль постов; плашка «пока сторис наследуют стиль постов»). UX-фиксы по фидбэку: после распознавания кнопка → вторичная **«Распознать заново»** + зелёная галка «Стиль распознан»; подсказка **«~30-60 секунд»** при анализе. Ник/лого — общие (вкладка постов).
- **Новая страница «Создать визуал»** `/projects/[id]/visual` (плитка в меню проекта вместо «Фирменный стиль»): 1) Серия сториз → билдер; 2) Картинка к посту (вставь текст → PostImage с крючком); 3) Пост-карусель (вставь текст → CarouselSlides). Владелец: «дальше докрутим».
- **Сторис-ответ в чате** → кнопка **«Оформить сторис»** (не «Сделать картинку поста» — было нелогично): `StoryDesignButton` кладёт сценарий в localStorage `ama_stories_script_<projectId>` → билдер подхватывает и префиллит. Детект: `genContext.type==='stories'` или /(сторис|stories|кадр)\s*\d/i. Порядок проверок: карусель → сторис → пост.
- **Safari-фикс:** страница проекта не докручивалась до конца (видео) — контейнеру добавлен `pb-28` (хвост уходил под нижнюю навигацию).
⚠️ Проверить владельцу вживую: вкладки стилей (распознать сторис-стиль по её примерам), «Создать визуал», кнопку «Оформить сторис» из чата.

### 🎬 Оформление сторис по УРОКУ владельца (11 июня, видео + урок «Визуальная концепция профиля»)
Видео-фидбэк по рендеру сторис: текст нечитаем (тёмный на фото), белые градиенты-подложки «не мой стиль», все кадры проштампованы одинаково (хук сверху + текст снизу), нет акцентов/обводки, «куда сохранилось — выйду и не найду», «всё зелёное в меню — скучно». Плюс: «возьми мой урок из материалов — там правила визуала».
**Сделано:**
- **Урок НАЙДЕН сам** в `knowledge_vault`: «Визуальная концепция профиля» (id 442c8569…, 29К символов, транскрипт). Дистиллирован в **`VISUAL_RULES`** (content-brain.ts): ≤2 коротких предложения на кадр; читабельность превыше всего; 2 размера шрифта; акцент фирменным цветом на 1-2 словах; перечисления структурой; первый кадр цепляет; вовлечение ~20%; единый стиль без чужих элементов. Вшит в `plan-stories` и `carousel/structure` (→ применимо и к постам/каруселям, как она просила).
- **Движок Story переписан** (engine.tsx): УБРАНЫ белые градиенты; текст поверх фото — **плашки-чипы по словам** в цветах story-стиля (RichText опция `chipBg`) → читаемо на ЛЮБОМ фото; акцентные **слова** цветом; ник в чипе; **`position` top/center/bottom** на кадр (SlideSpec) — plan-stories возвращает позицию (чередует, схема+валидация, фолбэк-чередование), серия больше не проштампована. На ровном бренд-фоне плашек нет (там и так читаемо).
- **Черновик сторис-билдера**: localStorage (сценарий+фото) → авто-восстановление при возврате; честная подсказка «скачай ZIP — в сервисе картинки пока не хранятся».
- **Плитки меню проекта** — цвета ротируются В ПАЛИТРЕ бренда (green/#D44E7E/#F5A84A/#E86BA0) — «повеселее», но в концепции.
- **Проверено ГЛАЗАМИ локально** (dev + расширенный demo-GET `&photo=&pos=`): чипы поверх реального фото читаемы, top/bottom работает, акценты розовым. (Урок «tsc≠проверка» применён.)
**✅ Бэклог ЗАКРЫТ в тот же день (владелец: «ну а это ты сделаешь?»):**
- **Галерея «Мои оформленные сторис»** на `/stories`: после «Собрать сторис» серия АВТОСОХРАНЯЕТСЯ — кадры (PNG→JPEG на устройстве, по одному файлу/запрос) в бакет `project-brand/stories-out/`, индекс сетов в `projects.brand_kit.story_sets` (jsonb, БЕЗ миграции, кап 12 сетов, при трим/удалении чистятся и файлы). Роут `/api/stories/sets` GET/POST/DELETE (ownership). UI: список сетов (дата+сценарий+миниатюры) + ZIP + удалить. ⚠️ Сопутствующий фикс: analyze (posts) теперь МЕРЖИТ brand_kit (раньше повторное распознавание затёрло бы story_sets/story).
- **Правки оформленных сторис голосом/чатом**: `/api/ai/edit-stories` (кадры+инструкция → правленые кадры; меняет ТОЛЬКО запрошенное, кол-во кадров не меняет, VISUAL_RULES+анти-теллы; «на третьей…» понимает) + панель «Правки — голосом или текстом» (VoiceTextarea) под готовой серией → пере-рендер → пересохранение ТОГО ЖЕ сета (старые файлы заменяются).
Проверки: tsc+build чисто; live-флоу (сборка→автосейв→правка голосом→галерея) тестирует владелец.

### 🎯 Визуал-полировка (12 июня вечер, видео+скрин владельца) — в main
Фидбэк: в «Создать визуал» нельзя диктовать; нужен выбор из Готового; вставила «сделай картинку с заголовком X» → обложка с первыми словами задания; нужен горизонтальный формат; сторис — ОПЯТЬ висячее слово и ОПЯТЬ призыв от себя на последнем кадре; подсказка про порядок фото; «будем делать видео-сторис?»
- **/visual**: оба поля → VoiceTextarea (диктовка); кнопка **«Выбрать текст из „Готового"»** (модалка-пикер; сторис-текст → билдер, карусель/пост → своё поле); подписи «вставь ГОТОВЫЙ текст… разложит ДОСЛОВНО».
- **post-hook понимает ЗАДАНИЯ**: «сделай картинку с заголовком X» → вернёт ровно X; иначе дословная фраза из текста. **PostImage: авто-крючок при открытии** (если заголовок = первая строка и текст длинный) + **формат «Горизонтальный» 1.91:1** (`FORMATS.postWide` 1080×566).
- **Висячие слова — добивка**: wrapWords теперь пере-вёрстывает с уменьшенной шириной (retry −2…−8 симв) до исчезновения widow; pull-down смягчён (prev ≥2 слов). Старый фикс пропускал случай «предыдущая строка из 2 слов».
- **Отсебятина — добивка КОДОМ**: blacklist-CTA («пиши в директ», «листай дальше», «жми ❤», «смотри до конца»…) вырезается из headline/body ЛЮБОГО кадра, если фразы нет в сценарии; **последний кадр** — verbatim-гард: его предложения, отсутствующие в сценарии, выбрасываются целиком.
- Сторис: подсказка «в каком порядке загрузишь фото — в таком и оформит (1-е фото = 1-й кадр)».
- **🎬 ВИДЕО С ТЕКСТОМ — СДЕЛАНО v1** (владелец уточнила объём: «просто накладывал текст на видео»): БЕЗ внешних сервисов/ключей — **ffmpeg-static прямо на Vercel** (новая зависимость в package.json; next.config: serverExternalPackages + outputFileTracingIncludes тащат бинарь в функцию /api/video/overlay). Пайплайн: видео грузится в Storage по signed-url (`/api/video/upload-url`, project-brand/{id}/videos/, кап 50МБ) → `/api/video/overlay` (maxDuration 300): скачивает, рендерит **прозрачный PNG-оверлей нашим же движком** (SlideSpec.transparent; story-стиль бренда, плашки/чистый, позиция top/center/bottom) → ffmpeg scale/crop 1080×1920 + overlay + H.264/AAC → результат в videos-out → public URL; исходник удаляется. UI на /visual: секция **«Видео с текстом (сторис)»** (загрузка, текст голосом, позиция, плашки-тоггл, превью+скачать; тайминги подписаны). **Пайплайн проверен локально глазами** (прозрачный RGBA подтверждён, тест-видео прожжено ТЕМ ЖЕ фильтром — кадр с плашками/акцентами/CTA поверх видео ок). ⚠️ Первый ПРОД-прогон за владельцем: если на Vercel бинарь/лимиты подведут — план Б: Shotstack. Багфикс попутно: Story рендерил `<img>` без src при transparent (фон теперь строго по s.photoUrl). Следующий шаг видео (по желанию): серия кадров на одно видео / AI-раскадровка на видео.

### 🛡️ Надёжная диктовка + «В Готовое» + 📌 ПРАВИЛА ГОЛОСА (12 июня, скрины/видео владельца) — в main
Фидбэк: «наговорила минуту — висит, 10 сек — Load failed, сайт долго грузит со всех устройств»; «куда убралась возможность делать избранное для обучения»; «Вика говорит „не пиши так" — он сохраняет это как паттерн в её проекте?»
1. **Прод проверен — здоров** (TTFB ~0.4s все маршруты, API 401 за 0.24s): «висит/Load failed» = слабый канал (LTE) + хрупкая диктовка. **Укреплено** (`useVoiceRecorder`): запись теперь **моно 48kbps** (минутная запись ~2МБ → ~350КБ — переживает слабый LTE), все сетевые вызовы с **таймаутом** (90/120с, AbortController), прямой путь **авто-ретрай 1 раз**, ошибки человеческие («Сеть оборвалась… надиктуй ещё раз» вместо вечного спиннера/«Load failed»). /api/ai/edit уже maxDuration 300 ✓.
2. **«Избранное» никуда не делось — стало незаметным**: кнопка в чатах переименована **«Сохранить» → «В Готовое»**, тост теперь объясняет: «Сохранено в Готовое ⭐ — AI ЭТОГО проекта будет учиться на этом тексте» (обучение per-project, RAG читает saved_content c прошлого захода).
3. **📌 ПРАВИЛА ГОЛОСА (новое, ответ на вопрос про Вику — раньше НЕТ, теперь ДА):** `/api/voice-rules` (append-дедуп-кап40 в `project_materials` type=`voice_rules`, title «Правила голоса (от блогера)» — видно в Материалах) → `buildRAGContext.voiceRules` → **секция в самом верху системного промпта** «ЛИЧНЫЕ ПРАВИЛА БЛОГЕРА — нарушать нельзя» → действует на chat/generate/edit (+edit-stories/carousel через AI_TELLS? нет — у них свои промпты; на следующий заход). UI: кнопка **«📌 Правило»** в строке действий каждого ответа (assistant+create, модалка с диктовкой) + **авто-подсказка**: если в сообщении паттерн «не пиши/всегда/запомни…» → toast с кнопкой «📌 Запомнить» (one-tap). Правила — per-project.

### ⛔ ДОСЛОВНОСТЬ при оформлении + правки сохранённых серий (11 июня вечер, фидбэк владельца) — в main
Фидбэк: «он ДОХУЯ поменял текст сторитела при оформлении, сократил»; «опять добавил „ответь мне в директ"»; «нет возможности редактировать визуал [сохранённый]».
1. **Текст утверждённого контента при оформлении НЕ МЕНЯЕТСЯ — нигде.** `plan-stories` переписан в режим «раскладчика»: формулировки ДОСЛОВНО, ни одного нового/выброшенного слова; маркеры «Сторис N» = готовые границы кадров; длинный кадр → ДЕЛИТСЯ на два, не сокращается; добавлять можно только **акценты** и переносы (VISUAL_RULES оттуда убран — конфликтовал «минимумом текста»). `carousel/structure` — те же правила (маркеры «Слайд N», делить а не сжимать). `post-hook` — крючок строго ДОСЛОВНАЯ фраза из текста (можно усечь), не перефраз.
2. **CTA-отсебятина убита КОДОМ, не промптом** (промпт модель пробивала): в plan-stories серверный фильтр `ctaFromScript` — cta выживает только если он реально есть в тексте блогера (нормализация + substring или ≥70% слов). «Листай дальше»/«пиши в директ» физически выбрасываются.
3. **Сохранённые серии можно править**: кадры теперь хранят source-фото (`SetFrame.photo`, frame.photo сквозь build/edit/save); в галерее кнопка **«Редактировать»** → серия восстанавливается в билдер (script+фото+позиции locked) → пере-рендер → панель правок (голосом) → пересохранение ТОГО ЖЕ сета.
4. **Картинка поста**: крючок теперь через VoiceTextarea (правка голосом). Карусели уже имели панель правок (предыдущий пуш).

### 🎨 Большой апгрейд оформления (11 июня, 6 пунктов владельца + видео про карусели) — всё в main
1. **Плашки слитные**: `StoryText` (бывш. PlateText) — строки-плашки ВПРИТЫК без межстрочных зазоров (как нативный текст-фон IG).
2. **Widow-fix**: общий `wrapLines` не оставляет одно слово на последней строке (перетягивает слово с предыдущей).
3. **Микс с подложкой/без**: `lib/photoBands.ts` — клиентский анализ фото (3 зоны: дисперсия+яркость, canvas 72×128, crossOrigin; зоны соответствуют паддингам движка). Спокойная зона → ТУДА текст; зона однотонная (variance<0.008) → **plate=false, чистый текст** (цвет по яркости: светлая зона → тёмный бренд-текст, тёмная → белый). SlideSpec: `plate?: boolean, textColor?`.
4. **Расположение по фото**: позиция текста = самая спокойная зона (центр штрафуется ×1.6 — там обычно лицо/фигура; bottom предпочтительнее). Никаких «текст на голове».
5. **Без отсебятины в CTA**: plan-stories — cta ТОЛЬКО из сценария блогера; «листай дальше»/«пиши в директ» НЕ выдумывать.
6. **Правки голосом расширены**: edit-stories понимает «подними текст наверх / убери подложку / выдели слово» (schema + plate: with/without, только если просили). Приоритет: ручная правка (lock при изменении) > анализ фото > подсказка AI; lock-флаги на Frame (posLocked/plateLocked).
7. **Карусели: правки голосом/чатом** (видео: «подредактировать не могу, только скачать») — `/api/ai/edit-carousel` (структура + инструкция → только запрошенные изменения; перенос «15 тысяч рублей» чинится переформулировкой) + панель правок (VoiceTextarea) в модалке `CarouselSlides` → пере-рендер.
8. **Свои фото как фон слайдов карусели** (доделано по требованию владельца сразу): `Frame` принимает `photo` → фото + тёмный скрим (0.38→0.66), текст белым, футер светлый (Cover/Content/CTA). Рендер-роут принимает `photos: {slideIndex: url}`. UI: на каждом слайде кнопка **«📷 Фото»** (загрузка с даунскейлом, kind='post') / «✕ фон» → пере-рендер ОДНОГО слайда; карта фото переживает текстовые правки. Проверено глазами в dev (фото+скрим+белый текст ок).
Проверено глазами в dev: плашки впритык на двери; чистый белый текст на спокойной зоне. Анализ фото — клиентский, владелец проверит на реальных сериях.

### 🐛 Фикс 11 июня (видео владельца): «В план» сохранял, но план показывал СТАРУЮ версию
Симптом: из чата контент-плана утвердила 2 рилза → «В план» → галочка, но в плане по «Посмотреть» — старый рилз, новых «нет». Причина: грид показывает ОДИН юнит на день+формат через `find()`, а выборка `content_items` шла БЕЗ сортировки → всегда всплывала старейшая версия, новые сейвы (version_number растёт) невидимы. Фикс: `content-plan/page.tsx` — `.order('created_at', {ascending:false})` → план показывает самую свежую версию. Старые версии остаются в БД как история.
(Второе видео той же отправки — без речи: результат сторис с новыми сплошными плашками, выглядит как надо.)

### 🩹 Догон по сторис (11 июня, скрины владельца): плашки СПЛОШНОЙ полосой + тайминги
- «Подложка должна быть полностью под фразой» — пословные чипы рвались. Новый **`PlateText`** в движке: свой перенос слов (оценка ширины ~0.62em/символ Montserrat) → **одна сплошная плашка на КАЖДУЮ СТРОКУ** (как нативный текст-фон Instagram), акцентные **слова** фирменным цветом внутри строки. Story-шаблон поверх фото использует PlateText; на ровном фоне — обычный RichText. chipBg из RichText удалён (мёртвый код). **Проверено глазами в dev на реальном фото** (top/bottom, акценты).
- Тайминги/подписи этапов на `/stories`: загрузка фото «5-15 сек на фото», кнопка теперь **«Оформить сторис» / «Оформляю сторис…»** + «обычно 1-2 минуты, не закрывай страницу», правка «до минуты».

### 📖 «Готовое»: открыть → редактировать → оформить (11 июня, видео владельца)
Фидбэк: карточку в «Готовом» нельзя открыть (только копировать); нужен флоу «открыл → Редактировать (в чате, AI спрашивает что менять) → Оформить визуально».
**Сделано:** (1) карточка кликабельна → **модалка-просмотр** (полный текст + Копировать/Редактировать/Оформить визуально); (2) **«Редактировать»** → handoff в `/create` через localStorage `ama_edit_prefill` {text, projectId} — чат сам отправляет текст с просьбой «спроси, что поменять» (проект айтема выбирается; гонка с дефолт-проектом решена functional-update + pendingEdit-эффект); (3) **«Оформить визуально»**: сторис-текст → сторис-билдер (существующий handoff `ama_stories_script_*`), карусель/пост → `/visual` с префиллом (`ama_visual_prefill_*` {type,text}, visual читает и подставляет в нужное поле). Без project_id — понятный тост. Детект типа: content_type или маркеры в тексте.

### ✍️ Анти-AI-тэллы: 2 новых ЖЁСТКИХ запрета (11 июня, фидбэк владельца по сторис-тексту)
Владелец разобрала сгенерированную серию сторис и зафиксировала 2 маркера:
- **Существительные через точку** («Море. Солнце. Новая жизнь.» / «Ребёнок. Работа. Чужой язык.») — было мягкое «максимум один приём», теперь **запрет полностью** (правило 2 в `AI_TELLS_TO_AVOID`, с её примерами и заменами).
- **Шаблонные вопросы-подводки** («И знаешь, что самое тупое?», «И знаете, что самое важное?», любые «И знаешь(те), что/почему…?») — новое правило 3: переход утверждением («Самое тупое: …»).
Внесено в: `AI_TELLS_TO_AVOID` (content-brain.ts → все генераторы через системный промпт), валидатор (`buildValidatorUserPrompt` чек-листы 6 и 8), per-field JSON-напоминание в generate. Нумерация правил сдвинулась (офферы=4, размытые=5).
Плюс мелочь: «смотри превью справа» → «превью обновилось» (на мобиле превью снизу, не справа).

### 🎨 UX-фикс бренда (11 июня, видео владельца): примеры — ГЛАВНЫЙ источник, всё автоматом
Фидбэк (видео): «загрузила примеры, а настройки и превью как стояли, так и стоят»; «если есть примеры — надо опираться на них, настройка — опционально»; «почему сторис-превью не как мои сторис»; «как выбрать фон, если у меня не однотонный».
**Сделано (`brand/page.tsx`):**
- **Авто-распознавание после загрузки примеров** (обе вкладки): загрузила → сразу `analyze()` (с переданным списком URL, не из стейла-стейта) → палитра+превью обновляются сами; тост «распознаю твой стиль (~30-60 сек)». Кнопка осталась как «Распознать заново». Guard от двойного запуска.
- analyze и так ПЕРСИСТИТ распознанное в БД → копия-лайн «Стиль распознан и применён»; секция 2 переименована в **«Доводка стиля (не обязательно)»** с подписью «AI заполняет это сам из примеров»; «Тип фона слайдов» + подпись «AI выбирает ближайший из трёх» (ответ на «у меня фон не однотонный» — фото-фоны движок не рендерит, ближайший тип).
- **Сторис-превью рендерится ПОВЕРХ первого загруженного примера сториз** (slide.photoUrl) — её сторис = живые фото с плашками, плоский фон выглядел «не моё». Фолбэк — ровный фон, если примеров нет.
- Превью при распознавании затемняется + плашка «Распознаю твой стиль — превью обновится само».

### 🐛 Фикс 11 июня: загрузка фото падала на iPhone («The string did not match the expected pattern», Safari)
Владелец ловила ошибку при загрузке фото примеров стиля. Причина — тот же класс, что у диктовки: **iPhone-фото 4-12 МБ (HEIC/JPEG) > лимит тела запроса Vercel ~4.5 МБ** → 413 ДО роута → не-JSON ответ → Safari-криптограмма из `res.json()`.
**Фикс:** `lib/downscaleImage.ts` — даунскейл НА УСТРОЙСТВЕ перед загрузкой (canvas → JPEG; HEIC конвертится — заодно чинит то, что sharp на Vercel не умеет HEIC в analyze) + **по одному файлу за запрос** (батч из 8 тоже не пробьёт лимит) + понятные ошибки (`res.json().catch` + спец-текст на 413). Применено во всех 3 точках загрузки фото: бренд-примеры (обе вкладки, лого 900px / примеры 1600px), фото сторис-билдера (2000px), фото поста в PostImage (2000px). Деградация качества нулевая для наших нужд (vision читает ≤820px, рендер ≤1080×1920). Fallback: если декод не удался — грузим оригинал.
⚠️ Урок процесса: tsc/build/логика ≠ живой тест с РЕАЛЬНЫМИ фото с телефона; класс «Vercel 4.5MB» теперь закрыт на всех known-поверхностях (диктовка ✓, интервью ✓ через Storage, фото ✓ через даунскейл).

### 🐛 Фикс 10 июня: длинная диктовка падала (фидбэк тестировщицы, видео)
Тестировщица наговаривала ~2 мин итогов созвона в «Материалы → Добавить текст» → ошибка (короткие диктовки работали).
**Причина:** `useVoiceRecorder` слал весь блоб напрямую в `/api/ai/transcribe-voice`, а **Vercel режет тело запроса ~4.5 МБ ДО роута**; iOS пишет AAC с высоким битрейтом → пара минут ≈ 3-4+ МБ → 413.
**Фикс (`lib/useVoiceRecorder.ts`):** записи ≤3.5 МБ — прежний быстрый путь; больше — **через Supabase Storage** (existing `audio-temp`: `/api/ai/transcribe/upload-url` → `uploadToSignedUrl` → `/api/ai/transcribe {storagePath, isLastChunk:true}` — роут сам удаляет temp-файл; при ошибке клиент чистит сам). Лимит 20 МБ (~15+ мин) с понятной ошибкой. `transcribe-voice` maxDuration 60→300. Миграций не нужно (бакет существует, флоу исследования им уже пользуется в проде).
⚠️ Владелец/тестировщица: перепроверить вживую двухминутную диктовку на iPhone после деплоя.

### 🤖 Решение по модели (10 июня): НЕ переходим на Fable 5, остаёмся на Opus 4.8
Вышла Claude **Fable 5** (`claude-fable-5`) — новый тир НАД Opus, **$10/$50 за 1М токенов = ровно 2× Opus 4.8** ($5/$25).
Проверил по официальной справке + наш код:
- **Совместимость:** в наших AI-роутах нет `temperature`/`top_p`/`thinking` → технически переход = смена `ANTHROPIC_CONTENT_MODEL` (единственная ловушка Fable 5: явный `thinking:{type:'disabled'}` даёт 400 — у нас его нет).
- **Экономика (главное):** генерация ~$0.08 → ~$0.16. Маржа: Соло ~65%→**~40%**, Про ~68%→**~41%**, **Продюсер ~46%→≈0/минус** (цены утверждались под Opus 4.8!).
- **Качество:** сила Fable 5 — долгие агентные задачи/код/knowledge work; наш кейс (короткий RU-контент голосом блогера, anti-AI-tells, forced-tool JSON) Opus 4.8 уже закрывает отлично (прогон 7 июня: регрессий нет, качество выросло).
**Рекомендация (принята как дефолт):** остаёмся на Opus 4.8. Пересмотреть после запуска биллинга (вариант: Fable 5 как премиум-фича топ-тарифа c соотв. ценой) или при снижении цен. Политика смены модели — `PRICING.md` §12 (env + прогон качества, не слепой апгрейд).

### ⚠️ Реферальная система — владелец решила ПЕРЕСОБРАТЬ (реко дана, финал — на биллинге)
Августа: реферал сырой, сервис изменился. Моя рекомендация (принять/доработать на этапе оплаты):
1) **Блогер→блогер: «месяц в подарок»** — друг оформляет тариф → оба получают месяц бесплатно / −50%. Без «запросов», без 2-го уровня (MLM убрать).
2) **Партнёрка для продюсеров (главный канал роста):** приводит клиентов → recurring-% с их подписок ИЛИ бесплатный «Продюсер» при N платящих.
Обе завязаны на оплату → **строить ВМЕСТЕ С БИЛЛИНГОМ** (там же промокоды/скидки). Слайд спрятан; страница `/referral` («Твои бонусы») пока оставлена — переработаем с биллингом.

### ⚠️ ПЕРВЫМ ДЕЛОМ дальше
1. Владелец смотрит UX-проход вживую на телефоне (меню / Готовое+плашка / Тренды+рилз / Материалы+исследование). Доправить по фидбёку.
2. Дальше большой блок: **БИЛЛИНГ** (цены утверждены — `PRICING.md`) ИЛИ **лендинг-финиш** (ждём от неё реальные отзывы + ссылки футера). Реферальную систему встроить в биллинг по реко выше.

---

## 🔜 СТАТУС / ПЕРВЫМ ДЕЛОМ В НОВОЙ СЕССИИ (обновлено 7–8 июня 2026 — визуал + экономика + активация)

### Сделано за сессию 7–8 июня (всё в `main`, прогнано; детали — в тематических разделах ниже + `git log`)
- **Визуальный контент (флагман):** движок слайдов-картинок на **`next/og`** (карусели 4:5 / посты / сторис 9:16) + кнопка «🖼 Сделать слайды-картинки» (превью + ZIP). **Работает из чата** (карусель пишется текстом → `/api/carousel/structure` структурирует → картинки). **«Сделать картинку поста»** (`PostImage`): заголовок поверх СВОЕГО фото или на фирменном фоне, формат 4:5/1:1 — на ВСЕХ экранах (ассистент/создать/генератор). **Сторис-билдер** `/projects/[id]/stories` (фото+сценарий→картинки). Владелец подтвердила: создание постов/каруселей-картинок РАБОТАЕТ.
- **Бренд-кит (индивидуальный стиль на проект):** `/projects/[id]/brand` — загрузка примеров → Claude **vision** извлекает палитру/фон/шрифт → правка цветов/лого/ника → живое превью → сохранить (`projects.brand_*`, миграция 015 применена). Движок dark-aware. Слайды берут бренд проекта.
- **Таблицы → настоящий XLSX:** план прогрева (Линия/Тема), исследование (пивот участники×вопросы), карта смыслов, **анализ конкурентов** (новый AI-инструмент в разделе «Конкуренты»).
- **AI-ассистент:** достаёт «Готовое» в чате (не просит «скинь текст») + проактивно предлагает углы/форматы/тренды при создании из контент-плана.
- **Модель + экономика:** весь контент на **Opus 4.8** (через env `ANTHROPIC_CONTENT_MODEL` — смена модели БЕЗ кода) + **prompt-кэширование** (в `chat`/`generate`/`edit`; ⚠️ остальные AI-роуты — докинуть кэш для маржи). Полная юнит-экономика + тарифы — **`PRICING.md`**.
- **Активация:** находимость (плитки «Фирменный стиль»/«Оформление сторис» в меню проекта), онбординг (показ при КАЖДОМ входе + реальная галочка «больше не показывать»; флаг старых юзеров сброшен), лендинг (тариф-онли, позиционирование «визуал + продюсеры», фейк-отзывы убраны).
- **Крон трендов** — проверен в проде (в начале сессии).

### ⚠️ ПЕРВЫМ ДЕЛОМ в новой сессии
1. Прочитать **`PROJECT_CONTEXT.md` + `PRICING.md` + `AGENTS.md`**, затем `git log --oneline -30`.
2. **Спросить владельца про приоритет.** Готово к работе:
   - **БИЛЛИНГ/ОПЛАТА** (главный следующий блок; цены УТВЕРЖДЕНЫ — Соло $49 / Про $149 / Продюсер $299, см. `PRICING.md`): тарифы в БД, гейтинг лимитов (~300 генераций на Соло), экран апгрейда (Соло — герой), **Продамус (РФ) + Stripe (мир)**, уведомления о конце триала, + докинуть **кэш во ВСЕ AI-роуты**. Открытые решения владельца: финальные лимиты, тариф «Старт» (потом), команда в Про (+1 место).
   - **Лендинг-финиш** (ЖДЁМ от владельца): реальные отзывы (2-3) + ссылки футера (privacy / terms / поддержка / Instagram / Telegram).
   - **Видео-сторис** (нужен видео-движок Shotstack/ffmpeg) или другое по запросу.
3. Тест-доступ/правила разработки — в разделах ниже (НЕ нарушать: `npx tsc --noEmit` до коммита, деплой = push в main, коммиты с Co-Authored-By, env-готча с ANTHROPIC_* при локальном dev).



Большая сессия 4 июня — всё задеплоено в `main`, прогнано вживую (тест-аккаунт сервис-ключом + dev-сервер через
Claude Preview). Кратко сделано (детали — в разделах ниже + `git log`):
1. **Диктовка везде → запись→Whisper** (Web Speech удалён): `VoiceTextarea` показывает волну В ПОЛЕ, текст целиком
   в конце, как в чате. Коммит `48f2e7a`.
2. **Визарды не теряют данные**: авто-сейв в localStorage + авто-восстановление + `beforeunload` + «Начать заново»
   (WarmupWizard — вкл. сгенерированный план; ProjectWizard; UnpackingInterview). Коммит `c92fc97`.
3. **AI-редактор плана прогрева** больше не падает «повреждён или пуст» (принимает ДВЕ формы plan_data: черновик
   `{phases}` и сохранённый `{warmup_plan:{phases}}`) + **выгрузка плана в таблицу CSV**. Коммит `7ef508d`.
4. **Убран React-19 варнинг «script tag while rendering»**: свой `ThemeProvider` вместо `next-themes`. Коммит `d9db4bf`.
5. **Контент НИКОГДА не показывается сырым JSON/кодом** — полный аудит всех поверхностей (генератор, AI-редактор,
   чаты, экспорт, `StructuredContentView`); `cleanMarkdown` флэттенит JSON-блобы. Коммиты `f96116f`, `540b56d`.
6. **AI-подбор трендов** (`/api/ai/suggest-trends`): кнопка «Подобрать» на `/projects/[id]/trends` и `/admin/trends`
   → список кандидатов с выбором. Веб-поиск (Haiku) + конкуренты + рилз. Селектор «по нише / популярные». `12402f3`, `0c61087`.
7. **Еженедельный авто-рефреш системных трендов** — Vercel Cron (`vercel.json`, Пн 06:00 UTC → `/api/cron/refresh-trends`).
   **`CRON_SECRET` владелец ДОБАВИЛ в env Vercel + сделал редеплой (4 июня).**

**✅ ПРОВЕРЕНО (4 июня, вечер): еженедельный cron трендов РАБОТАЕТ в проде.** Проверено:
- **Auth.** `CRON_SECRET` реально стоит в env Vercel: прод-роут без токена / с неверным отдаёт `403 {"error":"Forbidden"}`
  — это ветка `if(secret)`; если бы секрета не было, отдавал бы `"Forbidden (set CRON_SECRET)"`. Vercel сам шлёт
  `Authorization: Bearer <CRON_SECRET>` (документированное поведение) — код проверяет ровно это. Неверный/пустой токен → 403.
- **Пайплайн.** Прогнан end-to-end на dev-сервере против ПРОД-Supabase тем же кодом ветки `Bearer` (локально задал
  `CRON_SECRET` + `Authorization: Bearer …`): `200`, вставилось 18 трендов (`created_by IS NULL`, `scope=system`,
  ~2м25с). Веб-поиск (Haiku) → синтез (Sonnet) → delete авто-партии → insert. Замена по `created_by IS NULL` работает.
- **Состояние БД сейчас:** оставлено 12 системных трендов (6 «популярные» `niches=null` + 6 под нишу «Продюсирование»);
  мусорная партия под нишу «fs» (тест-проект) удалена по решению владельца. Крон заменит авто-партию в Пн 8 июня.
- **⚠️ ГОТЧА ТЕСТ-ОКРУЖЕНИЯ (важно для будущих ЛОКАЛЬНЫХ тестов AI-роутов):** Claude Code экспортит в шелл
  `ANTHROPIC_AUTH_TOKEN` / `ANTHROPIC_BASE_URL` / `ANTHROPIC_API_KEY`; `next dev`, запущенный через Bash, НАСЛЕДУЕТ их,
  а Next НЕ перетирает уже заданные `process.env` значениями из `.env.local`. Итог — SDK берёт OAuth-токен харнесса и
  шлёт `Authorization: Bearer …` на гейтвей → `401 "Invalid bearer token"`, все батчи падают (`inserted:0`, report `-1`).
  Лечится запуском dev с `env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN -u ANTHROPIC_BASE_URL -u ANTHROPIC_CUSTOM_HEADERS npm run dev`
  — тогда ключ берётся из `.env.local`. Это НЕ баг прода/крона (на Vercel этих переменных нет).
- **Ниша-тай-брейк (принято, НЕ чиним):** крон берёт топ-2 ниши активных проектов по числу проектов; пока у всех ниш
  по 1 проекту, «топ-2» произвольный — взял «fs», пропустил «Фитнес…». Владелец решила оставить как есть (с ростом
  клиентов реальные ниши получат счётчик > 1 и поднимутся; тест-проект «fs» удалит вручную).

**🔜 ПЕРВЫМ ДЕЛОМ в новой сессии:** крон-проверка закрыта. Остался один лёгкий хвост — после Пн 8 июня заглянуть в
Vercel → Cron Jobs/Logs, что реальный авто-запуск дал 200 (единственный незакрытый риск — спецсимволы/перенос строки
в значении `CRON_SECRET`, ломающие заголовок; вероятность низкая). Дальше — следующий большой блок: визуальный контент
— карусели/посты/сторис (НАЧАТО, см. ниже).

### 🎨 Визуальный контент — слайды/посты/сторис (НАЧАТО 7 июня; движок готов, WIP в рабочем дереве)
Референсы каруселей получены (4 шт., владельца). Владелец уточнила ВИДЕНИЕ: не один дефолтный стиль, а
**индивидуальный бренд-кит на проект** — пользователь грузит примеры своего фирменного стиля (сториз/посты/закрепы) +
называет/грузит шрифты + лого → движок делает В ЕГО СТИЛЕ **карусели, посты И оформление СТОРИС по его фото/видео**
(сторис — её «вау»-фича, «этого никто не делает»; видео — позже). Выбор владельца: «по максимуму сразу» + «сразу
индивидуальный» + добавить текстуру бумаги. ОЖИДАНИЕ (проговорено): не пиксельная копия макетов, а совпадение по
цветам/шрифтам/лого/типу фона на умных шаблонах.
**Сделано (движок рендерит вживую, `tsc` чисто):**
- `lib/carousel/engine.tsx` — бренд-параметризованный движок на **`next/og`** (НЕ @vercel/og — встроен; прецедент
  `app/icon.tsx`). Тема `CarouselTheme` = бренд-кит. `FORMATS`: carousel 1080×1350, post 1080×1080, story 1080×1920.
  Шаблоны Cover/Content/CTA/Post/Story. `planSlides()` маппит `structured_data.carousel` (cover/slides[]/last_slide).
- `app/api/carousel/render/route.ts` — POST {carousel|slide, format, index, brand}→PNG; GET `?demo=1&format=…&i=N` (глазная проверка).
- `public/fonts/Montserrat-*.ttf` (кириллица, читаются в рантайме readFile). `public/textures/paper.png` (sharp, 260K).
- Инлайн-акцент розовым через `**маркеры**` (Satori не умеет inline-span → токенизация в flex-wrap чипы); эмодзи twemoji.
- Доказательства: `~/Downloads/ama-slides-poc/` (карусель на бумаге, пост 1:1, сторис 9:16).
- **UI «Сделать слайды-картинки»** — `components/carousel/CarouselSlides.tsx`, встроен в `StructuredContentView`
  (карусельная ветка → виден на ВСЕХ поверхностях с каруселью): кнопка → модалка с превью каждого слайда (POST
  `/api/carousel/render` по слайду) → скачать по одному + «Скачать всё (ZIP)» (`jszip`). **Прогнано вживую в браузере
  (Claude Preview): 4 слайда отрендерились + ZIP без ошибок.** Берёт бренд проекта по `projectId`.
  **ВАЖНЫЙ ФИКС (коммит `125e036`):** чат генерит карусель ТЕКСТОМ (не structured_data), поэтому раньше из чата
  (главный флоу из контент-плана) кнопки слайдов НЕ было — тупик. Теперь `CarouselSlides` принимает `sourceText` и при
  открытии структурирует текст через **`/api/carousel/structure`** (Sonnet, text→{carousel}, расставляет `**акценты**`,
  ничего не выдумывает) → рендерит. Кнопка «🖼 Сделать слайды-картинки» добавлена в сообщения ассистента для каруселей
  (`assistant/page.tsx`, по `genContext.type==='carousel'` или тексту со «Слайд N»). Bridge проверен на реальном тексте.
- **«Сделать картинку поста»** (коммит `26a13dd`): `components/carousel/PostImage.tsx` — кнопка на постах в чате (`genContext.type==='post'`): заголовок (из 1-й строки, редактируется) поверх СВОЕГО фото (загрузка, `kind='post'`) ИЛИ на фирменном фоне; формат **4:5 (`post45`, дефолт, лучший охват в ленте)** или 1:1. Движок: добавлен шаблон `Photo` (фото + читаемый заголовок на тёмном градиенте, белый текст + акцент) + **case `'photo'` в `renderSlide`** (его НЕ БЫЛО — kind:'photo' падал в Content; это был баг, теперь чинено) + формат `post45`. Проверено: и фото-пост, и пост на фирменном фоне рендерятся (примеры `~/Downloads/ama-slides-poc/08,09`). ВАЖНО: og `<img>` НЕ ходит по редиректам (picsum.photos 302 не грузился) — реальные загрузки = прямые Supabase-URL, ок.
  **ВИДИМОСТЬ (коммиты `1fc8c71`,`3d9d9d9`):** владелец не видела кнопку — была только при `genContext.type==='post'` (посты из контент-плана). Расширено: в чатах (`assistant` + `create`) карусель-ответ → «Сделать слайды», любой существенный (>150 симв.) НЕ-карусельный ответ → «Сделать картинку поста»; плюс кнопка добавлена на страницу `/generator` под текстом поста. Теперь видна на ВСЕХ поверхностях генерации.
**Грабли (важно для продолжения):**
- Next dev НЕ подхватывает правки `lib/carousel/*` в og-роут по HMR → **после правки движка ПЕРЕЗАПУСКАТЬ dev** (`rm -rf .next` + рестарт), иначе старый рендер.
- Satori: только flexbox (НЕ grid); div с >1 ребёнком — `display:flex`; `backgroundImage:'none'` НЕЛЬЗЯ (→ прозрачный градиент); шрифты только ttf/otf/woff (НЕ woff2); градиенты фейдить в прозрачный ТОГО ЖЕ цвета (не `rgba(0,0,0,0)` — иначе серый ореол).
- В Satori `<img src>` берёт АБСОЛЮТНЫЙ URL — текстуру отдаём как `${origin}/textures/paper.png`.
**Осталось (next, порядок «всё параллельно»):** (1) Бренд-кит: миграция полей в `projects` + Storage-бакет (примеры/лого/шрифты) +
AI-извлечение палитры/шрифта/настроения из примеров (Claude vision) + ручная правка. (2) Загрузка кастомных шрифтов в
движок. (3) AI-обогащение генерации: расставлять `**акценты**`+эмодзи, маппить контент в шаблоны; сценарий сторис →
кадры поверх медиа. (4) UI: кнопки «Сделать слайды»/«Оформить сторис» → превью + загрузка фото (сторис) + «скачать всё»
zip (есть `jszip` 3.10). (5) Видео-сторис — позже (ffmpeg/Shotstack).
**ОБНОВЛЕНИЕ (вечер 7 июня):** п.4 для КАРУСЕЛЕЙ уже сделан и прогнан вживую (кнопка+превью+ZIP). Из п.4 осталось:
UI бренд-кита, загрузка фото для сторис, кнопка «Оформить сторис». Порядок дальше: бренд-кит (миграция+Storage+AI-vision) →
кастом-шрифты в движок → AI-обогащение генерации (`**акценты**`+эмодзи) → UI сторис-по-фото → видео.
**Мелочь:** в текстовых карточках `StructuredContentView` `**маркеры**` показываются буквально — когда AI начнёт их
расставлять, стрипать `**` в карточном виде (картинкам они нужны, тексту — нет).

### 🤖 AI-ассистент: проактивность + доступ к «Готовому» + бренд-кит (запросы владельца 7 июня)
Владелец попросила 3 вещи: **(A)** индивидуальный бренд-кит на КАЖДОГО юзера (из его Instagram / загруженных примеров / вручную), не централизованный; **(B)** при переходе из контент-плана в чат AI САМ предлагает 2-3 угла/формата/хука под тему (из материалов блогера), по рилзам — тренды под тему; **(C)** AI достаёт и правит сохранённое «Готовое» прямо в чате.
- **(C) СДЕЛАНО** (коммит `09bec2a`): `app/api/ai/chat/route.ts` — хелпер `buildSavedBlock()` инжектит `saved_content` пользователя (этого проекта вперёд, бюджет ~9k симв.) в системный контекст + инструкция «бери из Готового, не проси прислать текст». Оба режима (проект/standalone). Проверено: колонки `saved_content` совпали, query работает (2 реальные записи). Live-чат-тест в браузере — за владельцем (юзает чат ежедневно).
- **(A) Бренд-кит: миграция готова — `supabase/migrations/015_brand_kit.sql`** (поля `brand_*` в `projects` + публичный бакет `project-brand`). **⏳ ВЛАДЕЛЕЦ ПРИМЕНЯЕТ.** Затем код: загрузка примеров/лого/шрифта в `project-brand` (серверный роут, service-role + проверка владельца) → AI-vision (Claude) извлекает палитру/шрифт/настроение → пишет в `brand_*` → движок берёт тему из проекта (`themeFromBrand` уже умеет; нужно прокинуть brand из БД в `/api/carousel/render` и в `CarouselSlides`). Нужны 2-3 примера оформления от владельца для настройки распознавания.
- **(B) НЕ сделано — следующий шаг:** новый `/api/ai/suggest-angles` (углы/форматы из RAG-материалов + тренды по нише/теме, для рилзов адаптированные) + сделать опенер ассистента проактивным (`app/(dashboard)/projects/[id]/assistant/page.tsx` `buildOpener` сейчас статичный, ждёт ввода — заменить на сгенерированные предложения).

### 📊 Таблицы-выгрузки → XLSX (СДЕЛАНО 7 июня, коммиты `bd92e94` + `6d61d69`)
Владелец: все выгрузки были «каша» (CSV открывался ОДНОЙ колонкой в RU Excel/iOS — проблема разделителя запятой). Перевёл на **настоящий XLSX** (`xlsx` lib) + перестроил колонки. Все 4 проверены на РЕАЛЬНЫХ данных через service-role:
- **План прогрева** (`lib/planCsv.ts` → `planToAoa`/`downloadPlanXlsx`): `День | Фаза | Линия блога | Тема | Пост | Сторис | Рилз | Карусель | Email`. Сплитит префикс «[ЛИНИЯ: X] тема» в отдельные колонки. Кнопки: `WarmupPlanList` + `WarmupWizard`.
- **Карта смыслов** (`lib/researchTables.ts` → `meaningsMapToAoa`): `Тип | Категория | Формулировки участников | Идеи контента` (группировка по типу; добавлен тип «Преимущества эксперта» если появится).
- **Исследование аудитории** (`audienceResearchToAoa`): ПИВОТ — участники по строкам, вопросы по колонкам. ⚠️ если у каждого интервью РАЗНЫЕ вопросы (как в «19 мая») → таблица широкая/разреженная (54 колонки); чисто выходит при общем наборе вопросов. На будущее: `research-analyze` задавать ЕДИНЫЙ набор вопросов. Не-табличные материалы того же типа (заметки/сегменты) → fallback HTML.
- **Анализ конкурентов** (НОВОЕ): `/api/ai/analyze-competitors` (forced-tool+ретрай как suggestTrends) читает скрейп конкурентов (+ свой аккаунт) → сравнительная таблица (ник/подписчики/темы/форматы/что заходит/тон/регулярность/сильные стороны/вывод) → `CompetitorAnalysis.tsx`: превью-таблица + XLSX. Кнопка в разделе «Конкуренты». Проверено на 4 реальных конкурентах.
- Общий хелпер: `lib/utils/xlsxTable.ts` → `downloadXlsx(filename, sheet, aoa)`.
**Бренд-кит (A) — СДЕЛАНО 7 июня (коммит `7dffa5e`):** индивидуальный фирменный стиль на КАЖДЫЙ проект.
- `/projects/[id]/brand` (страница): загрузка примеров стиля → «Распознать стиль» (Claude **vision** → палитра/фон/настроение/шрифт) → правка цветов/ника/лого → ЖИВОЕ превью слайда → сохранить.
- Роуты: `app/api/brand-kit/upload` (Storage `project-brand`, service-role + проверка владельца), `.../analyze` (vision → save в `projects.brand_*`), `app/api/brand-kit` GET/POST (чтение/ручная правка).
- Движок `themeFromBrand` теперь **dark-aware** (тёмный фон → белый текст + светлый muted; `hexLum`/`lighten`); `Backdrop` убрал тёплые радиалы (надёжно для ЛЮБОГО цвета бренда; для paper остаётся текстура). `CarouselSlides` грузит бренд по `projectId` (прокинут опционально через `StructuredContentView` из контент-плана + генератора) → слайды в стиле бренда. Ссылка на страницу стиля — в модалке слайдов («🎨 Стиль») + прямой URL.
- ПРОВЕРЕНО вживую: vision извлёк бренд Августы верно (акцент `#FF2D78`, тёмный фон `#1A1A1A`, белый текст, solid); тёмный слайд отрендерился идеально. ⚠️ Саму страницу `/brand` в браузере НЕ кликал (под авторизацией — 307 на логин); **владелец тестит флоу вживую** (загрузка → распознать → сохранить → сделать слайды). Дискаверабилити: ссылка пока только в модалке слайдов + URL; в основное меню проекта НЕ добавлена (на будущее).
- Примеры стиля владельца в `~/Downloads/примеры` (светлые фото+розовый маркер; тёмные с белыми плашками+коллажи) — JPG (для vision) + MOV (видео-сторис, позже).
**Проактивные подсказки (B) — СДЕЛАНО 7 июня (коммит `7b59312`):** `app/api/ai/suggest-angles` (forced-tool + RAG-материалы блогера + тренды по нише) → опенер ассистента (gen=1, `app/(dashboard)/projects/[id]/assistant/page.tsx`) показывает тему СРАЗУ, затем подгружает 2-3 угла/хука/«почему зайдёт» из материалов + тренд под рилз; graceful fallback на статичный опенер. Проверено на реальных материалах (ниша «Фитнес» — угла грамотные, со ссылкой на боли аудитории). UI-флоу под авторизацией — владелец тестит вживую.
**Сторис-билдер (фото) — СДЕЛАНО 7 июня (коммит `48a9786`):** `/projects/[id]/stories` — загрузка фото + сценарий → `/api/ai/plan-stories` (раскадровка: минимум текста на кадре, голос бренда, по нарастающей → CTA) → движок рендерит каждый кадр 9:16 поверх фото в бренде → превью + PNG/ZIP. `brand-kit/upload` принимает `kind='story'` (папка `stories/`). Проверено: раскадровка на реальных материалах (фитнес) — кадры грамотные, с цитатами клиента; рендер сторис проверен ранее. Бренд-страница ссылается на сторис, и наоборот.
**Очередь (крупное):** видео-сторис — НАЛОЖЕНИЕ текста на ВИДЕО (фото-сторис уже есть). Владелец выбрала «сначала фото»; для видео нужен движок: **Shotstack** (ключ, есть free sandbox) или **ffmpeg.wasm** в браузере (без ключа, тяжело/рискованно на iOS). AI-раскадровка `plan-stories` переиспользуется — останется прикрутить burn текста-оверлея (можно рендерить оверлей прозрачным PNG через `next/og`) поверх видео.
### 🚀 АКТИВАЦИЯ (начато 7 июня) — направление выбрала владелец: «тест + находимость + онбординг»
- **Находимость СДЕЛАНО** (коммит `9f7e6b8`): плитки «Фирменный стиль» (`/brand`) и «Оформление сторис» (`/stories`) добавлены в меню проекта (`app/(dashboard)/projects/[id]/page.tsx`). Слайды и анализ конкурентов — уже кнопками в контексте.
- **Смоук-тест СДЕЛАН вживую** (Claude Preview, логин QA `ama-qa-bot@gmail.com`/`QaTest!23456`): бренд- и сторис-страницы рендерятся под авторизацией, у бренда живое превью генерит реальный слайд (1080px), логин-флоу работает (native-setter + requestSubmit на `/login`). QA-юзер оставлен (переиспользуемый), тест-проект удалён.
- **Онбординг СДЕЛАНО** (коммиты `f1ed14f`, `d66179f`): слайд про визуал/бренд добавлен; показывается при КАЖДОМ входе на дашборд с галочкой «Больше не показывать» (только она пишет `onboarding_done=true`; дефолт теперь `?? false`). **ОБНОВЛЕНО (коммит `952a239`):** галочка «больше не показывать» теперь пишет `onboarding_done` СРАЗУ при клике (реальный opt-out, как бы ни закрыли); флаг существующих юзеров уже СБРОШЕН в false (через service-role) — все увидят обновлённый онбординг и смогут отключить.
- **Лендинг обновлён** (коммиты `29986b4`, `c5dc296`): дизайн оставлен; **цены синхронизированы с утверждёнными** (были устаревшие: free-Старт/2990/5990 + «14 дней» → Соло/Про/Продюсер 4900/14900/29900 ₽, 2 мес триал, Соло «Оптимальный»-герой); **выдуманные отзывы УБРАНЫ** (владелец даст реальные позже), фейк-статистика «2847» убрана; добавлены блоки **«Картинки в твоём стиле»** (визуал/бренд) и **«Для продюсеров»** (мульти-клиент); хедлайн/маркер/метадата теперь про визуал; год 2026; правка «Plan»→«План». Проверено вживую (рендерится, цены верные). **ОБНОВЛЕНО (коммит `43b5eea`): лендинг сделан ТАРИФ-ОНЛИ** — убраны все «бесплатно / 2 месяца / без карты» (2-мес доступ — приватная договорённость для отдельных участников, не публичный оффер); CTA нейтральные («Начать»/«Выбрать»). **Лендинг TODO:** реальные отзывы (от владельца, добавим когда появятся), ссылки в футере + соцсети (владелец пришлёт: privacy/terms/поддержка/Instagram/Telegram URL).
- **Осталось в активации:** владелец прогоняет ПОЛНЫЙ флоу с реальными фото/материалами (upload→распознать→собрать — файловую загрузку через Preview не протестить); починить баги, что всплывут.
**Мелочи на потом:** стрип `**` в карточном тексте `StructuredContentView` (когда AI расставляет акценты); единый набор вопросов в `research-analyze` для чистого пивота; авторизация на `/api/carousel/render` (сейчас открыт, утечки нет — но бесплатный рендер-эндпоинт).
**Стратегический бэклог (по приоритету, обсуждено 7 июня):** активация (идёт; онбординг-слайды обновлены под визуал, коммит `f1ed14f`) → деньги (триал→оплата: провайдер/тарифы/гейтинг/экран апгрейда/уведомления; **рекомендация по ценам — `PRICING.md`, ждёт утверждения владельцем+партнёром**) → продюсерам мульти-клиент/команда → автопостинг (Telegram) → видео (рилз-титры/видео-сторис, Shotstack/Replicate ключи) → аналитика использования.
**Модель + экономика (решено 7 июня, коммит `89ce60d`):** `MODEL = claude-opus-4-8` (Opus 4.8 на ВЕСЬ контент — решение владельца «всегда сильнейшая модель = ценность»; веб-поиск трендов остаётся на Haiku). **Prompt caching** включён в `/api/ai/chat` + `/api/ai/generate` (хелпер `buildCachedSystem` в `lib/ai/client.ts`) — кэширует большой RAG/системный блок (cache_read проверен вживую). ⚠️ Остальные роуты (week-brief, warmup-plan, edit, competitor, angles, plan-stories, suggestTrends-синтез) теперь тоже на Opus, но БЕЗ кэша — **добавить `buildCachedSystem` и туда** (снизит стоимость). Цены/маржа/расчёты — `PRICING.md` (маржа добивается ценами+лимитами+адд-онами; полный отчёт по юнит-экономике там же).

> Ранее (3 июня): клиентские тренды проверены вживую; latent-баг `toArray()` (Sonnet возвращал массив forced-tool
> строкой) починен в week-brief/warmup-plan (коммит `7305030`) — см. раздел «Аудит сервиса».

## Что это за продукт

**AMA (amaproduct.com)** — AI-сервис для блогеров/экспертов: «личный продюсер + мозг контента».
Не генератор общего назначения, а система, которая впитывает голос конкретного человека
(Tone of Voice, его посты, кастдев, карту смыслов, линии блога, анализ его и конкурентов
Instagram) и пишет контент ИМЕННО как он, под методологию прогрева/запуска.

Владелец — Августа Василик, продюсер микроблогеров. Клиенты приходят из основного продукта
(продюсирование) и нативно переводятся в сервис. ~50 клиентов на бесплатный триал 2 мес.

## Стек / процессы

- Next.js 16 (App Router, Turbopack) + React 19, TypeScript. Supabase (Postgres+Auth+Storage+RLS).
- Claude (`lib/ai/client.ts`, `MODEL`). OpenAI Whisper (аудио). Apify (Instagram скрейп, `APIFY_TOKEN`).
- Хостинг Vercel **Pro** (maxDuration 300). Деплой = push в `main`.
- **Миграции применяет владелец вручную** (Supabase SQL Editor) — у ассистента нет DDL-доступа.
  Давать готовым SQL-блоком. Локального `APIFY_TOKEN` нет — Apify тестировать только в проде.
- Сервис-ключ Supabase есть в `.env.local` → можно скриптами через `node -e` читать/чинить БД,
  создавать тест-аккаунты, бэкофилл и т.д.

### Обязательные правила разработки (не нарушать)
- **ВСЕГДА `npx tsc --noEmit` ДО коммита** (ловили TS-ошибки уже в билде Vercel).
- Локальная сборка на macOS: `cp node_modules/lightningcss-darwin-x64/lightningcss.darwin-x64.node node_modules/lightningcss/`
  перед `npx next build`. НЕ коммить изменения package.json от этой установки (откатывать).
- Коммиты заканчивать `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- Vercel иногда «слетает» с транзиентной ошибкой Next 16 — если tsc+build локально чистые, это сборщик; пустой коммит/redeploy.

### Как тестировать вживую (воспроизводимо — этим пользовались в июне 2026)
Фичи за авторизацией/RLS проверяются так (НЕ просто tsc/build):
1. **Тест-аккаунт сервис-ключом** (node + `@supabase/supabase-js`, env из `.env.local`): создать confirmed-юзера
   `auth.admin.createUser({email,password,email_confirm:true})`, засидить проект/материалы/`warmup_plan` (status
   'approved', `plan_data.warmup_plan.phases[].daily_plan[]` с `briefs`). Креды: `ama-qa-bot@gmail.com` / `QaTest!23456`.
   Очистка СТРОГО по порядку (FK): content_items→warmup_plans→project_materials→projects→saved_content→content_trends→
   profiles, ПОТОМ `auth.admin.deleteUser` (иначе «Database error deleting user» из-за FK профиля). `listUsers` —
   пагинируй (perPage 200, до ~20 стр.), иначе юзера не находит.
2. **Дев-сервер через Claude Preview MCP**: `.claude/launch.json` = `{name:"ama-dev", runtimeExecutable:"npm", runtimeArgs:["run","dev"], port:3000}` → `preview_start`. Мобильный вид: `preview_resize preset:"mobile"`.
3. **Логин в браузере**: `preview_fill`/`preview_click` НЕ обновляют React-стейт надёжно. Рабочий способ — `preview_eval`:
   нативный сеттер `value` + dispatch `input`, затем `form.requestSubmit()`. Навигация — `window.location.href` (иногда
   нужен 2-й раз после старта сервера). Клики — `preview_eval` (найти кнопку по тексту + `.click()`). Замеры — `getBoundingClientRect`.
4. **Убрать за собой**: удалить тест-юзера+данные, `.claude/launch.json`, временные `qa-temp.mjs`/тест-скрипты, `.next`.
DDL ассистент применить НЕ может — даёт SQL, владелец применяет в SQL Editor.

### Ключевые модули
- `lib/ai/rag.ts` `buildRAGContext` — подтягивает материалы в генерацию. `ALWAYS_INCLUDE` = ВСЕ
  существенные типы материалов (голос, кейсы, воронка, стратегия, тактика, конкуренты, кастдев,
  карта смыслов, распаковка, линии, Instagram) подтягиваются ВСЕГДА (slice 3000). Используют:
  `/api/ai/generate`, `/api/ai/edit`, `/api/ai/chat`.
- `lib/ai/prompts/system.ts` — системный промпт (голос/словечки/форматирование/без хэштегов).
  Анти-AI-тэллы: `AI_TELLS_TO_AVOID` в `content-brain.ts` (тире «—» НЕ ставить — главный палящий признак;
  без телеграфных обрывков «Не А. Не Б. А В.»; офферы конкретные, не «скажу конкретно»/«разбираю внутри»).
  Вшит во ВСЕ генераторы: chat (через buildSystemPrompt + standalone), generate, edit, и в валидатор постов.
- `lib/ai/prompts/content-brain.ts` — хуки, фазы, схемы, `getViralReelsFramework`.
- `lib/supabase/upsertMaterial.ts` — ручной upsert материалов (нет unique-констрейнта на triple).
- `lib/completeness.ts` — единая формула «полноты базы». `lib/cleanText.ts` — `cleanMarkdown`.

## Что СДЕЛАНО

- Исследование аудитории: аудио→Whisper (через Supabase Storage, обход лимита Vercel)→таблица→карта смыслов (CSV).
- Материалы: ToV (+«из моих текстов»→сохраняется как style_examples), распаковка, карта смыслов, линии,
  кейсы, воронка, стратегия, тактика, **Instagram (свой+5 конкурентов через Apify)**, импорт из др. проекта.
- План прогрева + AI-правка (вкл. черновик в мастере, шаг 8; правит и `meaning`, и брифы форматов).
- Контент-план: двухшаговый. Шаг 1 — «Сгенерировать план» (AI расписывает брифы под форматы дня).
  Шаг 2 — кнопка «Сгенерировать рилз/сторис/пост» открывает **чат-окно** (`/projects/[id]/assistant?gen=1&day=&type=&phase=&brief=&back=content-plan&week=N`):
  сверху сообщение AI с темой дня + формат-подсказкой (сторис коротко, рилз по секундам), оно ЖДЁТ ввода
  (не авто-генерит) → юзер дописывает детали → AI пишет юнит в чате (правится как в ChatGPT). На ответе:
  «Сохранить в Готовое» (библиотека) + «В план» → `/api/content-items` сохраняет как content_item дня (✓).
  Назад — кнопка «← Контент-план» (backHref с `?week=N`, страница плана читает `?week`), возвращает на ТУ неделю.
  Чистый вывод: send() шлёт `genFormat` → chat-роут включает «режим генерации» (только сам текст, без вводных
  «Окей/Вот/Держи»), чтобы в сохранённое не попадала болтовня AI. Опенер (`opener:true`) — без кнопок сохранения.
  Уже сгенерированное в плане: «Посмотреть»/«Обновить» (Обновить = снова в чат).
  ВАЖНО (история, не переоткрывать вслепую): инлайн-панель деталей была (июнь 2026), но владелец попросила
  заменить на чат-окно — генерить инлайн в плане ей неудобно. `/api/ai/generate` (структурный) больше НЕ
  зовётся из контент-плана (остался для стр. `/generator`).
- **AI-ассистент** (`/projects/[id]/assistant`) — чат, знает проект, пишет голосом.
- **«Создать»** (`/create`) — чат, есть селектор проекта (по умолчанию — последний проект → полный контекст;
  «без проекта» → только методология). Нижняя навигация «Создать» ведёт сюда.
- Виральный движок рилз. Хэштеги убраны везде. StructuredContentView (рилз/карусели/сторис читаемо).
- **Календарь** (`/projects/[id]/calendar`), **Результаты** (`/projects/[id]/results` — замкнутый цикл:
  внёс охваты → топ-посты становятся эталоном стиля).
- **Тренды месяца** (`/admin/trends`) + **Виральные рилз-референсы** (`/admin/viral-reels` системные и
  `/projects/[id]/viral-reels` пользовательские) → вплетаются в контент-планы по нише. Бэкенд `/api/viral-reels`
  (Apify скрейп рилз → Whisper расшифровка → AI-разбор → сохранение).
- **AI-подбор трендов** (`/api/ai/suggest-trends`, коммит `12402f3`, июнь 2026): кнопка «Подобрать тренды» на
  `/projects/[id]/trends` (клиенту) и `/admin/trends` (системные, ниша-инпут) → список из 6-8 кандидатов с
  чекбоксами → выбранные падают в тренды. Обоснованность: (1) **живой веб-поиск** (`web_search_20250305` на
  **`MODEL_HAIKU`** — Haiku ищет в вебе ~6с против ~80с у Sonnet!), (2) конкуренты проекта, (3) залетевшие рилз →
  Sonnet синтезирует. Дедуп против существующих; если всё вырезалось — отдаём недедуплицированное (не падаем в 0);
  веб-поиск деградирует gracefully. scope `project` (проверка владельца) / `system` (только админ). Каждый юзер
  получает тренды ПОД СВОЮ нишу (берётся `project.niche` + его конкуренты/рилз; week-brief фильтрует системные по нише).
  - **Селектор «По моей нише / Популярные сейчас»** (коммит `0c61087`) — как селектор проекта в «Создать». `mode`
    `niche` (дефолт, под нишу) / `popular` (широкие тренды не из ниши, чтобы протестировать). Общая логика —
    `lib/ai/suggestTrends.ts` (переиспользует эндпоинт И cron). **Надёжность:** forced-tool ИНТЕРМИТТЕНТНО возвращает
    пустой список (~1 из 3) → **ретрай до 4×** до непустого (лог `empty result, retry N/4, stop=tool_use`). НЕ убирать ретрай.
  - **Еженедельное авто-обновление** (`vercel.json` cron Пн 06:00 UTC → `/api/cron/refresh-trends`): обновляет
    СИСТЕМНЫЕ тренды — general «popular» + топ-2 ниши активных проектов (последовательно — Anthropic rate-лимитит
    concurrency). Авто-тренды помечены `created_by IS NULL` (ручные из админки всегда `created_by=admin.id`) → старая
    авто-партия заменяется без миграции. Auth: **`CRON_SECRET`** (владелец добавляет в env Vercel; иначе 403). На дев —
    проверка заголовком `x-vercel-cron`. **ПРОВЕРЕНО вживую 4 июня (auth + пайплайн, 18 трендов вставилось) — см. ✅-блок вверху файла.**
- **Админ-аналитика** (`/admin/analytics`) — по юзерам + кнопка «Триал 2 мес».
- Голос везде через **запись→Whisper** (хук `lib/useVoiceRecorder.ts`: пишет полное аудио → ОДИН вызов
  `/api/ai/transcribe-voice` на стопе → текст возвращается ЦЕЛИКОМ). Никакого пофразового вывода и потери хвоста.
  В чатах (`/create`, ассистент) — `components/ui/ChatComposer.tsx`: запись разворачивается в **панель во всю
  ширину** (отмена · живая волна + таймер · подтвердить), как «Release to send» у ChatGPT.
  - **`components/ui/VoiceTextarea.tsx`** — поле для всех форм: во время записи textarea ЗАМЕНЯЕТСЯ на in-field
    панель (волна+таймер+отмена/✓), как в ChatComposer; затем «Распознаю речь…»; затем текст вставляется разом.
    Используется в style-bank, generator, trends, knowledge, warmup-wizard, admin. `VoiceRecordButton` — компактная
    кнопка (AiEditChat).
  - **Web Speech API ПОЛНОСТЬЮ УДАЛЁН** (июнь 2026, коммит `48f2e7a`). Раньше материалы/знания
    (`UnpackingInterview`, `KnowledgeUploader`, `KnowledgePageClient`) использовали `SpeechRecognition` →
    отставание, пофразовость, потеря последних фраз при остановке (фидбэк владельца). Переведены на `VoiceTextarea`.
    НЕ возвращать Web Speech — он не работает в webview/iOS и теряет хвост.
- Чат: читается сверху вниз. Логика пина — хук `lib/useChatPin.ts`: вопрос пинится к верху явным `scrollTo`
  (rect-based, НЕ scrollIntoView — тот промахивался на мобиле) + **динамический** нижний спейсер (заполняет
  пустоту под коротким ответом, схлопывается в 0 под длинным). КЛЮЧЕВОЕ: `lastUserRef` вешается на ПОСЛЕДНЕЕ
  сообщение юзера через `lastIndexOf('user')` (а не «последнее в массиве» — иначе реф отваливался, как только
  приходил ответ ассистента, и вопрос переставал держаться сверху). Без markdown-символов.
- **Вёрстка фулл-скрин чатов (НЕ ломать):** корни `/create` и `/assistant` — `h-full` (НЕ `100vh`!), и
  `components/shared/PageTransition.tsx` — `h-full` (НЕ `min-h-full`). Иначе ломается цепочка высот → `<main>`
  двойной скролл → поле ввода уезжает под нижнюю навигацию (юзер не находит). Обычные страницы скроллятся как
  раньше (контент перерастает `<main>`). Шапка контент-плана — `sticky top-0`, чтобы «AI-правка» не уезжала.
- Чат-роут `/api/ai/chat`: maxDuration 300, max_tokens 8000 + авто-дописывание при stop_reason=max_tokens
  (цикл до 4 раундов) — чинит обрыв «5 рилзов» на 2-м (раньше резал 60-сек лимит функции).
- Выгрузка текстовых материалов → стилизованный HTML (нормальный шрифт).
- **Библиотека «Готовое»** (`/library`, таб в нижней навигации + сайдбаре): одобренный контент сохраняется
  в папку, сгруппирован по типам, с копированием/удалением. Кнопки «Копировать»+«Сохранить» — СВЕРХУ каждого
  ответа ассистента (в `/create` и ассистенте) и в просмотрщике контент-плана. Хелперы: `lib/saveContent.ts`,
  `lib/contentToText.ts` (флэттенит структурный контент в текст), `components/content/SaveButton.tsx`.
  Таблица `saved_content` — см. «Миграции».

## КРИТИЧЕСКОЕ исправлено (важно помнить)
- **Регистрация была сломана для ВСЕХ** (триггеры профиля теряли search_path → падёж). Исправлено SQL:
  `handle_new_user` + `auto_generate_referral_code` с `SET search_path = public, extensions`. Применено.
- Тариф `'trial'` добавлен в CHECK `subscription_tier` (для кнопки триала). Применено.
- **Потеря данных в визардах** (фидбэк тест-юзера, видео, коммит `c92fc97`, июнь 2026): заполнила мастер «План
  прогрева», план сгенерировался, НЕ одобрила, ушла со страницы → всё пропало (сгенерированный план жил только в
  стейте `aiPlanData`, localStorage-черновик его не сохранял, восстановление было ручным баннером). Починено и
  сделано ОБЩИМ ПРАВИЛОМ для длинных форм: **авто-сейв в localStorage (debounce) + АВТО-восстановление при возврате
  + предупреждение `beforeunload` + кнопка «Начать заново»**. Применено в `WarmupWizard` (черновик включает
  `aiPlanData`+`planApproved`, возвращает на экран одобрения), `ProjectWizard` (`/projects/new`, ключ
  `ama_new_project_draft`), `UnpackingInterview` (ответы интервью). НЕ убирать этот паттерн; новые многошаговые
  формы делать так же. Очистка черновика — на успешном сохранении (`localStorage.removeItem`).
- **Тема: свой провайдер вместо `next-themes`** (коммит `d9db4bf`, июнь 2026): `next-themes` рендерил свой
  анти-флэш `<script>` ВНУТРИ клиентского компонента → React 19 спамил в консоль «Encountered a script tag while
  rendering React component» (dev-only, в проде вырезается, но засоряло). Заменено на лёгкий `ThemeProvider`+`useTheme`
  в `components/shared/ThemeProvider.tsx` (класс `light`/`dark` на `<html>` + localStorage, БЕЗ рендера `<script>`).
  Анти-флэш `<script>` теперь в server-layout `app/layout.tsx` (санкционированное место по докам Next — см. json-ld).
  `next-themes` больше нигде не импортируется (оставлен в package.json, безвреден). НЕ возвращать next-themes-провайдер.

### Аудит сервиса (июнь 2026) — исправлено, НЕ переоткрывать
- **maxDuration=300 на `/api/ai/generate`** (был 60 — тот же баг таймаута, что чинили в chat: убивало
  генерацию на полпути → контент не сохранялся И генерация не возвращалась). НЕ понижать.
- **`max_tokens: 8192` для JSON-типов** в generate + проверка `stop_reason==='max_tokens'` → рефанд+ошибка
  вместо сохранения битого JSON.
- **`phase_1..4` нормализуются в `content-brain.ts`** (`normPhase`/`PHASE_ALIAS` → niche/expert/product/objections).
  Это чинит week-brief и AI-правку (раньше для планов с phase_1..4 они молча падали на generic «awareness»).
  НЕ удалять нормализацию.
- **RAG `ALWAYS_INCLUDE`** теперь включает `interview_transcript` (+`additional`) — сырые цитаты кастдева.
- Чат «Стоп» сохраняет частичный ответ (читал устаревший `streaming` → теперь локальный `acc`).
- **AI-редактор плана (`AiEditChat` + `/api/ai/edit`)**: контент-план передаёт `weekContext` (текущая неделя +
  маппинг день-недели→номер-дня + текущие брифы). Промпт велит модели разрешать относительные дни («среда»,
  «эта неделя») по этому списку — иначе она угадывала номер и правила НЕ тот день. НЕ удалять weekContext.
  После «Применить» — подтверждение и в чат, и тостом. Пин в чатах теперь мгновенный (не smooth — тот промахивался).
  - **ДВЕ формы plan_data** (коммит `7ef508d`, июнь 2026): план приходит в редактор в ДВУХ формах — сохранённый
    `{ warmup_plan: { phases } }` (контент-план, `WarmupPlanList`) и черновик мастера `aiPlanData`
    `{ strategy_summary, phases }` (фазы на ВЕРХНЕМ уровне). Раньше роут читал только `plan_data.warmup_plan.phases`
    → на черновике (экран одобрения) всегда «План прогрева повреждён или пуст» и правка не давалась. Чинит хелпер
    `getPlanPhases()` (читает фазы из любой формы), применён и в валидации, и в применении `<changes>`. НЕ возвращать
    голый `plan_data.warmup_plan.phases`. Редактирование одобренного плана уже есть — кнопка «AI-правка» в
    `WarmupPlanList` (для любого статуса).
- **Выгрузка плана в таблицу (CSV)** (`lib/planCsv.ts`, коммит `7ef508d`): кнопка «Таблица» у каждого плана в
  `WarmupPlanList` + «Скачать в таблицу» на экране одобрения мастера. Колонки День/Фаза/Смысл + брифы по форматам.
  UTF-8 BOM + CRLF, чтобы кириллица открывалась в Excel/Google Sheets. Хелпер терпим к обеим формам plan_data.
- **tool-call массив как JSON-строка** (коммит `7305030`, 3 июня 2026): Sonnet интермиттентно возвращает массив
  forced-tool-call строкой, а не массивом. `generate-week-brief` (`days`) и `warmup-plan` (`phases`+`daily_plan`)
  теперь нормализуют через `toArray()` (массив ИЛИ JSON-строка). НЕ откатывать обратно на голый `Array.isArray`.
  Это единственные два роута с tool_use; `/api/ai/generate` парсит free-form JSON отдельно.
- **Контент НИКОГДА не показывается сырым JSON** (коммит `f96116f`, июнь 2026, фидбэк тест-юзера — «вижу весь код»):
  структурный контент (карусель/рилз/сторис) утекал JSON в трёх местах. (1) `generate`: для структурного контента
  `body_text` был null → генератор показывал `editedText = accText` = сырой стримленный JSON. Теперь `generate`
  всегда пишет чистый флэттен `body_text = contentItemToText(...)` рядом со `structured_data` (карточки рисует
  `StructuredContentView`). (2) `/api/ai/edit` content_item кормил модель `JSON.stringify(structured_data)` → она
  возвращала JSON. Теперь кормим `contentItemToText`, промпт ЗАПРЕЩАЕТ JSON/скобки/markdown, вывод чистится
  `cleanMarkdown`, и айтем становится текстовым (`structured_data` обнуляется; генератор убирает карточки после
  правки). (3) `StructuredContentView` fallback вместо `<pre>{JSON.stringify}</pre>` рисует читаемые строки.
  **Полный аудит всех поверхностей контента** (коммит `540b56d`): закрыты ещё два места — чаты (`cleanMarkdown`
  теперь режет ```-фенсы и флэттенит JSON-блоб через `jsonBlobToText`, no-op на обычном тексте; чат-промпт явно
  запрещает JSON) и экспорт (`lib/utils/export.ts` копирование/PDF/DOCX через `contentItemToText`, а не `body_text||''`).
  Общий хелпер `objectToReadableText` (в `lib/contentToText.ts`) — рекурсивный читаемый рендер любой формы.
  Проверено: генератор, AI-редактор, `StructuredContentView` (единственный рендерер `structured_data`), контент-план,
  библиотека, стиль-банк — чисто; календарь рисует только точки. Чат `/create` вживую → карусель «СЛАЙД 1 …» текстом.
  ИНВАРИАНТ: пользователь НИКОГДА не видит `{`, `"key":`, ```-фенсы, markdown — только готовый текст. НЕ возвращать
  `JSON.stringify` в UI/промпт; любой контент в UI/экспорт/буфер — через `contentItemToText`/`cleanMarkdown`.
- Низкий приоритет (не сделано): `refundGeneration` неатомарен (select+update); мёртвый код WeekView/ActivePanel
  в ContentPlanGrid; chat-стрим не сигналит об ошибке посреди потока (молча обрывает). На будущее.

## Миграции — статус
- 001–014 + фикс триггеров регистрации = ВСЕ ПРИМЕНЕНЫ владельцем. Таблицы content_trends, viral_reels существуют.
- **`saved_content`** (библиотека «Готовое») — ПРИМЕНЕНО владельцем (проверено вживую, работает).
- **`content_trends` + scope/project_id** (клиентские тренды) — **ПРИМЕНЕНО владельцем + ПРОВЕРЕНО ВЖИВУЮ (3 июня 2026).**
  Клиенты заводят свои тренды (`/projects/[id]/trends`, как виральные рилзы): свои + системные вплетаются в их
  контент-план. RLS = зеркало `viral_reels` (чтение system всем + свои project владельцу; запись system — админ,
  project — владелец проекта). Весь флоу прогнан — см. «✅ Клиентские тренды» вверху файла.
- **Аналитика**: `/api/admin/analytics` теперь через service-role клиент (RLS иначе показывал админу только себя). Без миграции.
- На будущее: новые миграции давать готовым SQL, владелец применяет в SQL Editor.

## Что НЕ доделано / очередь

1. **Оплата** (отложена владельцем до выбора провайдера: ЮKassa/CloudPayments РФ + Stripe мир).
   Счётчик генераций есть, платных планов нет. После — гейтинг лимитов по тарифу + экран апгрейда + уведомления о конце триала.
2. **Визуальный контент (следующий большой блок, направление согласовано):**
   - Карусели → реальные слайды-картинки через `@vercel/og` (HTML→PNG, без внешних API). НАЧАТЬ С ЭТОГО.
   - Посты→обложки. AI-картинки (Replicate). Рилз→видео с титрами (Shotstack). Ключи Replicate/Shotstack в env — ЗАГЛУШКИ.
   - Стиль ИНДИВИДУАЛЬНЫЙ на каждый проект (цвета/шрифт/лого), авто-подсказка из Instagram.
   - ЖДЁМ от владельца РЕФЕРЕНСЫ каруселей (стиль) перед стартом.
3. **Автопостинг** (отложен — нет смысла без визуала). Telegram: 1 общий бот (владелец создаёт, даёт токен),
   юзер добавляет бота админом в канал + вписывает @канал. Instagram: Meta App + ревью FB (недели, участие владельца) — в конце.
4. **Командная работа** (продюсер приглашает в проект) — таблица project_members + аккуратная RLS.
5. **Многоязычность** — в самом конце, перед мировым запуском.
6. **Мобильное приложение** — после стабилизации веба, нативное/React Native (диктовка будет работать нативно).

## Решения / ограничения (не переоткрывать)
- Диктовка: только запись→Whisper (Web Speech не работает в webview/iOS). Текст появляется ПОСЛЕ остановки (1-3с) — это норм, есть анимация.
- Instagram автопостинг невозможен без Meta App + ревью. Рилз «в кадре с человеком» сгенерировать нельзя (снимает сам).
- Большие AI-ответы: нормализовать вывод (customer_words мог прийти строкой), сканировать ВСЕ текстовые блоки (не content[0]),
  tool_use для гарантированного JSON где можно.

## Стратегия
- Тарифы (рекомендация, не вшиты): Solo $50 / Pro $99 / Продюсер $199-249. Главный сегмент — продюсеры (мульти-проект).
- Дифференциация: голос+методология+кастдев+анализ конкурентов — нет у Taplio/Hypefury/Predis и рус-агрегаторов.

## Как продолжать в новой сессии
1. Дать доступ к папке `/Users/macbook/project/ama`.
2. Прочитать ЭТОТ файл + `AGENTS.md`.
3. `git log --oneline -20`.
4. **ПЕРВЫМ ДЕЛОМ — выполнить проверку из блока «🔜 СТАТУС / ПЕРВЫМ ДЕЛОМ» вверху** (сейчас: проверить, что
   еженедельный cron трендов отрабатывает в проде). Способ живого теста — раздел «Как тестировать вживую».
5. Обновлять этот файл по ходу.
